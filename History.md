# History — 채팅 인덱스

## 이 문서의 지위

**여기에만 존재하는 결정을 만들지 않는다.**

적히는 것은 전부 **다른 문서나 채팅에 원본이 있는 포인터**다. 이 문서를
지워도 프로젝트가 성립해야 한다. 어기면 진실의 출처가 넷이 되고 문서
동기화 실패 지점이 하나 는다.

```
PRD.md  >  Tasks.md  >  History.md
계약·수용 기준   실행 순서·근거    "그 논의가 어디 있나"
```

**근거는 Tasks.md의 해당 항목에 그대로 둔다.** 여기로 옮기지 않는다.
이 문서가 푸는 문제는 "근거가 없다"가 아니라 **"어느 채팅에 있는지 몰라
매번 전부를 훑는다"**이다. 그래서 내용이 아니라 위치만 적는다.

---

## 1. 채팅 인덱스

### 채팅을 나누는 규칙

**규칙 자체는 Tasks.md "채팅 명명 규칙" 절에 있다.** Phase 경계가 아니라
대화 길이로 나눈다는 것과 그렇게 바뀐 경위가 거기 있다 — 그것은 결정이므로
Tasks.md에 남는다. **여기 있는 것은 그 규칙에 따라 실제로 나뉜 목록이다.**

---

### `Mellow_prepare` — 기획, 문서 셋 생성 (완료)

- **닫힌 태스크**: — (Phase 0 이전)
- **여기서만 나온 논의**
    - PRD.md · Tasks.md · CLAUDE.md 세 문서 생성
    - **"세션은 방향 순수" 원칙 수립** — 원본은 PRD 3장 · CLAUDE.md
      "확정된 설계 결정"
    - **회전 관련 지침 충돌 정리** — 시스템 회전 감지는 열어두고 UI
      반응형만 만들지 않는다는 층위 구분. **지금 CLAUDE.md에 있는 그
      서술("기기 회전에 따라 화면 구성이 바뀌는 대응은 하지 않는다")의
      출처다.** 3-7/3-7a가 이 층위 구분 위에 서 있다

### `Mellow_01` — Phase 0 ~ Phase 1의 1-C (완료)

- **닫힌 태스크**: 0-4~0-7, 1-1~1-16
- **여기서만 나온 논의**
    - Portrait 잠금 전제 뒤집기 (0-6)
    - 각도 실측표와 `preferredTransform` 실측표 — 원본은 CLAUDE.md
    - 병합 실험의 Mac 선행 검증이 유효하다는 확인 (1-12)

### `Mellow_02` — Phase 1의 1-D 익스포트, Phase 1 마감 (완료)

- **닫힌 태스크**: 1-17~1-21, 게이트 1
- **여기서만 나온 논의**
    - 세션 방향 모델 확정 — Tasks.md "세션 방향 모델 — 확정"
    - `shouldMoveFile` 결론 뒤집힘 (1-18 → 1-19)
    - 재인코딩 비용의 플랫폼 역전 — Mac 값으로 실기기를 추정할 수 없다

### `Mellow_03` — Phase 2 저장 계층 (완료)

- **닫힌 태스크**: 2-1~2-5 (Tasks.md "2-A. 저장 계층" 그룹 전체)
- **경계**: **2-A 완료 지점에서 끊겼다.** 저장 계층을 닫고
  `/codex:review`까지 돌린 뒤가 `03`의 끝이고, `04`가 2-6부터 시작한다
- **여기서만 나온 논의**
    - SwiftData `#Predicate`의 enum 미지원 (A 라운드) — 원본은 CLAUDE.md
    - 단일 클립 정규화 비용 실측 (B 라운드) — 원본은 Tasks.md "B." 절
    - 정규화 그룹 2-D 신설 (2-17~2-21)

### `Mellow_04` — 2-6·2-7 구현, 2-12a 신설 (완료)

- **닫힌 태스크**: 2-6 · 2-7
- **여기서만 나온 논의**
    - **2-12a 신설 (닫기 계약 확정)** — Tasks.md "세션 닫기 계약 — 확정"
    - **`rollback()`이 `insert`를 인메모리에서 되돌리지 않는다는 발견** —
      원본은 CLAUDE.md "API 주의사항". 3절 4번 사례가 여기서 나왔다
    - **`Session.init`을 witness 타입으로 막지 않은 것** — 이 프로젝트의
      첫 "규율로 지킨다" 예외이며 **재검토 트리거가 달려 있다**(두 번째
      생성 호출부가 생길 때). Tasks.md 2-6
    - 채팅 경계 재조정

### `Mellow_05` — 2-8 · 2-9, 문서 위계 정리

- **닫힌 태스크**: 2-8 · 2-9
- **여기서만 나온 논의**
    - `alsoApply`를 `insert` 앞으로 옮긴 이유 — Tasks.md 2-8
    - 실기기 검증 항목 3의 통과 판정 철회 — Tasks.md 2-8
    - `.missing` 발생 경로 정정, 2-16 대상 좁힘 — Tasks.md 2-16
    - PRD > Tasks 위계 확립 — CLAUDE.md "참조 문서"
    - 이진 확인 방법 정정(Debug dylib · `grep -aF`) — CLAUDE.md
    - 히스테리시스 (C) 확정 — Tasks.md 2-9

### `Mellow_06` — 2-10 · 2-11 · 2-12, History.md 신설

- **닫힌 태스크**: 2-10 · 2-11 · 2-12
- **범위 안이었으나 미착수**: 2-16 명세 재검토
- **여기서만 나온 논의**
    - **2-10 훅 위치를 `ClipStore.delete` 안으로 확정** — Tasks.md 2-10.
      **2-5의 조항 하나가 뒤집혔다**(2절 참고)
    - **`.corrupted` 취급 유보** — 초기화하지 않는다. **어느 쪽을 골라도
      검증할 수단이 없어서**이며 옳다고 증명돼서가 아니다. 트리거는 2-8에
      이미 있던 것을 쓴다(2-16의 `.corrupted` 복구). Tasks.md 2-10
    - **2-11이 선행 태스크로 흡수돼 구현 없이 닫혔다** — 삭제·재정렬은
      2-5, 방향 초기화는 2-10, 남은 AC 둘은 UI(3-4·3-5). Tasks.md 2-11
    - **삭제 실패 후 첫 조회가 거짓말한다** — `rollback()` 뒤
      `fetch<Session>` 1회차 0개 / 2회차 1개. 원본은 CLAUDE.md
      "API 주의사항" · Tasks.md 2-12. 3절 2번 패턴과는 별개다
    - **undo × 정규화 큐 명세 구멍** — 2-D 전체에 "삭제"도 "undo"도 한 번
      나오지 않는다. **2-19에 걸었다**(2-21이 아니다 — undo는 실패가 아니라
      대상 소멸이다). Tasks.md 2-19
    - **History.md 신설과 수록 기준 수립** — 이 문서 자체

---

## 2. 뒤집힌 결정

### 무엇을 싣는가

**남긴다** — 그 결정으로 실제로 무언가를 했고 나중에 잘못이었다고 판명된
것. 되돌리는 비용이 발생한 것.

**뺀다** — 근거가 없어 유보했다가 근거가 생겨 정한 것. 결론은 그대로고
확실성만 오른 것. **결론이 유지되고 근거만 정확해진 것도 뺀다.**

**왜 이 구분이 필요한가**: **이 표는 3절(반복 실수 패턴)에 올라갈 후보를
고르는 자리다.** 보류→확정은 아무리 쌓여도 실수 패턴이 되지 않는다.
섞이면 신호가 묻힌다.

### 목록

근거는 전부 아래 포인터에 있다. **여기 옮겨 적지 않는다.**

|무엇 → 무엇|채팅|원본|
|---|---|---|
|세션 방향 모델 2값 → 3값 → 2값|`02`|Tasks.md "세션 방향 모델 — 확정"의 논의 경위|
|방향 혼재 선택지 (b) 탈락 → 철회 → 재탈락|`02`|Tasks.md 1-17 · 1-19, "세션 방향 모델 — 확정"|
|`shouldMoveFile` 이동이 느리다 → 차이 없음(1회 측정은 노이즈였다)|`02`|Tasks.md 1-18(취소선) → 1-19|
|Portrait 잠금이 가로 감지를 막는다 → 틀린 전제|`01`|Tasks.md 0-6|
|실기기 검증 항목 3 통과 → **판정 불가로 철회**|`05`|Tasks.md 2-8 실기기 게이트|
|2-16의 `.missing` 발생 경로 셋 → 전부 틀렸다|`05`|Tasks.md 2-16|
|2-5 "방향 초기화는 여기서 하지 않는다" → `delete` 안으로|`06`|Tasks.md 2-5(⚠ 조항) → 2-10|

**마지막 행이 수록 기준에 맞는 이유**: 그 조항 위에서 2-10 설계가
진행됐다. 2-11이 "2-10이 호출부에서 본다"를 전제로 본문을 썼고, 뒤집으면서
2-5 · 2-10 · 2-11 셋을 함께 고쳐야 했다 — **되돌리는 비용이 실제로
발생했다.** 목적이 아니라 수단이 뒤집힌 것이지만, 그 수단을 근거로 다른
항목이 쓰였다는 점이 판단 기준이다.

### 검토했으나 뺀 것

**`.corrupted` 초기화 "해도 잃을 게 없다" → 유보 (`06`)** — **뺀다.**

채팅 안에서 교정됐고 **그 위에 쌓인 것이 없다.** 2-10 본문이 처음부터
유보로 쓰였고 코드도 유보 상태로 한 번에 들어갔다 — 고쳐야 했던 항목이
하나도 없다. `2-12a 근거 좁힘`을 뺀 것과 같은 잣대다.

**빼는 판단을 기록으로 남기는 이유**: 다음 사람이 같은 후보를 다시
들고 와 "왜 없지" 하는 것을 막는다. 기준에 걸러진 것과 빠뜨린 것은 다르다.

---

## 3. 반복 실수 패턴

**같은 종류로 두 번 이상 나온 것만 적는다.** 둘이 확인됐다.

### 1. 측정한 대상과 적용하려는 대상이 다르다

|#|사례|어디|
|---|---|---|
|1|Mac 재인코딩 비용을 실기기로 환산|1-17|
|2|단순 재인코딩 96ms를 방향 교정에 적용 — **24배 차이**|1-19 → 1-21|
|3|4클립 배치를 나눈 값을 단독 비용으로 인용|1-21 B라운드|
|4|`rollback()` 실측이 **update** 사례였는데 **insert**에 적용|`Mellow_04`|

**4번은 앞의 셋과 축이 다르다.**

- **1~3은 측정 *환경·조건*이 달랐다** — 어디서(Mac/실기기), 어떤 경로로
  (단순/`AVVideoComposition`), 몇 개를 묶어(단독/배치) 쟀는가
- **4는 측정 *대상의 종류*가 달랐다** — 같은 API(`rollback()`)를 같은
  기기에서 쟀지만 **잰 연산이 `update`였고 적용하려던 것은 `insert`였다**

그래서 "어디서 잰 값인지 확인한다"만으로는 4번이 걸러지지 않는다.
**"무엇에 대해 잰 값인지"를 함께 물어야 한다.** CLAUDE.md 측정값 인용
규칙에 그 한 줄이 들어간 것이 이 사례 때문이다.

### 2. 검증이 도는 것처럼 보이는데 실제 경로를 타지 않거나 대상이 없다

|#|사례|어디|
|---|---|---|
|1|하네스 9군이 `ClipStore.save`를 안 타 `alsoApply`가 호출되지 않았다|2-8 (`05`)|
|2|B군 픽스처가 `alsoApply` 없이 만들어져 세션이 `.missing`이었고, C군이 2-10 회귀를 물지 못했다|2-10 (`06`)|
|3|`git checkout`으로 커밋 안 된 H군이 날아갔는데 **"전부 통과"가 나왔다**|2-12 (`06`)|

**셋 다 "통과"로 보인다는 것이 공통점이다.** 실패가 아니라 성공으로
위장되므로, 결과만 보는 한 영원히 안 걸린다.

- **1·2는 픽스처가 실사용 저장 경로를 우회했다.** 검증은 돌았지만 검증
  대상 코드가 실행되지 않았다. 2는 한 겹 더 얄궂다 — C군은 `ClipStore.delete`를
  제대로 탔는데 **세션이 방향을 가진 적이 없어** 2-10이 있든 없든 결과가
  같았다. 경로는 맞고 **대상이 없었다**
- **3은 검증 자체가 사라졌다.** 남은 검사들이 통과해서 총평이 "전부 통과"로
  나왔고, 사라진 군이 없다는 것은 출력 어디에도 표시되지 않는다

**대응책**

|무엇을|어떻게|
|---|---|
|1·2 — 경로와 대상|**픽스처를 실사용과 같은 경로로 만든다.** 옵셔널 파라미터를 안 넘기면 조용히 통과하므로, 저장 계층을 거치는지와 **그 결과가 실제로 붙었는지**를 픽스처 단계에서 `check`로 못 박는다 (B군의 "★ 세션 방향이 .decided — C군의 전제")|
|1·2·3 공통|**킬 테스트.** 검증하려는 코드를 끄고 돌려 **실제로 실패하는지** 본다. 3을 잡은 것이 이것이다 — 끄기 전에 이미 "통과"였으므로 킬 테스트가 없었으면 소실을 못 봤다|
|3 — 소실|**커밋 안 된 검증에 `git checkout`을 쓰지 않는다.** 원복은 백업 파일로 한다|

**1번 패턴과의 관계**: 1은 *값*을 잘못 인용하는 것이고 2는 *검증*이
비어 있는 것이다. 축이 다르지만 증상이 같다 — **둘 다 근거가 있다고
믿게 만든다.**

### 승격 대기

**"1회 측정을 결론으로 삼는다"** — `shouldMoveFile`(1-18 → 1-19) 하나뿐이다.
**두 번째 사례가 나오기 전까지 승격하지 않는다.** 이 절의 규칙이 "두 번
이상"이고, 하나로 패턴을 만들면 다음 사례가 그 틀에 맞춰 해석된다.
