PRD의 기능 요구사항을 실행 순서로 풀어놓은 문서다. 위에서부터 순서대로 진행한다. 각 Phase 끝의 **게이트**를 통과하지 못하면 다음 Phase로 넘어가지 않는다.

```
Phase 0. 셋업          프로젝트 생성, 실기기 배포 경로 확보
Phase 1. 기술 스파이크   촬영 → 병합 → 저장 파이프라인 검증  ← 최대 리스크
Phase 2. 구조화        SwiftData, 세션 개념, 파일 관리
Phase 3. UI           화면 4개 구현
Phase 4. 실사용 검증    여행 한 번
```

**Phase 1이 끝날 때까지 UI를 다듬지 않는다.** 화면 하나에 버튼 몇 개 얹은 상태로 파이프라인만 확인한다. 여기서 설계가 뒤집히면 공들인 UI를 전부 버리게 된다.

---

## 작업 방식

### 역할 분담

|도구|담당|
|---|---|
|**Claude 웹 채팅**|기획, 설계 논의, 문서 작성, Claude Code에 줄 프롬프트 작성|
|**Claude Code**|실제 코드 작성, 파일 생성·수정, 디버깅, 커밋|

설계가 흔들리는 논의는 웹 채팅에서 끝내고, 확정된 것만 Claude Code로 넘긴다. 코드를 쓰다가 설계 의문이 생기면 웹 채팅으로 돌아온다.

### 채팅 명명 규칙

**채팅별 범위 목록은 `History.md` 1절에 있다.** 여기 두지 않는 이유는
목록이 두 곳에 있으면 다음에 한쪽만 갱신되기 때문이다 — 그 중복을 없애려고
`History.md`를 만들었다. **규칙은 결정이라 여기 남고, 목록은 인덱스라
그쪽으로 간다.**

**Phase 경계와 채팅 경계가 항상 일치하지는 않는다.** 처음에는 Phase 단위로 나누려 했으나, Phase 1의 익스포트 구간이 예상보다 길어져 `Mellow_01`이 먼저 한계에 닿았다. 그래서 `Mellow_02`가 Phase 경계가 아니라 Phase 1 중간(1-D)에서 시작했다.

**대화가 길어지면 Phase 중간에도 나눈다.** Phase의 크기는 미리 알 수 없고, 맥락이 흐려지는 시점이 실제 분할점이기 때문이다. 채팅을 넘길 때는 진행 중인 작업 항목 번호와 열려 있는 미결을 인수인계에 명시한다.

### 문서 배치

```
mellow/
├── CLAUDE.md          ← 프로젝트 지침 (Claude Code가 매 세션 자동으로 읽음)
├── PRD.md
├── Tasks.md
├── History.md         ← 채팅 인덱스. 여기에만 있는 결정은 없다
└── Mellow/            ← Xcode 프로젝트
```

웹 채팅의 "프로젝트 지침"과 같은 내용을 `CLAUDE.md`로 저장한다. 지침이 바뀌면 양쪽을 함께 고친다.

---

## Phase 0 — 셋업

앞의 세 개는 직접 하고, `0-4`부터 Claude Code에 넘긴다.

### 0-A. 저장소와 문서 (직접)

- [ ] **0-1** GitHub 레포 생성
    - 이름 `mellow`, private, `.gitignore`는 Swift 템플릿
    - 로컬에 클론
- [ ] **0-2** 문서 배치
    - 프로젝트 지침을 `CLAUDE.md`로 저장
    - `PRD.md`, `Tasks.md`를 루트에 복사
    - 첫 커밋
- [ ] **0-3** Claude Code 연결
    - 레포 폴더에서 실행, `CLAUDE.md`를 읽는지 확인
    - `Mellow_01` 채팅에서 첫 지시 프롬프트를 받아 전달

### 0-B. 프로젝트 생성 (Claude Code)

- [x] **0-4** Xcode 프로젝트 생성 (SwiftUI, iOS 17 타깃)
    - **XcodeGen으로 관리한다.** `project.yml`이 단일 소스이고 `.xcodeproj`는 생성물이라 git에 커밋하지 않는다. clone 직후 `xcodegen generate`가 필요하다
    - 새 Swift 파일을 추가한 뒤에는 `xcodegen generate`를 다시 실행하면 된다. `sources: Mellow` 디렉터리 전체가 대상이므로 빌드 포함 여부를 파일마다 확인할 필요가 없다
    - `.pbxproj`를 직접 편집하지 않는다. 구조·빌드 설정 변경은 `project.yml`만 고친다
    - Swift 언어 모드는 5로 고정했다 (`SWIFT_VERSION: "5.0"`). AVFoundation 동시성 어노테이션 문제 때문이며 Phase 2에서 재검토한다
    - **2-2에서 재검토 완료 — 5를 유지하고 2-18 착수 직전에 다시 본다.** 근거는 아래 "Swift 언어 모드 재검토 (2-2)" 참고
- [x] **0-5** `Info.plist` 권한 문구 작성
    - `project.yml`의 `info.properties`에서 생성한다. Info.plist 파일을 직접 만들지 않는다
    - `NSCameraUsageDescription`
    - `NSMicrophoneUsageDescription`
    - `NSPhotoLibraryAddUsageDescription`
    - `NSPhotoLibraryUsageDescription`(읽기)은 넣지 않는다. v0.1은 사진 앱에서 읽어오는 기능이 없다
- [x] **0-6** 앱을 **Portrait 전용으로 잠근다** (`UISupportedInterfaceOrientations`)
    - iPhone·iPad 키 모두 `UIInterfaceOrientationPortrait` 하나만. iPad는 v0.1 지원 대상이 아니다
    - 종전 서술("잠그면 가로 촬영 감지가 막힌다")은 틀린 전제라 폐기한다. iOS 17의 `AVCaptureDevice.RotationCoordinator`는 인터페이스 방향이 아니라 가속도계 기준 물리적 기기 방향을 추적한다. 따라서 앱을 Portrait로 잠가도 가로 촬영 감지와 가로 클립 저장이 가능하다
    - Portrait 잠금은 프리뷰 회전각을 상수로 만들어 Phase 1 파이프라인 검증에서 회전 변수를 제거하기 위한 선택이다. 이 전제는 Phase 1 초반 실기기 테스트로 검증하며, 실패 시 이 설정만 되돌리면 된다
    - 레이아웃은 세션 방향에 맞는 고정 구성 하나만 만들고, 기기를 돌릴 때 재배치되는 반응형 대응은 만들지 않는다

### 0-C. 실기기 (직접)

- [x] **0-7** 실기기 빌드·설치 확인
    - 시뮬레이터에는 카메라가 없어 Phase 1부터 실기기가 필수다
    - `xcodebuild build`는 설치까지 하지 않는다. 설치·실행은 `devicectl`로 따로 한다:
      `xcrun devicectl list devices` → `... device install app --device <UDID> <Mellow.app>` → `... device process launch --device <UDID> com.lunalism.mellow`

### 게이트 0 — 통과 조건

- [x] 실기기에서 빈 앱이 실행된다
- [x] `xcodegen generate` 후 새 Swift 파일이 빌드에 포함된다
- [x] 커밋이 GitHub에 올라간다 (`github.com/lunalism/Mellow`, origin/main)

---

## Phase 1 — 기술 스파이크

목표: **10초 클립 여러 개가 재인코딩 없이 안 튀고 붙는지** 확인. 못생겨도 된다. 이 Phase의 코드는 버려도 아깝지 않게 짠다.

> **완료 (게이트 1 통과).** 재인코딩 없이 붙는다는 것이 실기기에서 확인됐다 — 출력의 인코딩된 비디오 샘플이 입력과 바이트 단위로 동일하다(1-17). 클립 30개(300초) 저장이 2.45초, 이음새 29곳에서 검은 프레임·멈칫·무음 없음(1-19, 1-20).
>
> 남은 **1-21은 게이트 항목이 아니다.** 세션 방향 모델 결정용이며 Phase 2와 병렬로 진행할 수 있다.

### 1-A. 촬영

- [x] **1-1** 카메라·마이크 권한 요청 및 상태 분기
    - 두 권한이 모두 있어야 프리뷰를 시작한다. 설정 앱 이동 버튼은 2-13에서 붙인다
- [x] **1-2** `AVCaptureSession` 구성 + 프리뷰 표시
    - 세션 구성은 백그라운드 큐에서, UI 갱신은 메인에서
    - 세션 시작은 씬이 활성일 때만. 권한 팝업·구성 중에 앱을 내리면 백그라운드에서
      `startRunning`이 불려 복귀해도 프리뷰가 살아나지 않는다
    - `.inactive`에서는 정지하지 않는다. 권한 팝업·제어 센터·알림 배너에서도
      발생해 프리뷰가 깜빡인다. `.background`에서만 정지한다
    - **씬 활성 가드(P2)**: 구성 완료 시점(t=115.5ms)에 씬이 비활성이라 `start()`가
      가드에서 반환되고, `.active` 수신 후 정상 시작. 권한 팝업 경로는 실기기
      재현이 불가능하나(팝업이 시스템 모달이라 홈 제스처·화면 잠금이 먹지 않는다)
      로그로 가드 동작이 확인됨
- [x] **1-3** 촬영 스펙 고정 (1080p / 30fps / H.264 / AAC)
    - `sessionPreset = .hd1920x1080`으로 고정. `activeFormat` 직접 지정은 하지 않았다
    - **preset은 해상도만 잡는다.** fps는 `activeVideoMin/MaxFrameDuration`으로 따로 잠근다. 이 기기의 activeFormat 지원 범위가 **1.0~30.0**이라, 잠그지 않으면 저조도에서 클립마다 fps가 갈린다
    - **기본 코덱은 H.264가 아니라 HEVC였다.** `availableVideoCodecTypes = hvc1, avc1, jpeg`이고 지정 전 `AVVideoCodecKey = hvc1`. `setOutputSettings`로 명시해야 avc1이 된다. 부수 효과로 비트레이트가 7.65Mbps → 15.3Mbps로 증가
    - `automaticallyAdjustsVideoHDREnabled = false`. 단 `isVideoHDREnabled`는 true로 남아 있다(끌지 여부 미결정)
    - 오디오는 지정하지 않았다. 실측 결과 AAC / 48000 / **1채널(모노)** 로 일관되게 나온다
- [x] **1-4** `AVCaptureMovieFileOutput`으로 파일 녹화
    - 임시 디렉터리에 `mellow-<UUID>.mov`
    - 회전각은 녹화 시작 직전에 `videoRotationAngleForHorizonLevelCapture`를 한 번 읽어 고정한다. 녹화 중에는 바꾸지 않는다(PRD 8장)
    - `.background` 진입 시 세션 정지 전에 녹화를 먼저 닫는다. 녹화 중 세션을 멈추면 파일이 온전하지 않을 수 있다
    - 종료 오류 시 `AVErrorRecordingSuccessfullyFinishedKey`를 확인해 살릴 수 있는 파일은 살린다
- [x] **1-5** 10초 자동 정지 (`maxRecordedDuration` 활용 검토)
    - `maxRecordedDuration` 사용. 타이머를 쓰지 않는다
    - 이 한도로 끊기면 델리게이트에 `AVError.maximumDurationReached`가 **에러로** 오지만 파일은 정상이다. 정상 종료로 구분해야 한다
    - 실측 duration **9.940 ~ 9.960초** (5개). 최대 편차 −0.060초로 AC(±0.2초) 통과. 전부 10초 이하이며 초과 없음
- [x] **1-6** 녹화 버튼: **탭 방식** (확정)
    - 탭 → 시작, 10초 도달 → 자동 정지, 녹화 중 다시 탭 → 조기 종료
    - 조기 종료를 남기는 이유: 없으면 모든 클립이 10초로 강제된다. 짧은 컷을 의도적으로 찍을 길이 있어야 한다
    - 조기 종료로 1초 미만이 나오면 1-7에 따라 폐기된다
    - 탭 방식에도 "시작 콜백 전에 조기 종료 탭" 경쟁이 있다. 시작/정지 판단은 `isRecording`이 아니라 `RecordingIntent`로 한다
- [x] **1-7** 1초 미만 클립 폐기
    - **파일 duration 기준**으로 판정한다. 버튼을 누른 시간이 아니다
    - 근거: 녹화 시작 지연이 **0.077 ~ 0.174초**로 일정하지 않다(2.3배 변동). 누른 시간에서 상수를 빼는 방식으로는 1초 판정이 불가능하다
    - 폐기 사실이 로그와 화면 양쪽에 드러난다. 조용히 사라지면 사용자가 혼란스럽다
- [x] **1-8** 저장된 파일의 실제 스펙 검증
    - `AVAsset`에서 해상도·fps·코덱·오디오 포맷을 읽어 로그로 출력
    - **여러 클립이 정말 동일한 값인지 눈으로 확인한다**
    - 세로 9개 클립(1.3~10초, 밝은 곳·저조도 혼재)이 **✓ 9개 전부 호환**으로 판정됐다
    - 저조도에서 `nominalFrameRate` 30.0 → 29.995 (감소율 0.017%). fps 잠금은 유효하나 미세 지터 발생. 더 어두운 환경(실내 야간 등)에서 추가 저하 여부는 미확인. Phase 4 실사용에서 확인한다
    - 트랙 길이 어긋남은 9개 중 4개에서 `1/600`(1.67ms)
    - **`minFrameDuration` 판정 제외가 1-17에서 실증됐다.** 지터가 20/600과 19/600으로 섞인 세트 두 개(혼재 9/1, 혼재 6/4) 모두에서 passthrough가 성립했고, 출력의 인코딩된 비디오 샘플이 입력과 바이트 단위로 동일했다(`AVAssetReader` + FNV-1a 해시). `ClipSpec` 판정 기준을 되돌릴 필요 없다

**미확인으로 남은 것**

- 1초를 사이에 두고 판정이 뒤집히는 케이스(1초 넘게 눌렀는데 파일은 1초 미만) 미재현. 이론상 1.0~1.17초 구간에서 발생 가능
- ~~폐기 파일의 실제 디스크 삭제는 로그로 미확인~~ **실측으로 해소됐다** (2-A tmp 점검).
  실기기 `tmp`에 쌓인 녹화본 129개를 전부 판독한 결과 **duration 1초 미만이 0건**이고
  최단이 1.332초다. 폐기분이 디스크에 남지 않는다는 뜻이며, `deleteFile(at:)`이 실제로
  지우고 있음이 확인됐다. 추정이 아니다
- **트랙 길이 어긋남이 조기 종료와 관련 있는지 미확인.** 2-9 실기기 검증 중
  4초 조기 종료 클립에서 `⚠ 트랙 길이 어긋남  video - audio = 1/600 (비디오가 김)`이
  나왔다. **새로운 현상은 아니다** — 위 1-8이 이미 "9개 중 4개에서 `1/600`(1.67ms)"로
  기록하고 있고 값도 같다. **모르는 것은 발생률이 조기 종료 경로에서 높아지는가다.**
  1-8 표본은 1.3~10초가 섞여 있어 길이별 분포를 남기지 않았다
    - **왜 미결로 남기나**: 어긋남이 클립마다 같은 방향이면 클립 간 diff에는 한 줄도
      안 잡히지만 **이어붙이면 클립 수만큼 누적되어 뒤쪽 오디오가 밀린다**
      (`ClipSpec.reportTrackDurationSkew` 주석). 1.67ms × 30클립 = 50ms이고,
      그것이 들리는 수준인지는 확인된 바 없다
    - 1-12·1-17 병합 검증은 이음새의 **영상**을 봤다. 오디오 밀림을 따로 재지 않았다
    - **담당은 3-12(미리보기) 또는 1-7 계열이다. 2-9와 무관하다** — 여기 적는 것은
      2-9 검증 중에 눈에 띄었기 때문이고, 조사는 그쪽에서 한다

### 1-B. 방향

- [x] **1-9** 기기 방향 감지
    - API 확정: `AVCaptureDevice.RotationCoordinator`(iOS 17+). `init(device:previewLayer:)`로 만들고 `videoRotationAngleForHorizonLevelPreview` / `...Capture`를 KVO로 관찰한다. 적용은 `AVCaptureConnection.videoRotationAngle`(설정 전 `isVideoRotationAngleSupported` 확인)
    - **실측 각도** (iPhone 12, 후면 광각, Portrait 잠금. 1-2에서 측정)

      |기기 방향|capture|preview|
      |---|---|---|
      |portrait|90|90|
      |landscapeLeft|0|90|
      |landscapeRight|180|90|
      |portraitUpsideDown|270|90|

    - Portrait 잠금 상태에서도 capture 각도가 정상적으로 변한다. preview는 90 고정
    - 네 방향 모두 실측 완료
    - 각도를 방향 상수로 매핑하는 테이블은 만들지 않는다. 기기·카메라마다 값이 다를 수 있어 위 표는 iPhone 12 참고치다
- [x] **1-10** 가로로 찍은 클립의 저장 스펙 확인 (1920×1080이 맞는지)
    - 네 방향 전부 `naturalSize` 1920×1080으로 동일하다. 세로도 마찬가지이며, 세로/가로 구분은 해상도가 아니라 `preferredTransform`에만 나타난다
    - 표시 규격은 transform 적용 후에 갈린다 — 세로 계열 1080×1920, 가로 계열 1920×1080
- [x] **1-11** 회전 정보(`preferredTransform`)가 어떻게 기록되는지 확인
    - 4방향 실측 및 재현성 확인 완료 (CLAUDE.md "실측 preferredTransform")
    - 같은 방향이면 클립 길이와 무관하게 값이 완전히 동일하다(7초~188초 확인). 앱 재실행 후에도 재현된다
    - 방향이 다르면 값이 다르다. 90↔270과 0↔180은 각각 180도 회전 관계

### 1-C. 병합과 재생 ← **최대 고비**

- [x] **1-12** 클립 2개를 `AVMutableComposition`으로 이어붙이기
    - 비디오 트랙과 오디오 트랙을 각각 `insertTimeRange`
    - `Mellow/Merge/ClipMerger.swift`. `ClipSpec`과 같은 제약(UIKit 비의존)이라 Mac에서 `swiftc`로 단독 실행하며 검증했다
    - **커서 전진은 비디오 트랙 길이 기준으로 확정.** 오디오 기준으로 하면 `insertTimeRange`가 삽입-밀어내기로 동작해 앞 클립 끝이 잘려 뒤로 밀린다 — 실측에서 클립 10개가 비디오 세그먼트 15개로 파편화되고 총 차이 8.33ms가 남았다
    - **이 단계에서는 인코딩이 일어나지 않는다.** 컴포지션은 편집 목록일 뿐이라 병합 성공이 passthrough 가능을 뜻하지 않는다. 재인코딩 여부는 1-17에서 확정한다
- [x] **1-13** `AVPlayer`에 물려 즉시 재생
    - `AVPlayerItem(asset: composition)`을 그대로 물린다. 익스포트하지 않으므로 파일을 쓰지 않고, "합치는 중" 로딩이 필요 없다
    - **첫 프레임 판정은 `AVPlayerLayer.isReadyForDisplay`로 한다.** `status == .readyToPlay`와는 순서가 보장되지 않는다 — 실측에서 진입마다 앞뒤가 바뀌었다(차이 1.5~2.3ms). 로딩 표시를 붙일 일이 있으면(3-12) `isReadyForDisplay`를 기준으로 삼는다
- [x] **1-14** 이음새 검증 — 검은 프레임, 끊김, 오디오 어긋남
    - 멈칫 0건 (`likelyToKeepUp`/`bufferEmpty`/`bufferFull`/`PlaybackStalled` 4종 계측). 검은 프레임은 육안 관찰상 없음 — 로그로는 감지 불가한 항목이다
    - 이음새 검증 대상은 "오디오가 짧은 클립 경계의 1.67ms 무음이 들리는가" 하나. 클립 수를 늘려도 각 경계 값은 1.67ms로 일정하며 누적되지 않음(실측)
    - 세로 10개 기준으로 5곳의 경계에 무음이 들어간다. 총 트랙 길이 차이는 0
    - **1.67ms 무음 청취 결과 — 확정: 들리지 않는다.** 여기서는 배경음이 있는 환경이라 미확정으로 남겼으나, 1-20에서 조용한 환경 + 에어팟 맥스 + 큰 볼륨으로 클립 30개(경계 29곳)를 확인해 들리지 않음을 확정했다. 이론상 지각 하한(약 10ms) 미만이고 누적되지 않는다는 실측과 일치한다. **커서 전진 정책(비디오 트랙 기준, 1-12)을 재검토할 이유가 없다**
- [x] **1-15** 클립 10개로 늘려서 동일 검증
    - **첫 진입 계측(iPhone 12)**
        - 클립 4개 241.8ms / 10개 131.6ms — 클립을 2.5배 늘렸는데 오히려 110ms 빨라졌다
        - 원인: 4개 1회차의 204.8ms는 클립 수와 무관한 일회성 비용(앱 실행 후 첫 AVPlayer 초기화, 오디오 세션 활성화, 디코더 준비). 10개 1회차에서 이 구간은 53.1ms로 2·3회차와 큰 차이 없음
        - 클립 수에 비례하는 것은 병합뿐이며 클립당 약 7.5ms
        - 예측: 20개 ~195ms, 30개 ~270ms. 앱 첫 실행 직후 일회성 비용 200ms를 더해도 470ms
        - **30개 실측 확인됨 (1-19).** 병합 191~233ms, 클립당 약 7.3ms로 위 외삽값(클립당 ~7.5ms)과 일치한다
    - 멈칫 0건 (`likelyToKeepUp` / `bufferEmpty` / `bufferFull` / `PlaybackStalled` 4종 관찰, 100초 재생 중 t=0 이외 변화 없음)
    - 경계 정확도 0.3~0.8ms (프레임 간격의 2.4% 이하). 클립이 늘어도 나빠지지 않는다
- [x] **1-16** 가로 세션 클립으로도 1-12~1-15 반복
    - **가로 세션 클립끼리 붙일 때 `preferredTransform`이 일치해야 passthrough가 가능하다.** 이 확인이 여기서 이뤄진다 (1-11에서 옮겨옴 — 1-11은 "회전 정보가 어떻게 기록되는지"까지가 범위)
    - 가로 10개(전부 `capture 0`) 실측: 첫 프레임 121.4~126.7ms, 멈칫 0건, 경계 어긋남 0.3~0.6ms, 표시 규격 1920×1080, `✓ 10개 전부 호환`. 세로 라운드와 모든 항목이 동등하다
    - 방향은 첫 프레임 시각에 영향을 주지 않는다. `preferredTransform`은 재생 시점의 표시 변환일 뿐 `AVPlayerItem` 준비 과정에 관여하지 않는다. 세로와의 차이 3~10ms는 라운드 내 편차와 같은 자릿수다
    - 어긋남이 있는 클립 비율은 라운드마다 다르다(세로 5/10, 가로 7/10). 각 어긋남 값은 `1/600`으로 일정하고, 무음 구간이 늘어도 총 트랙 길이 차이는 0이다
    - 가로L(identity)과 가로R(180도)을 섞은 세션 관찰 결과는 결정 대기 목록 "세션 방향 모델" 참고

### 1-D. 내보내기

- [x] **1-17** `AVAssetExportSession` + passthrough 프리셋으로 익스포트

    **판정: passthrough 성립.** 미결 세 가지가 여기서 닫혔다. 측정은 Mac(M2, 내부 SSD)이며, 절대값이 아니라 비율을 실기기로 옮긴다.

    **API**

    - passthrough 판정은 `exportPresets(compatibleWith:)`가 아니라 `determineCompatibility(ofExportPreset:with:outputFileType:)`로 한다. SDK 헤더에 `Passthrough`는 그 배열에 포함되지 않는다고 명시돼 있다. 빠져 있다고 불가능한 것이 아니다
    - `export(to:as:) async throws` — 구 `exportAsynchronously`는 iOS 18에서 deprecated. `states(updateInterval:)`은 iOS 18+라 배포 타깃 17에서 못 쓴다

    **passthrough**

    - 소요 시간(Mac M2, 내부 SSD): 세로 10개 99.583초 → **292.6ms**. 실시간의 **0.0029배**. 파일 크기 **98.49%**
    - 익스포트는 클립 수에 선형이며 바이트당 비용이다(약 660MB/s)
    - `shouldOptimizeForNetworkUse = true`는 파일 크기를 1바이트도 줄이지 않고 시간만 3배로 늘린다. **켜지 않는다**

    **측정 환경이 결과를 지배한다**

    - 저장 위치가 지배적이다. USB 외장(~31MB/s) 6564ms vs 내부 SSD 293ms, **22배 차이**. 성능 측정은 반드시 내부 SSD에서 한다
    - 같은 출력 디렉터리에 큰 파일을 쌓으면서 반복하면 30클립 익스포트가 817ms → 2194ms로 밀린다. 라운드마다 새 디렉터리를 쓴다

    **재인코딩**

    - 비용(Mac M2, `AVAssetExportPreset1920x1080`): 실시간의 **0.150배로 일정**하다. granularity(단일 클립 / 세션 전체)와 videoComposition 유무에 관계없이 유지된다. passthrough 대비 **43~53배**
    - **재인코딩 시 비트레이트가 15.5Mbps → 10.5Mbps(68.2%)로 떨어진다.** 네 케이스 전부 동일한 비율. `AVAssetExportSession`은 preset이 비트레이트를 정하고 세션 API로 제어할 수 없다. 원본 비트레이트를 유지하려면 `AVAssetWriter`가 필요하다

    **방향 교정 · 확장성**

    - 방향 교정(계열 내 180도)은 두 계열 모두 성공. 여백 0px(픽셀 검사), 전 구간 정방향. `renderSize`는 가로 1920×1080 / 세로 1080×1920
    - 컴포지션 구성은 클립 수에 선형이며 클립당 1.3ms (M2, 구성만). 1-15의 클립당 ~7.5ms는 iPhone 12에서 병합 + `AVPlayerItem` 준비까지 포함한 첫 프레임 시각 증가분이므로 직접 비교 대상이 아니다
- [x] **1-18** 사진 앱 저장 (`PHPhotoLibrary`)
    - `PHAccessLevel.addOnly`로 요청. 팝업이 "사진 추가만 허용"으로 뜨는 것을 실기기에서 확인했다
    - **권한 확인을 병합·익스포트보다 앞에 둔다.** 뒤에 두면 (ⓐ) 계측에 사람이 버튼을 누르기까지의 대기 시간이 통째로 들어가 숫자가 오염되고(실측에서 22.7초), (ⓑ) 거부 시 이미 만들어진 익스포트 파일이 고아가 된다. 앞으로 옮긴 뒤 두 문제 모두 해소됨을 확인했다
    - 실측 (iPhone 12, 클립 3개 / 29.883s / 58.00MB)

      |구간|이동|복사|
      |---|---|---|
      |병합|49ms|41ms|
      |익스포트|217ms|168ms|
      |사진 앱 저장|215ms|120ms|
      |합계|483ms|329ms|

    - ~~**`shouldMoveFile = true`가 오히려 느리다.**~~ **→ 1-19에서 뒤집힘.** 여기 1회 측정의 1.8배 차이는 노이즈였고, 3회씩 반복하니 성능 차이가 없었다. 결론은 1-19 참고
    - 거부 경로: 크래시 없음, 안내 표시됨, 임시 파일 생성되지 않음
    - 실기기 환산 계수 (Mac 대비): 병합 클립당 16ms vs 1.3ms로 약 12배. 익스포트 277MB/s vs 660MB/s로 42%
- [x] **1-19** 저장 소요 시간 측정 (클립 20개 기준 3초 이내)

    **클립 30개 (300초 / 570MB, iPhone 12)** — AC 기준(20개)보다 1.5배 무거운 조건에서 측정했다.

    |회차|모드|병합|익스포트|저장|합계|
    |---|---|---|---|---|---|
    |1|복사|191|1489|199|1880ms|
    |2|복사|221|1506|135|1861ms|
    |3|복사|220|2726|146|3093ms|
    |4|이동|222|2176|163|2561ms|
    |5|이동|233|1933|172|2339ms|
    |6|이동|233|2730|155|3119ms|

    - 합계 중앙값 2.45초. **PRD F-06 "클립 20개 3초 이내" 통과**
    - **`shouldMoveFile`은 성능 차이가 없다** (저장 구간 중앙값 복사 146ms / 이동 163ms). 1-18의 1회 측정에서 나온 1.8배 차이는 노이즈였다. **복사(false)로 확정한다** — 성능이 같다면 이동은 사진 앱이 파일을 가져간 뒤 실패 시 재시도할 여지가 없어 불리하다
    - 익스포트 구간이 회차마다 1489~2730ms로 흔들린다(1.8배). 라운드마다 출력 디렉터리를 새로 잡았는데도 발생했다. 측정 중 사진 앱이 저장된 에셋을 백그라운드 처리(썸네일 생성·인덱싱)하고 있었던 것이 유력한 원인이다. 실사용에서도 발생 가능한 조건이나, 최악값 3119ms도 30개 기준이므로 20개 AC에는 여유가 있다
    - 병합은 191~233ms로 안정적. 클립당 약 7.3ms로 **1-15의 외삽값(클립당 ~7.5ms)이 30개 실측으로 확인됐다.** 미결 3 해소
    - **단일 10초 클립 재인코딩 96ms** (영상 대비 0.010배). 세션 방향 모델 논의는 결정 대기 목록 참고
- [x] **1-20** 사진 앱에서 재생해 화질·이음새 확인
    - 실기기 확인 (iPhone 12, 클립 30개 300초, 에어팟 맥스): 이음새 29곳에서 검은 프레임·멈칫·전환 튐 없음. 방향 정방향, 화질 원본과 동일. 파일 앱에서 정상 재생되어 사진 앱 밖에서도 열린다는 것이 확인됐다(PRD 11장 항목)
    - 메시지 앱 전송은 테스트 기기의 환경 문제로 확인하지 못했다 — 10초 클립도 첨부가 되지 않는 상태이며 저장된 파일과 무관하다
- [x] **1-21** 방향 교정 재인코딩 실기기 측정 — 세션 방향 모델 결정용
    - 방향 교정 익스포트 9125ms (4클립 / 39.8초). 클립당 2281ms, 실시간의 0.229배. **이 클립당 값은 배치 익스포트를 4로 나눈 것이다.** 단독 정규화는 1.77초로 더 빠르다(B 라운드)
    - **`AVVideoComposition`이 붙으면 단순 재인코딩보다 24배 느리다** (96ms → 2281ms). 배치 기준이며 단독 기준으로는 18배다(96ms → 1770ms). 픽셀을 다시 그려야 해서 하드웨어 인코더 경로를 타지 못하는 것으로 보인다. Mac M2에서는 두 경로의 비율이 같았으나(둘 다 0.150배) 이는 M2에서 양쪽 모두 느린 경로로 떨어졌기 때문이며, 실기기에서는 갈린다
    - **비트레이트 97.0%.** Mac에서 관찰된 68.2%는 M2의 preset 동작이었지 재인코딩의 본질적 비용이 아니다. 실기기에서는 화질이 사실상 유지되므로 **`AVAssetWriter`로 내려갈 필요가 없다**
    - 여백 없음. 교정 전 뒤집힌 상태를 육안 확인한 뒤 교정해 대조가 성립한다

### 게이트 1 — 통과 조건

아래를 만족해야 Phase 2로 넘어간다.

- [x] 세로 클립 10개가 이음새 없이 붙어 재생된다 (1-15)
- [x] 가로 클립 10개도 마찬가지다 (1-16)
- [x] 오디오가 클립 경계에서 어긋나지 않는다 (1-20)
- [x] passthrough 익스포트가 3초 이내에 끝난다 (1-19, 30개 2.45초)
- [x] 사진 앱에 저장된 결과물이 정상 재생된다 (1-20)

**→ 게이트 1 통과. Phase 1 완료.**

세션 생성 → 10초 촬영 → 순서대로 미리보기 재생 → 사진 앱에 한 편으로 저장까지의 파이프라인이 실기기에서 끝까지 확인됐다. 되돌아갈 조건(촬영 스펙 고정 방식 재검토)은 발동하지 않았다.

**1-21은 게이트 항목이 아니다.** 세션 방향 모델을 정하기 위한 측정이며, 그 결정은 Phase 2의 2-8·2-9(세션 방향 확정, 불일치 시 녹화 차단)와 2-D(방향 정규화)를 설계하기 전에 필요하다. Phase 2 착수와 병렬로 진행할 수 있다.

**통과 못 했다면** 촬영 스펙 고정 방식(1-3)으로 돌아갈 예정이었다. `sessionPreset`으로 안 되면 `activeFormat` 직접 지정을 시도하고, 그래도 안 되면 세션 방향 고정 전략 자체를 재검토한다.

---

## B. 단일 클립 정규화 비용 실측 — Phase 2 설계용

1-21의 클립당 2281ms는 **4클립 배치 익스포트를 4로 나눈 값**이라 실제 시나리오(클립 1개 = 익스포트 1회)와 조건이 다르다. 익스포트 세션 생성·`AVVideoComposition` 구성·인코더 초기화가 익스포트당 한 번인데 4클립에 희석됐고, 카메라가 돌지 않는 상태였으며, 연속 촬영의 발열도 반영되지 않았다. 세 요인 전부 느려지는 쪽으로만 작용하므로 2281ms는 낙관적 하한이었다. 단독 실행 비용을 세로·가로 두 계열에서 다시 측정했다.

**게이트 항목이 아니다.** 1-21과 마찬가지로 게이트 1 통과 여부와 무관하며, Phase 2의 정규화 태스크(2-D) 규모를 정하기 위한 측정이다.

|조건|세로 1080×1920|가로 1920×1080|
|---|---|---|
|B-1 단독 (카메라 stopped)|1791ms (×0.79)|2905ms (×1.27) ※|
|B-2 단독 (카메라 running)|1769ms (×0.78)|1763ms (×0.77)|
|B-3 연속 4개|1765ms (×0.77)|1764ms (×0.77)|
|B-3′ 냉각 후 연속 4개|1763ms (×0.77)|1766ms (×0.77)|

배수는 1-21의 클립당 2281ms 대비. 각 값은 3~4회의 중앙값이다.

※ 가로 B-1은 회차별로 3007 / 2905 / **1838**ms이며 3회차에서 정상값으로 수렴했다. 앱 실행 후 첫 인코딩의 워밍업 비용이고 클립 수와 무관하다. 1-15에서 겪은 "첫 회 일회성 비용 204.8ms"와 같은 성격이다. **실사용에서 정규화가 처음 도는 시점은 이미 카메라 running + 촬영 직후라 B-2 조건이지 B-1 조건이 아니다.**

- **클립당 약 1.77초.** 10초 촬영 사이클 대비 5.6배 여유가 있다
- **고정비가 거의 없다.** 1-21의 배치(2281ms)보다 단독이 오히려 0.77배 빠르다. 익스포트 세션 생성·컴포지션 구성은 잡음 수준이고 비용의 본체는 인코딩이다. **배치로 묶어도 이득이 없다**
- **세로와 가로가 같다.** 90도 회전이 180도보다 불리할 수 있다는 추정은 성립하지 않는다 (1763~1791ms로 동일 범위)
- **카메라 running이 비용에 영향을 주지 않는다.** 세로 stopped 1791ms vs running 1769ms. 가로는 B-1이 워밍업에 오염되어 3회차 1838ms를 대조군으로 쓰면 running 1763ms와 견준다. 어느 쪽도 running이 느리지 않다 — 인코더가 캡처 파이프라인과 경합하지 않는다
- **발열이 걸리지 않는다.** 연속 4회에서 1회차→4회차가 세로 0.99배 / 가로 1.01배, `thermalState`는 전 구간 `nominal`. 냉각 조건(B-3′)과 누적 조건(B-3)의 차이도 없다
- **duration이 재인코딩 후 5980/600(9.966667s)으로 정규화된다.** 원본이 무엇이든 같은 값으로 수렴한다 (세로 9.938333s → +28.3ms, 가로 9.960000s → +6.7ms). CFR 정규화로 보인다
- `FileManager.replaceItemAt` 방식이 성립한다. 정상 교체 후 트랙이 온전하고, 잘못된 대상 URL과 새 파일 없음 두 경우 모두 throw 하며 **원본이 크기까지 동일하게 살아남는다.** 두 계열에서 확인했다

측정 코드는 `NormalizeMeasurement.swift` / `ClipNormalizeController.swift` 와 `ContentView`의 측정 버튼이며 3-13에서 제거한다.

---

## Swift 언어 모드 재검토 (2-2)

0-4에서 미뤄둔 판단. **결론: 5를 유지하고 2-18 착수 직전에 다시 본다.**

**실측 (Xcode 26.6 / SDK iOS 26.5).** 언어 모드 6에서 깨지는 곳은
**총 3건 · 2파일**이다.

|파일|건수|내용|성격|
|---|---|---|---|
|`CaptureTrace.swift:24`|1|`static var isEnabled` 가 nonisolated 전역 가변 상태|**해소함.** 쓰기가 없어 `let` 으로 바꿨다|
|`ClipNormalizeController.swift:160,288`|2|`AVComposition` 을 `@MainActor` 에서 nonisolated 함수로 sending|남은 블로커|

**2-6·2-7 이후에도 3건이 유지된다** (2-7 리뷰에서 확인). 세션 이름을
`DateFormatter` `static let` 으로 만들려다 **non-Sendable 정적 상태라 4건이
될 뻔했다.** `Calendar` 로 월·일만 뽑아 조립하는 형태로 바꿔 baseline 을
지켰다 — 문자열은 같고 로케일 의존도 함께 사라졌다. **이 숫자가 2-18
재검토의 근거이므로 조용히 늘어나면 안 된다.**

**Phase 1 카메라 파이프라인은 0건이다.** 0-4 당시 "AVFoundation 동시성
어노테이션 문제"라는 인상보다 범위가 훨씬 좁았다.
`CaptureSessionController`는 경고 3건뿐이고 언어 모드 6에서도 에러가
아니다. **2-1 모델 3개는 Swift 6에서 경고조차 없이 통과한다** (단독
컴파일로 확인).

**`#Predicate` 매크로 전개의 `KeyPath` Sendable 경고는 실체가 없다.**
언어 모드 5 + strict concurrency 에서만 "this is an error in the Swift 6
language mode"로 뜨고, 실제 언어 모드 6에서는 나오지 않는다.

**그런데도 5를 유지하는 이유.**

- 남은 블로커는 `AVComposition`이 격리 경계를 넘는 문제다. **SDK 26.5에서도
  `AVComposition`은 여전히 non-Sendable이다** — 0-4의 원인이 해소되지 않았다
- `sending` 파라미터로는 안 된다. 호출부가 `Prepared` 구조체를 계속 들고
  있어 소유권 이전이 성립하지 않는다(실제로 시도해 확인)
- 제대로 고치려면 "prepare + export를 메인 밖에서 한 단위로 돌린다"는
  **정규화 경로의 동시성 계약**을 정해야 한다. 그 계약의 진짜 소비자는
  2-18이고, 지금 정하면 **3-13에서 삭제될 측정 코드의 호출 형태에 맞춰
  설계**하게 된다
- `ClipNormalizeController`는 B 라운드 측정 코드다. 지금 구조를 바꾸면
  1.77초의 근거가 된 코드 형태가 달라진다
- 2-3 ~ 2-16이 만들 코드는 **메인 컨텍스트 전용**이라 언어 모드 5에서도
  동시성 부채가 거의 쌓이지 않는다

**언제 다시 보나: 2-18 착수 직전.** 그때는 `AVComposition` 경계 문제를
어차피 풀어야 하고(정규화가 백그라운드로 돌아야 한다), 2-19의 직렬 큐가
이 프로젝트에서 동시성 검사가 가장 값어치 있는 지점이다. 무기한 미룸이
아니다.

**중간 경로(`SWIFT_STRICT_CONCURRENCY=complete` 유지)는 택하지 않았다.**
경고가 1건에서 9건으로 늘고 그중 3건은 위의 실체 없는 `#Predicate` 경고다.
경고를 무시하는 습관이 생기는 값이 얻는 것보다 크다.

---

## Phase 2 — 구조화

Phase 1에서 검증된 파이프라인 위에 세션 개념을 올린다.

### 2-A. 저장 계층

- [x] **2-1** SwiftData 모델 정의 (`Session`, `Clip`, `Orientation`)
    - **스키마 형태 확정 (A 라운드).** `Orientation`은 `String` raw value로
      저장하고 계산 프로퍼티로 감싼다. `#Predicate`가 enum을 지원하지 않기
      때문이며, 근거는 CLAUDE.md "API 주의사항" 참고. 확정된 설계 결정(방향
      2값)에는 영향이 없다 — 바뀌는 것은 영속화 형태뿐이다.
      `Clip`에는 enum 필드를 두지 않으므로 이 제약에 걸리지 않는다.
    - `Mellow/Models/` 아래 `Orientation` / `Session` / `Clip` 세 파일
    - **`Session.OrientationState` (`unset` / `missing` / `decided` /
      `corrupted`)로 정상적인 미정과 값 손상을 구분한다.** `Orientation?`
      만으로는 전부 `nil`로 보인다. 저장 프로퍼티는 늘리지 않았다 — 네 상태
      전부 `orientationRaw`와 `clips` 관계에서 파생된다. 복구는 2-16의
      몫이며 여기서는 판별 수단만 남겼다
    - `orientationRaw`가 nil이 되는 경로는 **둘**이다. 첫 클립이 아직
      없어서(정상 = `.unset`)와, 값이 있었는데 사라져서(손상 = `.missing`).
      클립 유무가 이 둘을 가른다. 처음에는 전자만 고려해 `.unset`으로
      뭉뚱그렸고 Codex adversarial review에서 지적받아 고쳤다 — 뭉뚱그리면
      2-8이 방향을 다시 정해 계열 혼재가 만들어질 수 있다
    - predicate 팩토리는 `matching(_:)` / `orientationRawIsNil()` /
      `inProgress()` 세 개. 전부 `String`을 캡처한다
    - **`orientationRawIsNil()`은 저장 상태 기준이라 `.unset`과 `.missing`을
      함께 반환한다.** 조회로는 갈리지 않으므로 구분이 필요하면 결과를
      `orientationState`로 다시 걸러야 한다. 2-8이 이를 빠뜨리면 `.missing`
      세션에 방향을 다시 정해 계열 혼재가 만들어진다. 팩토리를 나눌지는
      2-7·2-16의 실제 사용 형태를 보고 정한다
    - **결론 (2-7 착수 후): 나누지 않는다.** 2-7이 `inProgress()`로 fetch한
      뒤 Swift에서 `isResumable`로 거른다. 애초에 `.unset`과 `.missing`을
      가르는 predicate를 **만들 수가 없다** — `orientationState`는
      `orientationRaw`뿐 아니라 `clips` 관계를 세는 계산 프로퍼티라 키패스가
      없고, 이는 enum 미지원과는 별개의 제약이다. 팩토리를 나누는 문제가
      아니라 **조회는 저장 상태 기준, 판정은 코드에서**가 이 모델의 구조적
      귀결이다. 이름을 raw 기준으로 붙여둔 것이 그 사실을 드러내고 있었다
    - 클립 순서는 모델의 `orderedClips`가 책임진다. SwiftData의 to-many
      관계는 순서를 보장하지 않으므로 호출부에 맡기지 않는다
- [x] **2-2** `ModelContainer` 설정 및 앱 진입점 연결
    - **메인 컨텍스트 전용 (확정).** 별도 `ModelContext`·`ModelActor`를
      만들지 않는다. 근거는 CLAUDE.md "SwiftData 사용 원칙"
    - 스토어는 `Library/Application Support/Mellow.store`. 영상은
      `Documents/sessions/{uuid}/clips/`(2-3)라 **둘이 다른 디렉터리에 있다.**
      한쪽만 살아남는 복원이 성립하므로 2-16은 고아를 양방향으로 봐야 한다
    - **컨테이너 생성 실패 시 크래시시키지 않는다.** 영상은 `Documents`에
      무사하므로, 사용자가 앱을 지워 영상까지 날리지 않도록 안내 화면을
      띄운다. 스토어를 새로 만들거나 지우지 않는다 — 메타데이터를 버리면
      영상이 전부 고아가 된다. 화면 자체는 3-15에서 제대로 만든다
    - `mainContext.autosaveEnabled`는 기본 `true`로 확인했다. 삽입·삭제가
      명시적 `save()` 없이 반영된다
    - 저장 확인 프로브를 `ContentView`에 붙였다. 3-13에서 다른 측정 버튼과
      함께 지운다
- [x] **2-3** 파일 관리자 구현
    - 세션 디렉터리 생성 / 삭제
    - 상대 경로 → 절대 경로 변환 (**절대 경로는 저장하지 않는다**)
    - `Mellow/Storage/SessionFileStore.swift`. **이 타입만 세션 디렉터리
      경로를 안다.** 2-4·2-5·2-12·2-16·2-D가 전부 여기를 거친다
    - **경로를 돌려주는 API만 두고 받아 보관하는 API를 두지 않았다.**
      절대 경로를 저장할 수 있는 표면 자체가 없어야 재설치 후에도 안 깨진다
    - `Session`/`Clip`이 아니라 `UUID`와 파일명을 받는다. 파일시스템만 다루는
      타입이 SwiftData를 알 이유가 없고, `@Model` 객체는 메인 컨텍스트에
      묶여 있어 인자로 돌리면 격리 문제를 이 층까지 끌고 온다
    - **루트(`Documents/`)를 주입받는 구조다.** 전역 상수로 박으면 Mac
      하네스가 사용자의 실제 `~/Documents`를 건드린다. 앱은 `.shared`를 쓴다
    - **삭제는 "이미 없음"과 실패를 구분한다.** `Bool`을 돌려주고 진짜
      실패만 throw 한다. 없는 것을 지우려는 것은 2-16이 정상적으로 밟는 경로다
    - 실패는 `.outOfSpace` / `.notPermitted` / `.failed(underlying:)` 셋으로
      접는다. **`.outOfSpace`를 따로 둔 이유는 2-14다** — 뭉뚱그리면
      "공간을 비우세요"를 말할 근거가 사라진다
    - **`adopt(fileAt:as:in:)` 추가 (2-4 직후).** 밖에서 만들어진 파일을
      세션으로 들이는 동작. 2-4가 쓰면서 `FileManager` 직접 호출과
      `SessionFileError.mapping` 호출이 이 타입 밖으로 새어나간 것을 보고
      되돌렸다. **경로에 이어 파일 변경 표면도 여기로 모았다** — 2-3에서
      절대 경로 금지를 API 형태로 강제한 것과 같은 논리다.
      2-18의 `replaceItemAt`이 같은 모양이 될 것이므로 지금 막지 않으면
      새어나감이 두 곳으로 늘어난다.
      `replace(clipNamed:with:in:)`은 만들지 않았다 — 2-18에서 실제 요구가
      드러날 때 붙인다. 지금 만들면 소비자 없는 API가 하나 더 생긴다

    **임시 파일 위치 — `.itemReplacementDirectory` (확정)**

    2-18의 정규화가 temp에 쓰고 `replaceItemAt`으로 교체한다.
    **원자적이려면 교체 대상과 같은 볼륨이어야 한다** — 볼륨이 다르면
    rename이 아니라 복사 후 삭제가 되어 중간 상태가 생기고, 2-18이 요구하는
    "원본이거나 교정본이거나 둘 중 하나"가 깨진다.

    경로를 손으로 조합하지 않고 `FileManager.url(for: .itemReplacementDirectory,
    appropriateFor: clips/)`를 쓴다. 같은 볼륨을 잡아주는 것이 이 API의 용도다.
    하네스에서 `volumeIdentifier`를 대조해 실제로 같은 볼륨임을 확인했고,
    2-18이 밟을 경로 그대로 `replaceItemAt`까지 돌려봤다.

    **iCloud 백업 제외 — 제외한다 (확정)**

    `sessions/` 루트에 `isExcludedFromBackup`을 건다.

    - **완성본은 사진 앱에 저장되고 그쪽에서 백업된다.** `sessions/`는 그
      완성본을 만들기 위한 작업 파일이다. 결과물이 이미 백업되는데 재료를
      또 백업할 이유가 약하다
    - 클립 30개가 570MB다. 세션이 쌓이면 GB 단위로 사용자의 iCloud 용량을
      **조용히** 먹는다. 사용자가 동의한 적 없는 소비다
    - v0.1은 클라우드를 쓰지 않는다(PRD 범위 밖)

    **맞바꾼 것: 기기를 복원하면 세션이 전부 사라진다.** 사진 앱에 저장한
    완성본만 남는다. 아직 저장하지 않은 진행 중 세션은 잃는다. 이것이
    받아들일 만한지는 Phase 4 실사용에서 다시 본다 — 되돌리는 것은 이
    플래그 한 줄이다.

    루트에만 걸면 하위 전체에 적용된다. 세션마다 걸지 않는다.
    **확인은 macOS 하네스에서 했고 실기기 확인은 하지 않았다.**
    - **백업 제외 실패는 더 이상 치명적이지 않다** (2-7 리뷰). 예전에는
      `setResourceValues` 실패가 `createSessionDirectory` / `adopt` 전체를
      실패시켜 **세션을 아예 시작할 수 없었다.** 백업 편의 하나 때문에 앱이
      못 쓰게 되고, 디렉터리는 이미 만들어진 뒤라 재시도마다 새 UUID 로 고아가
      쌓였다 — 막으면서 새기까지 했다. 이제 로그만 남기고 계속한다
    - **그래서 반쪽이 뚫려 있다.** 제외에 실패하면 `sessions/` 가 iCloud 백업에
      포함되는데 **사용자에게 가는 신호가 없다.** 이 결정의 근거가 "사용자
      iCloud 용량을 조용히 먹는 것을 막는다" 였으므로, 조용히 먹는 상태가 여전히
      가능하다는 사실은 기록해 둔다. 사용자에게 알릴지는 2-14·3-15에서 본다

- [x] **2-4** 클립 저장: 파일 이동 + 메타데이터 기록을 하나의 단위로
    - `Mellow/Storage/ClipStore.swift`. `SessionFileStore`의 첫 소비자다
    - **순서: 파일 먼저, 메타데이터 나중.** 완전한 원자성은 불가능하므로
      어긋났을 때 덜 나쁜 쪽으로 기울였다. 파일만 남으면 **고아 파일**이고
      사용자는 클립이 저장됐다고 들은 적이 없다 — 2-16이 조용히 지우면 끝난다.
      반대로 메타데이터만 남으면 **화면에는 클립이 늘어 있는데 실체가 없고**,
      그 세션의 병합·미리보기가 2-16이 돌 때까지 깨진다. 뒤쪽이 명백히 나쁘다
    - **메타데이터 실패 시 옮긴 파일을 원래 자리로 되돌린다. 지우지 않는다.**
      처음 구현은 지우고 있었고 Codex 리뷰(P1)에서 **사용자 데이터 손실
      경로**로 지적받아 고쳤다. `save()`가 실패하는 현실적 원인은 저장 공간
      부족인데, 하필 방금 찍은 것을 잃으면 안 되는 상황이다. 지우면 같은
      URL로 재시도할 수도 없다.
      되돌리기마저 실패하면 세션 디렉터리에 고아로 남긴다 — 그때도 지우지
      않는다. **어느 경우에도 녹화본을 지우지 않는 것이 계약이다**
    - `.metadata` 에러의 계약: **원본이 제자리로 돌아가 있어 같은 URL로
      재시도할 수 있다.** 그것을 줍는 경로는 2-14에서 판단하고 3-15에서 붙는다
    - **복사가 아니라 이동.** 원본(`temporaryDirectory`)과 목적지가 둘 다 앱
      컨테이너라 같은 볼륨이고, 같은 볼륨 이동은 rename이라 사실상 공짜다.
      10초 클립이 약 19MB라 복사하면 저장 공간이 빠듯할 때(2-14) 불리하다.
      1-19에서 사진 앱 저장에 복사를 고른 것은 **다른 맥락이다** — 그쪽은
      외부 프로세스로 넘기는 것이라 재시도할 원본이 필요했다. 여기서는
      `moveItem`이 실패해도 원본이 제자리에 남아 재시도 여지가 그대로다
    - **`order`는 최대값 + 1이다. 개수가 아니다.** 2-5의 재정렬이 어떤 이유로든
      끝나지 않았을 때 개수를 쓰면 이미 있는 order와 겹친다. **틈은 무해하고
      중복은 해롭다** — 겹치면 완성본의 컷 순서가 흔들린다
    - `fileName`은 클립 id에서 나온다(`{uuid}.mov`). `order`로 지으면 2-5가
      재정렬할 때마다 파일을 개명해야 한다
    - **`alsoApply` 클로저가 2-8이 쓸 자리다.** 넘긴 세션 변경이 클립 기록과
      같은 저장 단위 안에서 적용된다. 2-8은 여기에 방향 확정을 넣으면 되고
      구조를 바꿀 필요가 없다
        - **2-8에서 구조가 조금 바뀌었다.** 짝이 되는 `revertOnFailure`가
          붙었고, `alsoApply` 호출이 `context.insert(clip)` **앞으로** 옮겨졌다.
          근거는 2-8 항목 참고
    - 1초 미만 폐기(1-7)는 `recordedURLs.append` 전에 return하므로 여기까지
      오지 않는다. 코드로 확인했다
    - 자동 연결은 2-6·2-7이 들어와야 성립한다. 2-7에서 프로브가
      `startOrResume`이 돌려준 세션에 넣도록 바뀌었다 (3-13에서 제거)
    - **저장 실패 시 유령 클립을 걷어낸다 (2-7 이후 추가).** `rollback()`만으로는
      스토어에서만 사라지고 **관계와 컨텍스트에는 남는다.** 그 상태가 나쁜 이유는
      파일이 `release`로 되돌아간 뒤라 **2-4가 피하려던 "파일 없는 메타데이터"가
      인메모리에 정확히 만들어지기 때문이다.** 게다가 `orderedClips`에 들어가
      병합 대상이 오염되고, `orientationState`가 `.unset` → `.missing`으로 바뀌어
      **2-7의 이어가기 판정까지 오염된다**
    - **풀 곳이 두 군데다.** 관계는 보유 참조가, 컨텍스트 등록은 재조회 인스턴스가
      쥐고 있고 둘은 같은 객체가 아니다. `clip.session = nil`(보유) +
      `purgePhantom(id:)`(재조회) 둘 다 해야 한다. 실측표는 CLAUDE.md
      "API 주의사항" 참고
    - **2-4의 원자성 판단은 이 케이스를 상정하지 못한 상태에서 내려졌다.**
      `rollback()`이 인메모리를 안 되돌린다는 것은 알고 있었으나, 그 실측이
      업데이트 사례였고 insert는 다르다는 것이 2-7에서 드러났다
    - **회귀 하네스는 `Tools/StorageCheck.swift`에 있다.** 2-4·2-5 하네스가
      이전 세션의 스크래치패드에만 있어 사라졌고, 2-7에서 회귀를 보려다 없어서
      다시 써야 했다. 2-6·2-7 것과 한 파일로 합쳐 저장소에 넣었다 —
      **한 명령으로 저장 계층 전체가 확인된다.** 빌드 명령은 파일 머리에 있다
    - `SessionStore.purgePhantom`과 합치지 않았다. 제네릭으로 뽑으려면 `id: UUID`를
      요구하는 프로토콜을 새로 만들고 두 모델에 채택시켜야 하는데 그 표면이
      8줄짜리 `private` 함수 둘보다 크다. **`insert`하는 곳만 해당하고**
      2-12a(`isClosed`)·2-18(`duration`)은 업데이트라 걸리지 않는다.
      세 번째 소비자가 실제로 오면 그때 뽑는다
- [x] **2-5** 클립 삭제: 파일과 메타데이터 동시 제거, `order` 재정렬
    - **순서: 메타데이터 먼저, 파일 나중 — 2-4와 반대다.** 순서가 뒤집힌
      것이지 원칙이 뒤집힌 것이 아니다. 지키는 것은 늘 하나다:
      **메타데이터가 있으면 파일도 있다.** 저장에서는 파일을 먼저 놓아야
      메타데이터가 파일보다 앞서지 않고, 삭제에서는 메타데이터를 먼저 지워야
      메타데이터가 파일보다 오래 남지 않는다. 어긋나면 양쪽 다 **고아 파일**
      쪽으로 떨어지며 그건 2-16이 조용히 치운다
    - **파일 삭제 실패는 실패로 보지 않는다.** 메타데이터가 이미 사라져
      사용자가 본 결과는 이미 "지워짐"이다. 되돌리려고 메타데이터를 되살리면
      지운 컷이 되살아나 더 놀란다. 로그만 남기고 고아는 2-16이 치운다.
      실패 케이스는 `.metadata` 하나뿐이며 그때는 클립도 파일도 그대로다
    - **재정렬은 삭제와 같은 저장 단위다.** 삭제와 재번호를 다 해놓고
      `save()`를 한 번 부른다. 그래서 **재정렬이 절반만 반영된 상태는
      스토어에 생기지 않는다** — 저장이 실패하면 통째로 되돌아간다.
      틈도 중복도 이 경로에서는 만들어지지 않는다
        - **⚠ 이 문장은 스토어에 대해서만 참이었다. 인메모리에는 거짓이다.**
          아래 "완료 후 발견된 결함" 참고
    - **⚠ 완료 후 발견된 결함과 수정 — 삭제 실패 시 `order`가 중복된다**
      (2-10 구현 중 발견. **2-10과 무관하게 이미 있던 것이다**)
        - **증상**: 클립 3개에서 가운데를 지우다 `save()`가 실패하면 롤백 후
          관계의 `order`가 **`[0, 1, 1]`** 이 된다. 재번호가 인메모리에 남은
          채 삭제된 클립이 되살아나 뒤쪽과 겹친다. 하네스 실측이다
        - **왜 위 문장과 어긋나지 않나.** 스토어는 정확히 `[0, 1, 2]`로
          온전하다. `rollback()`이 되돌리는 것은 스토어뿐이고 **우리가 들고
          있는 객체는 재번호된 값을 그대로 들고 있다**(CLAUDE.md "API
          주의사항"의 `update` 성질). 즉 **"절반만 반영된 상태는 스토어에
          생기지 않는다"는 여전히 맞고, 인메모리에 대해서만 틀렸다**
        - **왜 해로운가.** `orderedClips`가 중복 키로 정렬되어 순서가
          모호해진다. "틈은 무해하고 중복은 해롭다"(2-4)에 정면으로 걸리며,
          그 상태로 미리보기나 병합이 돌면 컷 순서가 흔들린다
        - **수정**: `delete`가 재번호 **이전** 값을 들고 있다가 `catch`에서
          명시적으로 되돌린다. 방향 복원(2-10)과 같은 `catch`에 있지만
          **다른 문제이고 다른 근거다**
        - **stale은 영영이 아니다.** 같은 컨텍스트에서 `Clip`을 다시
          `fetch`하면 스토어 값으로 복원된다(실측). 그러나 **삭제 실패 직후
          곧바로 읽는 호출부는 fetch를 거치지 않는다** — 복원 시점을 우리가
          제어하지 않으므로 코드로 되돌린다
        - **하네스 G-2가 이 회귀를 문다.** 복원을 껐더니 `[0, 1, 1]`이 나와
          실제로 무는 것을 확인했다. **가운데 컷이어야 한다** — 마지막 컷은
          재번호가 애초에 돌지 않아(`each.order != index`가 전부 거짓) 결함이
          드러나지 않는다
        - **G-2는 `fetch` 전에 읽는다.** 조회를 먼저 돌리면 스토어 값으로
          복원되어 결함이 가려진다. 그 순서가 검증의 일부다
    - 남은 클립 전부에 0부터 다시 매긴다. 뒤쪽만 당기지 않는 이유는 2-16이
      파일 없는 메타데이터를 지워 만든 **기존 틈도 같이 메워지기** 때문이다.
      그 경로가 있는 한 2-4의 `order = 최대값 + 1` 선택은 계속 유효하다
    - 재정렬해도 파일을 개명하지 않는다. `fileName`이 클립 id에서 나오기
      때문이다 (2-4 결정). 하네스로 확인했다
    - **마지막 클립도 같은 경로다.** 재정렬할 뒤쪽이 없을 뿐이고 분기하지
      않는다. 2-11이 이 함수를 그대로 쓴다
    - `Deletion.sessionBecameEmpty`로 **이 삭제로 클립이 0개가 되었는지**를
      호출부에 알린다. 이 값을 둔 목적 — **2-10이 직접 클립 수를 세면 세는
      시점과 삭제 시점이 갈라질 수 있다** — 은 그대로 유효하고, 호출부의 UI
      갱신(3-5)과 하네스 관측 지점으로 계속 쓰인다
        - **⚠ "방향 초기화는 여기서 하지 않는다. 2-10이 호출부에서 보면
          된다"는 조항은 2-10에서 뒤집혔다.** 초기화는 `delete` 안으로
          들어간다 — 호출부에서 하면 삭제·재정렬의 `save()` 뒤에 두 번째
          저장이 생겨 그 사이 실패가 "클립 0개 + 방향 남음"을 다시 만든다.
          **막으려던 것(세는 시점과 삭제 시점의 어긋남)은 오히려 사라진다.**
          근거 전체는 2-10 참고

### 2-B. 세션 로직

- [x] **2-6** 세션 생성 (제목 비면 날짜로 자동 생성)
    - `Mellow/Storage/SessionStore.swift`. `ClipStore`와 대칭이며 2-12·2-12a도
      나중에 여기로 온다. 세션 생성이 파일시스템과 SwiftData 양쪽을 한 단위로
      건드리므로 성격이 저장 계층이다
    - **순서: 디렉터리 먼저, 메타데이터 나중.** 세션 층에도 같은 불변식이
      걸린다 — 메타데이터가 있으면 디렉터리도 있다. 어긋나면 빈 디렉터리가
      고아로 남고 2-16이 치운다. 반대면 클립을 쓸 자리가 없는 세션이 생겨
      첫 촬영이 깨진다. 2-A에서 프로브로 실기기 검증한 순서를 그대로 옮겼다
    - **`save()`를 명시적으로 부른다.** 프로브는 autosave에 맡겨 저장 실패를
      관측조차 못 했다. `ClipStore`처럼 실패 시 `rollback()` 후 분류해서 던진다
    - **실패해도 디렉터리를 지우지 않는다.** `ClipStore`가 녹화본을 `release`로
      되돌리는 것과 다르다 — 빈 디렉터리는 되돌릴 데이터가 없으므로 고아로 두는
      쪽이 안전하고, 지우려다 실패하면 처리할 것이 하나 더 생긴다
    - **제목은 저장하지 않고 파생한다.** `title`이 비면 `displayTitle`이
      `createdAt`에서 `8월 9일 세션`을 만든다. 자동 생성분을 `title`에 넣으면
      **사용자가 지은 이름과 우리가 지어준 이름을 가릴 수 없어** 나중에 제목
      편집이 붙을 때 깨진다. "파생 가능한 값을 저장하지 않는다"와도 같은 결이다.
      공백만 입력한 것은 입력하지 않은 것으로 보고 저장 시점에 다듬는다
    - 같은 날 세션 이름이 겹치는 것은 **v0.1에서 넘버링하지 않는다.** 2-7 때문에
      진행 중 세션이 하나뿐이라 겹치려면 하루에 세션을 닫고 또 만들어야 하고,
      홈 목록에서 클립 수·길이로 구분된다
    - **세션을 무조건 만드는 API를 밖에 두지 않는다.** `create`는 `private`이고
      `startOrResume`만 부른다. 근거는 2-7 참고
    - `SessionFileStore.createSessionDirectory`의 반환값(`URL`)을 없앴다.
      "소비자가 나타날 때까지 보류"의 답이 나왔다 — **2-6도 소비자가 아니다.**
      경로를 받아 보관하면 "절대 경로를 저장하지 않는다"를 깨는 표면이 되고,
      필요하면 `sessionDirectory(id)`로 다시 조합한다
    - 방향은 여기서 정하지 않는다. 확정은 첫 클립 저장과 같은 저장 단위여야
      하며(2-8) 그 자리는 `ClipStore.save`의 `alsoApply`다. **비워뒀다**
    - **`save()` 실패 경로를 하네스로 검증했다.**
      `ModelConfiguration(url:allowsSave: false)` 로 API 층에서 결정적으로
      실패시킨다. 파일 권한을 건드리는 방법은 통하지 않는다 — SQLite 가 이미
      연 fd 로 쓰기 때문이다. `.metadata` 분류 · 디렉터리가 고아로 남는 것 ·
      메타데이터가 늘지 않는 것을 전부 확인했다
    - **저장 실패 시 유령 세션을 걷어낸다.** `rollback()`만으로는 스토어에서만
      사라지고 컨텍스트에는 남아 `@Query`가 그것을 그린다(2-7 리뷰 지적 →
      하네스 실측). 보유 참조로는 어떤 순서로도 안 지워지고 **재조회한
      인스턴스를 지워야** 한다. 실측표는 CLAUDE.md "API 주의사항" 참고
    - `displayTitle`이 **파생값이라 여행하면 이름이 하루 밀려 보일 수 있다**
      (서울에서 만든 세션이 LA에서 하루 앞으로). 타임존은 기기 로컬이 맞다 —
      밤 11시에 찍은 세션이 다음 날짜로 보이면 안 된다. **감수한 트레이드오프다**
    - **`Session.init`은 막지 않았다. 이 프로젝트의 첫 예외다.** 지금까지 세 번은
      전부 "표면 자체를 없앤다"였다(절대 경로 저장 금지 · `adopt`으로 파일 변경
      표면 봉쇄 · `create` private). 리뷰는 같은 논리로 `Session(title:)` +
      `insert`만으로 디렉터리 없는 메타데이터가 만들어진다고 지적했고 그 지적은
      유효하다. 여기서 "규율로 지킨다"를 고르는 근거는 **2-16이 그 위반을 어차피
      검출한다**는 것이다 — 불변식이 생성 시점이 아니라 복구로 지켜지는 구조이고,
      같은 이유로 "이어가기 때 디렉터리 재보장"도 거절했다(`adopt`이 이미
      디렉터리를 만들므로 중복이고, 2-16이 봐야 할 신호를 지운다).
      **재검토 트리거: 두 번째 생성 호출부가 생기거나 모듈이 갈라지면 다시 본다**
- [x] **2-7** 진행 중 세션 조회 — 있으면 새로 만들지 않고 이어가기
    - **이어갈 수 있는 세션의 정의**: `isClosed == false` **그리고**
      `orientationState ∈ { .unset, .decided }`. `Session.isResumable`에 있다
    - **`.missing` / `.corrupted`는 후보에서 제외한다.** 이어가면 2-8이 그
      세션의 방향을 **다시** 정하게 되고, 먼저 찍힌 클립과 계열이 다르면 한
      세션에 세로·가로가 섞여 정규화로도 복구되지 않는다. 제외된 세션은
      목록에 그대로 보이고 미리보기·삭제도 된다. 복구는 2-16의 몫이다
    - **이로써 2-8은 이 문제를 다시 고민하지 않아도 된다.** 2-8 항목의 "착수 시
      기존 세션을 지우거나 `.missing` 처리 경로를 먼저 만들어야 한다"를 후자로
      해결한 것이다
    - **두 단계다.** `inProgress()`로 fetch → Swift에서 `isResumable` 필터.
      `orientationState`가 `clips` 관계를 세는 계산 프로퍼티라 `#Predicate`에
      들어갈 수 없다(enum 미지원과 별개의 제약). 2-1의 predicate 팩토리 결론은
      그쪽 메모 참고
    - **API**: `resumableSession() throws -> Session?` /
      `startOrResume(title:) throws -> Start`, `enum Start { created, resumed }`.
      **`startOrResume`이 내부에서 직접 조회하는 것이 핵심이다** — F-01 AC를
      호출부 분기에 맡기면 배너를 그리지 않는 경로가 하나만 생겨도 세션이
      늘어나기 시작하고, 그 증가는 조용해서 한참 뒤에 발견된다
    - `.resumed`일 때 넘긴 `title`은 버린다. 이름이 있는 세션을 이어가는 것이다
    - **후보가 여럿이면 `createdAt` 최신 하나.** 나머지는 건드리지 않고 로그만
      남긴다. 자동으로 닫으면 사용자 데이터를 우리 판단으로 마감하는 것이라
      2-5의 "어느 경우에도 녹화본을 지우지 않는다"와 결이 맞지 않는다
    - 프로브의 "세션 +1"을 "세션 시작"으로 바꿔 `SessionStore`를 부르게 했다.
      **생성 경로가 둘이면 실기기에서 검증한 것과 실사용이 갈린다.** 3-13에서
      버튼이 사라져도 `SessionStore`는 남는다
    - 프로브의 클립 저장·삭제도 **`startOrResume`이 돌려준 세션**을 향하게
      바꿨다. 예전에는 `sessions.first`(최신)였는데 둘이 다를 수 있고, 다르면
      2-8이 "첫 클립 저장과 방향 확정이 같은 저장 단위인가"를 확인해도 무엇을
      확인한 것인지 알 수 없다
    - **⚠ 2-8 없이는 2-7이 성립하지 않는다.** 방향을 쓰는 코드가 없으면
      (`ClipStore.save`의 `alsoApply` 호출부가 0이다) 클립이 들어가는 순간
      모든 세션이 `.missing`이 되어 이어가기 후보에서 빠지고, **F-01 AC가
      깨진다** — 탭할 때마다 세션이 새로 생긴다. 2-8은 2-7의 선행 의존이며
      **반드시 바로 다음 태스크여야 한다**
    - 하네스 9군이 이 상태를 실행 결과로 남긴다("#1 재현"). **2-8이 들어오면
      그 네 줄의 기대값이 뒤집혀야 하고, 그때 2-8이 이 문제를 닫았다고 말할
      수 있다**
        - **닫혔다 (2-8).** 9군을 `ClipStore.save` 경유로 다시 쓰고 네 줄이
          뒤집혔다: `.missing`→`.decided(portrait)`, 이어가기 불가→가능,
          "세션이 새로 생긴다"→안 생긴다, 세션 2개→1개.
          **직접 삽입으로는 뒤집히지 않았다** — 그 경로가 `alsoApply`를
          타지 않기 때문이며, 그래서 9군은 저장 계층을 반드시 거쳐야 한다
    - **왜 놓쳤나.** 하네스 4번이 정확히 이 상태를 만들어놓고 "이어가기 불가 ✓"를
      찍었다. `.missing`에 "손상"이라는 이름이 붙어 있었기 때문에 기대값이
      그렇게 적혔다 — **테스트가 가정을 인코딩했다.** `.missing`의 정의("클립이
      있는데 방향이 없는 세션은 존재할 수 없다")는 2-8이 있어야 참이 되는
      명제인데, 지금은 그것이 모든 세션의 정상 상태다. Phase 1에서 두 번 밟은
      "근거로 삼은 값이 실제로 무엇을 잰 것인지 안 따진 것"과 같은 종류다
- [x] **2-8** 세션 방향 확정: 첫 클립 촬영 시점 방향을 기록
    - **첫 클립 저장과 세션 방향 확정은 같은 저장 단위여야 한다.** 나눠서
      `save()`하면 그 사이 크래시로 "클립이 있는데 `orientation`이 nil"인
      세션이 생긴다. 이 상태에서 2-8이 "아직 미정"으로 판단하면 다음 촬영이
      다른 계열로 방향을 다시 정할 수 있고, **한 세션에 세로·가로가 섞여
      정규화로도 복구되지 않는다**(계열 간은 차단 대상이다)
    - 2-4의 원자성은 **클립 하나에 대한 것**이지 클립 저장과 방향 기록
      사이의 원자성이 아니다. 여기서 따로 보장해야 한다
    - 원자성으로 경로를 좁히되 크래시 타이밍을 완전히 막을 수는 없으므로,
      `Session.OrientationState.missing` 판별은 별도로 유지한다
      (2-1 Codex 리뷰에서 나온 요구사항)
    - **착수 시점에 기기에 `.missing` 세션이 남아 있을 수 있다.** 2-8 이전에
      만들어진 세션은 방향이 `NULL`인데 클립이 있는 상태다. 실제로 2-A 검증
      기기에 그런 세션이 하나 있었다(클립 2개, 화면에 "방향 없음(손상)").
      2-8이 이 세션을 `.unset`으로 오인하면 다음 촬영이 방향을 다시 정하고,
      **계열이 섞이면 정규화로도 복구되지 않는다**
        - **⚠ 그 세션은 없다.** 2-8 실기기 검증 뒤 "전부 삭제"로 소실됐다.
          2-16이 쓸 유일한 실데이터 손상 표본이었다. 자세한 것은 2-16 참고
    - 착수 시 기기의 기존 세션을 지우고 시작하거나, `.missing` 처리 경로를
      먼저 만들어야 한다. `orientationRawIsNil()`이 `.unset`과 `.missing`을
      함께 반환한다는 점을 반드시 고려해라 — predicate 이름이 raw 기준인
      이유가 이것이다
    - **방향은 파일의 `preferredTransform`에서 도출한다.**
      `VideoTrackSpec.orientation`이 `naturalSize.applying(preferredTransform)`으로
      렌더 규격을 구하고 폭·높이만 비교한다. 각도→방향 상수 테이블을 만들지
      않는다는 제약이 유지되며, 기기·카메라와 무관하게 성립한다.
      **2-16·2-17이 같은 프로퍼티를 쓴다** — 판정 수단이 갈라지면 세 곳이 같은
      파일을 다르게 본다
        - 계열 내 180도는 렌더 규격이 같아 한 값으로 접힌다. 그것이 `Orientation`이
          2값인 이유이며, 180도를 봐야 하는 2-17은 `preferredTransform` 원본을
          직접 읽는다 — `VideoTrackSpec`이 두 값을 그대로 들고 있으므로 막히지 않는다
        - 정사각은 `nil`이다. 한쪽으로 붙이면 판정 불가가 조용히 정상값으로 둔갑한다
        - **`captureRotationAngle`을 쓰지 않는다.** 그것은 *지금* 각도이고, 녹화와
          "클립 저장" 사이에 임의의 시간·회전이 끼어든다
    - **`Session.orientation`의 setter를 없앴다.** getter가 `.decided`일 때만 값을
      주므로 `if session.orientation == nil { 세팅 }`이 **`.missing`과 `.corrupted`를
      둘 다 덮어썼다.** 쓰기는 `decideOrientation(_:) -> Bool` 하나로 모으고
      `.unset`일 때만 통과시킨다. `@discardableResult`를 붙이지 않는다 — 반환값을
      봐야 저장 실패 시 되돌릴 대상인지 알 수 있다
    - **⚠ `alsoApply`가 `context.insert(clip)` 앞으로 옮겨졌다. 순서에 이유가 있다.**
      `Clip(session:)`이 그 자리에서 관계를 맺으므로 insert 뒤에는 `session.clips`에
      새 클립이 이미 들어가 있고, `orientationState`가 그 관계를 세기 때문에
      **방향이 아직 없는 세션이 그 순간 `.unset`이 아니라 `.missing`으로 보인다.**
      그 상태에서 `decideOrientation`을 부르면 가드가 막아 **첫 클립이 방향을 영영
      정하지 못한다.** 구현 첫 시도가 정확히 여기서 걸렸고 하네스 9군이 잡았다
        - **가드를 `.missing`까지 허용하는 것은 답이 아니다.** 그러면 2-8 이전에
          만들어진 진짜 손상 세션의 방향을 다음 촬영이 새로 정하고, 계열이 다르면
          한 세션에 세로·가로가 섞인다
        - 둘 다 `save()` 앞이므로 **저장 단위는 그대로 하나다.** 클로저에게
          "이 클립이 붙기 전의 세션"을 보여주는 것이 "첫 클립인가"의 원래 판단 기준이다
    - **비디오 트랙이 없거나 방향을 못 정하면 저장하지 않는다.** 이것이 2-8의 진짜
      완료 조건이다 — 이 거부가 있어야 `.missing`이 정상 경로로 만들어질 수 없게 되고,
      2-1이 `.missing`을 "값이 있었는데 사라진 손상"으로 정의한 것이 **그때부터
      참이 된다.** 하네스 4군이 손상 상태를 재현하려고 저장 계층을 우회하는 이유도 이것
        - **파일은 지우지 않고 `forgetRecorded`도 부르지 않는다.** "어느 경우에도
          녹화본을 지우지 않는다"가 계약이고, 목록에 남겨둬야 재시도 여지가 있다
    - **저장 실패 시 세션 변경을 되돌린다 (`revertOnFailure`).** 클립 삽입은
      `insert`라 롤백 후 유령으로 남고, 방향은 `update`라 **롤백해도 인메모리에 새
      값이 그대로 남는다.** 걷어내지 않으면 스토어는 "방향 없음·클립 없음"인데
      인메모리는 **"방향 있음·클립 없음"**이 된다. 다음 촬영이 방향을 이미 정해진
      것으로 읽고, 2-9가 들어오면 **클립이 하나도 없는 세션에서 녹화가 막힌다**
        - **스토어에 흔적이 없어 2-16이 이 어긋남을 검출하지 못한다.** 그래서
          규율이 아니라 코드로 막는다
            - **⚠ 정정: "영영"이라고 적었는데 인메모리 stale은 영영이 아니다.**
              같은 컨텍스트에서 `Session`을 다시 `fetch`하면 스토어 값으로
              복원된다(2-10 하네스 실측). 타입별이라 `fetch<Clip>`으로는
              복원되지 않는다. 2-16이 못 본다는 것은 **스토어에 흔적이
              없다**는 뜻으로만 참이다
            - **그래도 실질적 위험은 줄지 않는다.** 실패 직후 곧바로 읽는
              호출부(2-9 녹화 차단 판정, 3-5 버튼 상태)는 `fetch`를 거치지
              않고 stale을 그대로 본다. **"언젠가 복원된다"가 "그 사이에
              안전하다"를 뜻하지 않으며**, 복원 시점을 우리가 제어하지도
              않는다. 코드로 되돌린다는 결론은 그대로다
        - **호출부에 맡기지 않은 이유**: `save`는 이미 실패 시 파일을 `release`로
          되돌리고 유령 클립을 걷어낸다. 원상복구를 저장 단위 안에서 하고 있으므로
          세션 변경만 밖에 두면 대칭이 깨진다. 대칭이 깨진 것을 한참 뒤에 발견하는
          것이 2-7→2-8에서 실제로 일어난 일이다
        - **무엇을 되돌릴지는 호출부가 판단한다.** 두 번째 이후 클립은
          `decideOrientation`이 `false`를 돌려주는데, 그 경우까지 되돌리면 이미
          스토어에 있던 방향을 지운다. 호출부가 반환값을 들고 있다가 넘긴다
        - **`revertOnFailure`가 클로저인 이유가 이것이다.** 되돌릴 대상을
          호출부만 알기 때문이지, 저장 계층이 정책을 몰라야 해서가 아니다.
          **정책이 호출부마다 같은 경우에는 클로저로 뺄 이유가 없다** —
          2-10이 이 판단을 참조해 방향 초기화를 `ClipStore.delete` 안에
          직접 넣는다
    - **하네스 9군을 `ClipStore.save` 경유로 다시 썼다.** 예전에는
      `insert(Clip(...))`로 직접 넣어 `alsoApply`를 타지 않았고, **구현이 아무리
      옳아도 결과가 `.missing` 그대로였다.** 직접 삽입으로 되돌리면 이 검증이
      조용히 무력해진다
    - 하네스 D군이 실패 시 방향 복귀를, E군이 실측표 4케이스의 방향 도출을 본다.
      **D군이 회귀를 실제로 무는지 확인했다** — revert 호출을 껐더니
      `.decided(landscape)`가 나왔다
    - **실기기 게이트 (iPhone 12).** 절차와 기대 로그는
      `Tools/2-8-device-check.md`
        - **항목 2 (재실행 후 `.resumed`) — 통과.** 완전 종료 후 재실행하고
          `세션 시작`을 탭하니 `▶ 이어가기 8월 19일 세션
          id=F6426A5C-BB46-4E12-86EE-04158EC6CE30`. 재고의 id와 같고
          `방향=portrait 1컷`도 일치한다. 새 프로세스가 스토어에서 읽은
          값이므로 방향이 인메모리가 아니라 저장까지 갔다는 것도 함께 확인됐다
        - **항목 4 (거꾸로 촬영해도 `.portrait` 유지) — 통과.** 이번 게이트에서
          가장 강한 증거다. `도출 방향=portrait  transform=a:0.0 b:-1.0 c:1.0
          d:0.0 tx:0.0 ty:1920.0` — transform이 CLAUDE.md 실측표의 **거꾸로(270)
          행과 정확히 일치**하고(세로 행 `tx:1080 ty:0`과 명확히 다르다),
          도출값이 `landscape`가 아니다. 세션은 `방향=portrait` 유지,
          `order 목록 = 0,1`
            - **`도출 방향=` 로그(`be4b794`)가 없었으면 판정이 성립하지 않았다.**
              두 번째 클립은 가드에 막혀 세션 방향을 어차피 안 바꾸므로,
              `방향=`만 보면 도출이 틀려도 통과한다
        - **항목 1 (첫 클립이 방향을 정한다) — 조건부 통과.** `order=0`의
          `도출 방향=` 줄이 콘솔 유실로 없다. 대신 재실행 후 재고가
          `방향=portrait 메타 1컷 / 파일 1개 이어가기=가능`이었다
            - **실질은 지지된다.** 클립이 있는 세션이 `.missing`이 아니라
              `.decided`이고 재실행을 넘어 살아남았다 — 2-8이 닫으려던 문제
              (클립 하나에 모든 세션이 `.missing`이 되어 F-01 AC가 깨지는 것)가
              실기기에서 닫혔다
            - `orientationRaw`를 쓰는 곳은 셋뿐이고(`init`은 항상 `nil`,
              `decideOrientation`, `undoOrientationDecision`은 `nil`만),
              `decideOrientation`의 앱 내 호출부는 하나다. 따라서 **값이 있으면
              그것은 어떤 클립의 도출 결과다**
            - **⚠ 구멍: 값을 쓴 클립과 지금 남아 있는 클립이 같다는 보장이
              없다.** 저장 → 삭제 → 재저장이면 방향이 먼저 클립 것으로 남는다.
              **이것은 2-8이 아니라 2-10 부재의 효과다** — `ClipStore.delete`는
              방향을 건드리지 않고, 클립 0개가 되어도 초기화되지 않는다
            - ~~세로(90) → portrait 도출은 실기기에서 미확인이다~~ →
              **닫혔다 (2-10 게이트).** 아래 참고
        - **✅ 항목 1이 2-10 게이트에서 무조건 통과로 닫혔다** (2026-08-21,
          iPhone 12). 2-10이 빈 세션을 만들 수 있게 해서 가능해졌다 —
          클립을 전부 지우면 `.unset` 세션이 남고, 거기에 세로로 첫 컷을
          찍었다

          ```
          [probe]   도출 방향=portrait  transform=a:0.0 b:1.0 c:-1.0 d:0.0 tx:1080.0 ty:0.0
          [probe] ■ 클립 저장  order=0  9.940s
          [probe]   방향=portrait
          ```

            - **`도출 방향=` 줄을 실제로 찍었다.** 콘솔 유실로 없던 그 줄이며,
              **세로(90) → portrait 도출이 실기기에서 처음 확인됐다**
            - `transform`이 실측표 **세로(90) 행과 완전 일치**한다
              (`tx:1080 ty:0`). 거꾸로(270)의 `tx:0 ty:1920`과 명확히 갈리므로
              우연히 맞은 것이 아니다
            - **`order=0`이 독립 증거다.** 클립이 실제로 0개였다는 뜻이라
              "빈 세션에서 찍었다"는 전제를 뒤에서 받친다
            - **위 "⚠ 구멍"도 함께 닫혔다.** 저장 → 삭제 → 재저장에서 방향이
              먼저 클립 것으로 남는 문제는 2-10 부재의 효과였고, 이번 게이트가
              그 경로(2컷 삭제 → 0컷 → 미정 → 새 첫 컷)를 그대로 밟아 방향이
              **새 클립에서 다시 나오는 것**을 확인했다
        - **항목 3 (`.missing` 세션 불변) — 판정 불가. 게이트에서 뺀다.**
          한때 통과로 적었다가 철회했다
            - **`.missing` 세션은 `decideOrientation`의 인자가 될 수 없다.**
              그 함수는 `ClipStore.save`에 넘긴 세션에만 불리고, 그 세션은
              `startOrResume`이 돌려준 것이며, `isResumable` 필터가 `.missing`을
              제외한다. **앱 UI로는 도달 경로 자체가 없다**
            - 그래서 8:35~9:37 관찰(손상 세션이 전 구간 불변)이 검증한 것은
              **2-7의 제외 동작이지 2-8의 가드가 아니다.** 손대지 않은 세션이
              안 변하는 것은 자명하다 — 그 세션에 닿은 코드가 없었다
            - **실기기로 검증 불가능한 항목이므로 하네스로 옮겼다.** 4군이
              `.missing` 세션에 `decideOrientation(.landscape)`를 직접 불러
              `false` 반환과 상태 불변을 확인한다
            - `.corrupted` 거절은 **여전히 미검증이다.** `orientationRaw`가
              `private`이고 쓰는 곳 셋이 전부 유효값만 넣어서, 해석 불가 문자열을
              넣을 방법이 없다. 스토어 SQLite를 직접 고치는 수밖에 없는데
              CoreData의 `Z` 접두 스키마에 하네스를 묶는 비용이라 **2-16에서
              `.corrupted` 복구를 만들 때 함께 정한다**
        - **부수 확인.** 클립 세 개가 전부 18.7MB(15.0Mbps)로 프리셋 특성과
          일치한다(CLAUDE.md의 클립당 19.0MB와 1.6% 차이). `tmp` 고아 1개는
          계약상 정상이다 — `클립 저장`을 안 눌러 메타데이터가 안 생겼고
          "어느 경우에도 녹화본을 지우지 않는다"가 지켜졌다. 소유자는 3-17이다
          (2-16 대상이 아닌 이유는 3-17 항목 참고)
        - **기기 초기화는 `tmp`를 비운다.** 129개가 1개가 됐다. 2-A의
          "재설치는 tmp를 비우지 않는다"와 배치되지 않는다 — 재설치가 아니라
          컨테이너째 사라진 것이다. 3-17이 인용할 값이다
    - **하네스가 설계 오류를 잡은 첫 사례다.** 지금까지 하네스는 이미 내린
      결정이 유지되는지 보는 회귀 장치였는데, 2-8에서는 **구현 첫 시도의
      설계가 틀렸다는 것을 하네스가 먼저 말했다** — 9군이 전부 실패했고 그
      원인이 `alsoApply` 호출 위치였다. 코드 리뷰나 실기기 검증이 아니라
      하네스가 잡았다
        - 잡을 수 있었던 이유는 **9군을 `ClipStore.save` 경유로 바꿔 경로를
          실사용과 같게 만들었기 때문이다.** 직접 삽입이었다면 `alsoApply`를
          타지 않아 그대로 통과했을 것이고, 오류는 실기기에서 "방향이 안 붙는다"
          로 나타났을 것이다
        - **반대 사례도 같은 실행에 있다.** 항목 3은 실기기 관찰이 가드를
          검증한다고 잘못 읽혔고, 그것을 바로잡은 것은 코드였다. 검증 수단마다
          닿는 범위가 다르다는 것이 2-8에서 양방향으로 드러났다
- [x] **2-9** 방향 불일치 시 녹화 차단
    - **(A) 판정 소스는 `UIDevice.current.orientation`이다.**
      `UIDeviceOrientation.isPortrait` / `.isLandscape`로 계열을 얻는다.
      앱이 이미 `CaptureSessionController.deviceOrientation`으로 들고 있다
        - **"각도→방향 상수 테이블을 만들지 않는다"(CLAUDE.md) 제약과
          충돌하지 않는다.** 그 제약이 금지한 것은 **우리가 각도를
          해석하는 것**이고, `UIDeviceOrientation`은 각도가 아니라 이미
          기기 방향이다. 세로/가로 판정을 UIKit이 한다
        - **근거: AVFoundation에 각도→계열 API가 없다** (SDK 26.5 헤더
          확인). `AVCaptureDevice.RotationCoordinator`의 공개 표면은 각도
          두 개뿐이고, `AVCaptureVideoOrientation`(portrait/landscape 4값)은
          iOS 17에서 deprecated되면서 대체 API가 각도만 준다. **애플이 매핑
          책임을 앱으로 넘긴 것이고 우리 제약은 그 책임을 거부한다.**
          UIKit 경로는 그 책임을 UIKit에 돌려주는 길이다
        - `videoRotationAngleForHorizonLevelCapture`는 **판정에 쓰지
          않는다.** 촬영 파이프라인용과 판정용은 목적이 다르다
    - **(B) 계열이 안 나오면 차단하지 않는다.** `.faceUp` / `.faceDown` /
      `.unknown`에서 통과시킨다. **상태를 들고 있지 않는다**
        - 근거: **브이로그를 폰 수평으로 눕혀 찍는 자세는 없다.** 녹화 시작
          시점에는 항상 세로 아니면 가로다. 이 구간은 촬영 자세가 아니라
          **폰을 들거나 내릴 때 지나가는 상태**다
        - "마지막 유효 계열 유지"안을 검토했으나 채택하지 않았다. 실제로
          찍는 순간엔 계열이 확실해 기능적 차이가 없고, 차이는 지나가는
          구간에 버튼이 깜빡이느냐뿐이다. **깜빡임이 실기기에서 실제로
          관찰되면 그때 넣는다**
        - 3-7a의 아이콘 회전은 **이 결정과 별개다.** 그쪽은 같은 세 상태에서
          무엇을 보여줄지를 따로 정해야 한다
    - **(C) 히스테리시스는 만들지 않는다 — 확정. 실기기 관찰로 닫혔다.**
      종전 항목 "기울기 경계에서 안내가 떨리지 않도록 임계값(히스테리시스)
      처리"를 만들지 않는다
        - **관찰 (2-9 실기기 게이트 5번)**: 경계 45도 부근에서 천천히
          왕복하고 **10초간 손목으로 흔들었으나 버튼·안내가 깜빡이지
          않았다.** 착수 시점에는 필요하다는 실측도 불필요하다는 실측도
          없어 보류였고, **그 공백을 이 관찰이 메웠다**
        - **원인: `UIDevice`의 방향 통지가 자체 임계값을 갖고 있어 각도를
          연속으로 따라오지 않는다.** 임계값이 이미 UIKit 안에 있으므로
          우리가 겹쳐 넣을 이유가 없다
        - **판정 소스를 UIKit으로 고른 (A) 결정의 부수 효과다.** 각도를
          직접 읽었다면(`videoRotationAngleForHorizonLevelCapture`)
          **임계값을 우리가 만들어야 했다** — (A)는 "각도→방향 테이블을
          만들지 않는다"를 지키려고 고른 것인데, 히스테리시스까지 함께
          가져왔다. 두 결정이 독립이 아니었다는 뜻이며, **(A)를 뒤집으면
          (C)도 함께 열린다**
        - **PRD 요구는 살아 있다. 건드리지 않았다** — F-03 AC "기기를 천천히
          기울일 때 안내가 깜빡이며 튀지 않는다", 8장 "세션 방향과 기기 방향
          불일치" 행의 "기울기 경계에서 안내가 떨리지 않도록 임계값 처리".
          **요구는 충족됐다. 다만 우리가 임계값을 만들어서가 아니라 UIKit이
          이미 갖고 있어서다.** 요구사항 문면을 고칠지는 별도 판단이며,
          구현하지 않기로 한 근거가 여기(2-9 (C))에 있다
        - 지금 넣었다면 **임계값을 추측으로 정했을 것이다.** Phase 1에서 두 번
          밟은 "근거로 삼은 값이 실제로 무엇을 잰 것인지 안 따진 것"의 반대
          방향 — 재지도 않고 대책부터 만드는 것 — 을 피했다
        - **재검토 트리거**: 다른 기기·OS에서 깜빡임이 관찰되면. 이 관찰은
          iPhone 12 한 대의 것이다
    - **(D) 최소 안내 문구까지 2-9가 만든다.** 판정 + 버튼 비활성 +
      **불일치 안내 한 줄**이 이 태스크의 범위다
        - 근거: 차단만 있고 이유가 없으면 커밋 시점에 **"버튼이 죽었는데
          왜인지 모르는" 상태**가 남는다. 실기기 검증에서 정상인지 버그인지
          구분이 안 된다. **2-8에서 정한 "원자성이 깨진 중간 상태를 커밋에
          남기지 않는다"와 같은 근거다** — 판정과 그 결과를 알리는 것을
          나누면 나눈 사이가 검증 불가 구간이 된다
        - 문구를 다듬는 것은 3-6이다. 2-9는 **읽을 수 있는 한 줄**이면 되고
          카피·배치·애니메이션은 그쪽 몫이다
    - **착수 시 알아야 할 사실 (2-9 조사)**
        - **녹화 버튼에 `.disabled` 훅 자체가 없다.** `ContentView.recordButton`
          에 없고 시작 가드는 `CaptureSessionController.startRecording()`의
          `guard let movieOutput, setupState == .configured, intent == .idle`
          뿐이다. **2-9는 훅과 판정 양쪽을 새로 만들어야 한다**
        - **⚠ `UIDevice.current.orientation`과
          `videoRotationAngleForHorizonLevelCapture`가 항상 일치하는지는
          미확인이다.** 판정 소스와 파일 transform 소스가 다르므로, 어긋나면
          **버튼은 통과시켰는데 파일은 다른 계열로 찍히는 경로**가 생긴다.
          그 클립은 2-8이 세션 방향과 다른 계열로 판정하게 되고, 계열 간
          혼재는 정규화로도 복구되지 않는다
        - **확인 방법: 녹화 시작 시점에 두 값을 함께 찍는 로그를 넣고
          실기기에서 네 방향을 왕복한다.** `startRecording()`이 이미 각도를
          읽는 자리가 있으므로(`let angle = rotationCoordinator?...`) 그
          옆에서 `UIDevice.current.orientation`을 함께 찍으면 된다
    - **구현 결과**
        - **판정을 `RecordingGate`(`Mellow/Capture/RecordingGate.swift`)로
          뺐다. UIKit을 import 하지 않는다.** 판정 소스는
          `UIDevice.current.orientation`이지만 **경계를 계열에서 잘랐다** —
          `UIDeviceOrientation`을 인자로 받게 만들었으면 Mac 하네스에서 못
          돈다(실측: macOS에 UIKit이 없다). 잘라낸 쪽
          (`UIDeviceOrientation.family`)에는 우리 로직이 없다 — UIKit의
          `isPortrait`/`isLandscape`를 그대로 부르는 세 줄이다
        - **각도→방향 상수 테이블이 코드 어디에도 없다.** 이 경로를 고른
          이유가 그것이고, 하네스 F군이 값만 넣어 판정을 검증한다
        - 차단은 뷰(`recordButton`의 `.disabled`)가 한다. **컨트롤러 시작
          가드에 방향 조건을 넣지 않았다** — 그쪽은 상태 기계용이다
        - **⚠ 녹화 중에는 차단하지 않는다.** 이 버튼이 조기 종료를 겸하므로
          (1-6 탭 방식) 녹화 중 폰을 돌렸다고 비활성하면 **시작한 녹화를 끊을
          수 없게 되고 10초가 다 찰 때까지 갇힌다.** PRD 8장 "진행 중인 녹화는
          중단하지 않는다"와 짝이 맞아야 한다. `isRecordingRequested`로 건다
        - 안내 한 줄은 차단 중일 때만 뜬다. **상시 표시를 두지 않았다**(3-6)
        - `[rec] 판정 <계열>  UIDevice=<orientation>  capture각도=<angle>`을
          `startRecording()`에 `#if DEBUG`로 넣었다. **이진 확인: Debug 1건 /
          Release 0건.** 안내 문구는 사용자 UI라 Release에 남는다(1건)
        - 하네스 **F군 11줄 전부 통과.** 지시된 네 경우 + 반대 계열 대칭 +
          연관값(맞춰야 할 방향) + 도달 불가 경로(`.missing`/`.corrupted`가
          통과로 떨어지는 것 = 특별 처리를 만들지 않았다는 고정)
    - **실기기 게이트: 통과 (iPhone 12).** 절차는 `Tools/2-9-device-check.md`

      |항목|결과|근거|
      |---|---|---|
      |1. 세로 세션 + 가로 → 차단|**✓**|화면. `landscapeRight`·`landscapeLeft` 양쪽|
      |2. 다시 세로 → 해제|**✓**|화면|
      |3. 거꾸로(180도) → 차단 안 됨|**✓**|화면. 계열 내 통과|
      |4. `.unset` 세션 + 가로|**✓**|화면. 2-10 게이트에서 닫힘|
      |5. 경계 왕복 → 깜빡임|**없음**|화면. (C) 확정 근거|
      |6. 두 소스 일치|**✓**|콘솔|

        - **✅ 4번이 2-10 게이트에서 닫혔다** (2026-08-21, iPhone 12).
          한때 "미정 세션이 기기에 없었다"로 미확인이었다. **2-10이 빈 세션을
          만들 수 있게 해서 가능해졌다** — 클립을 전부 지우면 `.unset` 세션이
          남는다
            - 화면 상태: `8월 19일 세션 · 0컷 · 미정` + `기기 방향
              landscapeLeft`에서 **녹화 원이 또렷하고 안내 문구가 없다**
            - **`faceUp`이 아니라 `landscapeLeft`인 것이 중요하다.** 판정이
              실제로 가로를 읽고 통과시킨 것이지, 판정이 안 들어가서 그대로인
              것이 아니다 — 2-8 항목 3에서 밟은 함정("그 세션에 닿은 코드가
              없었을 뿐")을 피했다
            - **판정 근거는 화면이다.** 차단은 로그를 남기지 않는다
            - 덤으로 실측표가 재확인됐다 — `capture 0.0 / preview 90.0`으로
              landscapeLeft 행 그대로다
        - **6번 — 판정 소스와 파일 transform 소스가 갈리지 않는다.**
          착수 시 미확인으로 남겨 뒀던 바로 그 항목이다

          ```
          [rec] 판정 portrait  UIDevice=portrait            capture각도=90.0
          [rec] 판정 portrait  UIDevice=portraitUpsideDown  capture각도=270.0
          ```

          transform도 실측표와 일치했다 (90 → `tx:1080 ty:0`,
          270 → `tx:0 ty:1920`). **버튼이 통과시킨 계열과 파일에 기록된
          계열이 같다는 것이 실기기에서 확인됐다**
        - ~~가로 계열(0·180)은 이번에 못 봤다~~ → **가로L(0)이 2-10
          게이트에서 닫혔다** (2026-08-21). 미정 세션에서 가로로 첫 컷을 찍어
          **가로 세션을 처음 만들었다**

          ```
          [rec] 판정 landscape  UIDevice=landscapeLeft  capture각도=0.0
          [probe]   도출 방향=landscape  transform=a:1.0 b:0.0 c:0.0 d:1.0 tx:0.0 ty:0.0
          ```

            - transform이 실측표 **가로L(0) 행과 완전 일치**한다. 6번(두 소스
              일치)이 **가로 계열에서도 성립**하는 것이 확인됐다
            - 저장 후 폰을 세로로 세우니 **녹화 원이 흐려지고 "가로로 들고
              촬영하세요"가 떴다.** 1·2번의 거울상이며 **가로 세션 차단을
              실기기에서 처음 본 것이다**
            - **가로R(180)은 여전히 미확인이다.** 계열 내 180도라 하네스 E군이
              값으로 덮고 있고, 실기기에서 보려면 폰을 반대로 눕혀 한 컷 더
              찍으면 된다. 2-D(정규화) 착수 때 자연히 필요해진다
        - **1~5는 콘솔에 흔적이 없다.** 차단이 뷰의 `.disabled`라 로그를
          남기지 않는다 — 판정이 순수 함수라 부작용이 없어서이고 그것이
          의도다. 위 표의 "근거" 열이 화면인지 콘솔인지를 밝히는 이유다
          (2-8에서 화면 관찰이 가드 검증으로 잘못 읽힌 사례)
- [x] **2-10** 클립 0개가 되면 방향 미정으로 초기화
    - **닫힌 세션은 고려하지 않아도 된다.** 2-12a가 닫힌 세션을 읽기 전용으로
      정했으므로 클립 삭제가 막히고, 따라서 닫힌 세션이 클립 0개가 되는 경로가
      없다. "닫혔고·비었고·방향 미정"인 세션은 도달 불가능하다
    - **⚠ 지금 이 부재가 만드는 상태가 있다 (2-8 실기기 검증에서 드러남).**
      `ClipStore.delete`는 방향을 건드리지 않으므로 **클립을 전부 지워도
      세션은 `.decided`로 남는다.** 그 세션에 다음 클립을 넣으면
      `decideOrientation`이 `false`를 내고 **먼저 지운 클립의 방향이 그대로
      적용된다** — 새 클립의 계열이 달라도 그렇다
        - 2-9가 들어오면 그 세션에서 반대 계열 녹화가 막히므로 사용자에게는
          "클립이 하나도 없는데 가로로 돌리라고 한다"로 보인다
        - 2-8 항목의 "항목 1 조건부 통과"에 걸린 구멍이 이것이다. 저장 →
          삭제 → 재저장이면 `메타 1컷 · 방향=X`인데 그 X가 지금 클립에서
          나온 값이 아닐 수 있다. **2-8의 결함이 아니라 이 태스크의 부재다**
        - 되돌리는 API는 **새로 만들었다** (`resetOrientation()`) —
          `undoOrientationDecision()`은 저장 실패 복구 전용이고 의미가 다르다
          (2-8에서 이름을 겹치지 않게 둔 이유)
    - **훅 위치 확정: `ClipStore.delete` 안에서 초기화한다.** 후보 셋
      (호출부마다 / `delete` 안 / 별도 타입) 중 두 번째다. 근거는 둘이다
        - **(가) 저장 단위 원자성.** `ClipStore.delete`는 삭제와 재정렬을
          하나의 `save()`로 묶는다(2-5 "재정렬은 삭제와 같은 저장 단위다").
          호출부에서 방향을 초기화하면 그 `save()`가 끝난 **뒤**라 두 번째
          저장이 되고, 그 사이 실패하면 "클립 0개 + 방향 남음" — **2-10이
          없애려는 바로 그 상태**가 다시 만들어진다
            - 2-8이 대칭인 문제를 이미 "첫 클립 저장과 세션 방향 확정은 같은
              저장 단위여야 한다"로 풀었다. 마지막 클립 삭제와 방향 초기화만
              다르게 갈 이유가 없다
            - 덧붙여 방향 변경은 `update`라 **롤백해도 인메모리에 새 값이
              남는다** (2-8 `revertOnFailure`가 다룬 것과 같은 성질)
        - **(나) 호출부가 셋 이상이다.** undo(2-11) · 세션 상세의 클립 개별
          삭제(3-11) · **2-16의 파일 없는 메타데이터 정리**. 세 번째가 세션의
          마지막 클립이면 클립 0개가 된다. 호출부마다 붙이면 호출부가 늘
          때마다 빠뜨릴 자리가 하나씩 생긴다
    - **2-5 결정과의 관계 — 뒤집는 것이 아니라 더 곧게 달성한다.** 2-5는
      "방향 초기화는 여기서 하지 않는다"로 정했다. 그 조항이 막으려던 것은
      **"저장 계층이 방향 정책을 안다"가 아니라 "2-10이 클립 수를 따로 세다가
      삭제 시점과 어긋나는 것"**이었다. `delete` 안에서 하면 그 어긋남이
      좁아지는 게 아니라 **사라진다** — 삭제한 코드가 그 자리에 있어 셀 필요가
      없다. 뒤집히는 것은 목적이 아니라 목적을 이루려고 고른 수단 한 줄뿐이다
      (2-5 항목도 그에 맞춰 고쳤다)
    - **`save`의 `alsoApply`와 왜 비대칭인가.** `ClipStore.save`는 클로저
      (`alsoApply` / `revertOnFailure`)를 받는데 `delete`는 직접 호출이다.
      비대칭에 이유가 있다
        - `save`가 클로저인 것은 **정책이 호출부마다 다르기 때문**이다 —
          2-8: "무엇을 되돌릴지는 호출부가 판단한다. 두 번째 이후 클립은
          `decideOrientation`이 `false`를 돌려주는데, 그 경우까지 되돌리면
          이미 스토어에 있던 방향을 지운다"
        - 2-10은 그렇지 않다. undo든 개별 삭제든 2-16의 정리든 **정책이 전부
          같다** — "0개가 되면 미정으로". 갈라질 여지가 없으므로 밖으로 뺄
          이유가 없고, 빼면 넘기는 것을 잊을 자리만 생긴다
    - **`.decided`만 초기화한다. `.corrupted`와 `.missing`은 건드리지 않는다.**
        - `.missing`은 애초에 훅이 필요 없다. `orientationState`가 계산
          프로퍼티라 raw가 이미 `nil`이고, **클립이 0개가 되는 순간 자동으로
          `.unset`이 된다**
        - `.corrupted`는 raw에 값이 남아 클립 0개가 되어도 `.corrupted`로
          남는다. 그래도 초기화하지 않는다 — **2-10이 고치려는 증상이 여기엔
          없다.** 2-10이 막으려는 것은 "먼저 지운 클립의 방향이 새 클립에
          그대로 적용되는 것"인데, `.corrupted`는 값이 파싱되지 않아
          `orientation` getter가 `nil`을 준다. 적용될 방향 자체가 없다.
          **가드에 막히는 것과 잘못된 값이 적용되는 것은 다른 사건이고,
          `.corrupted`는 앞쪽만 해당된다**
        - **⚠ 이것은 확정이 아니라 유보다.** `.corrupted`는 앱 코드로도
          하네스로도 만들 수 없어(`orientationRaw`가 `private`) **어느 쪽을
          골라도 검증할 방법이 없다.** 건드리지 않는 쪽을 고른 것은 그것이
          옳다고 증명돼서가 아니라 **초기화할 근거가 아직 없어서**다
            - 트리거는 새로 만들지 않는다. 2-8에 이미 있다 — "`.corrupted`
              거절은 여전히 미검증이다 (…) 2-16에서 `.corrupted` 복구를 만들
              때 함께 정한다". 그때 같이 본다
            - **클립 0개인 `.corrupted` 세션의 최종 처리는 2-16 명세에서
              정한다.** 복구 근거(첫 클립 파일)가 없는 케이스라 2-16 재검토
              때 별도로 봐야 한다
    - **⚠ 구현 시 함정 (가) — 호출 순서는 2-8의 거울상이다.** 2-8은
      `alsoApply`를 `insert` **앞**에 둬야 했다(클립이 미리 붙으면 `.missing`
      으로 보여 가드가 막는다). 2-10은 반대로 **삭제가 `session.clips`에서
      빠진 뒤**여야 한다. 방향을 먼저 지우면 `raw nil + 클립 있음` =
      **`.missing`을 직접 만든다**
        - 새 발견이 아니다. 2-1 리뷰 때 이미 나왔다 — "`.missing`은 2-10이
          클립 확인 없이 방향을 지우면 실제로 도달하는 상태이며, 클립 0개
          확인은 순서 문제가 아니라 불변식 문제다"
        - 따라서 **`.missing`이 도달 불가라는 2-16의 전제에는 '2-10을 올바로
          만들었을 때'라는 단서가 붙는다**
    - **⚠ 구현 시 함정 (나) — `sessionBecameEmpty`는 남긴다.** 초기화가
      `delete` 안으로 들어가도 이 값을 지우지 마라. 호출부의 UI 갱신(3-5 undo
      버튼 비활성)에 쓰이고, **하네스가 "이 삭제로 0개가 됐다"를 관측할
      지점**이 필요하다. 지우면 검증이 `orientationState`만 보게 되는데 그것은
      결과지 신호가 아니다
    - **⚠ 저장이 실패하면 방향을 되돌려야 한다. 확인하고 넣었다**
        - **`.missing`이 실제로 만들어진다** (`allowsSave: false` 하네스 실측).
          `rollback()` 후 raw는 `nil`인데 **삭제된 클립이 관계에 되살아나서**
          `raw nil + 클립 있음`이 성립한다. `isResumable=false`가 되어 2-7이
          그 세션을 이어가기 후보에서 빼고, 2-9는 `orientation == nil`을 보고
          판정한다
            - **롤백된 `delete`가 되살아난다는 것은 새로 확인한 사실이다.**
              CLAUDE.md에 있던 것은 롤백된 **`insert`**가 유령으로 남는다는
              것이었고, 방향이 반대인 이 사건은 확인된 바 없었다
        - **되돌리기는 `Session`에 짝 API를 두는 쪽으로 했다** —
          `resetOrientation() -> Orientation?` / `undoOrientationReset(_:)`.
          `decideOrientation` / `undoOrientationDecision` 쌍과 같은 모양이다
        - **반환값이 `Bool`이 아니라 `Orientation?`인 이유.** 되돌리려면
          이전 값이 필요하다. `decideOrientation`으로는 되돌릴 수 없다 —
          그 가드가 `.unset`을 요구하는데 **실패 시점의 상태는 `.missing`이라
          막힌다**(실측). `undoOrientationReset`에 가드를 두지 않은 것도 같은
          이유다. 상태를 보고 거절하는 가드를 넣으면 되돌리기가 그 가드에
          막힌다
        - **탈락한 안: `catch`에서 컨텍스트를 재조회해 새로 고친다.**
          `fetch<Session>` 한 번이면 방향과 `order`가 함께 복원되고 새 API도
          필요 없다(실측). 그런데 그것은 **되돌릴 대상 하나를 위해 같은
          컨텍스트의 모든 세션을 흔드는 것**이고, `@Query`로 그리는 화면이 그
          순간 무엇을 보는지 통제하지 못한다. **2-4에서 이미 같은 갈림길에
          섰고** `purgePhantom(id:)`로 "방금 만든 id 하나만" 걷어내기로 정했다.
          그 판단을 뒤집을 새 근거가 없으므로 같은 결론이다
        - **`order` 복원도 같은 `catch`에 있지만 2-10이 아니다.** 2-5의 기존
          결함이며 근거가 다르다. 2-5 항목 "완료 후 발견된 결함" 참고
    - **⚠ `context.delete(clip)`은 관계를 그 자리에서 비우지 않는다** (실측).
      삭제 직후에도 `session.clips`에는 지운 클립이 남아 있고, `save()`가
      끝나야 빠진다. 그래서 "0개가 되는가"를 **미리 계산한 `remaining`**으로
      판단한다 — 2-5가 "삭제 후의 `session.clips`를 믿지 않는다"고 적어둔
      것이 여기서 한 번 더 쓰인다
        - 그 때문에 `save()` 전까지 세션이 잠깐 `.missing`으로 보인다. 2-8이
          저장 단위 안에서 `.missing`을 스쳐 지나가는 것과 같은 성질이고,
          **이 구간은 전부 동기·메인 액터라 아무도 그 상태를 읽지 않는다**
    - **하네스 (2-10에서 고치고 더한 것)**
        - **B군 픽스처를 `alsoApply` 경유로 고쳤다. 구현의 일부다.** 예전에는
          `alsoApply`를 넘기지 않고 저장해서 그 세션이 **`.missing`**이었다 —
          2-8이 "정상 경로로는 만들어지지 않는다"고 한 상태를 저장 계층을
          거치고도 하네스가 만들고 있었다. 파라미터가 옵셔널이라 안 넘겨도
          조용히 통과한다
            - **여파는 C군에서 터진다.** 방향이 없는 세션은 클립을 전부 지워도
              `.unset`이 되므로 **2-10이 있든 없든 결과가 같아** 회귀를 물지
              못한다. 2-8이 9군을 `ClipStore.save` 경유로 고친 것과 같은
              종류의 구멍이다
        - **C군이 이제 회귀를 문다.** `.decided(portrait)` → 전부 삭제 →
          `.unset`. 중간(2컷 남음)에서 방향이 유지되는 것도 함께 본다
        - **G군을 더했다.** G-1은 삭제 실패 후 방향 복원(D군의 거울상),
          G-2는 `order` 중복 회귀(2-5 결함)
        - **셋 다 실제로 무는지 껐다 켜서 확인했다** (2-8 D군에 한 것과 같다).
          초기화를 끄면 C군 ★ 1건, 방향 복원을 끄면 G-1이 4건
          (`.missing` · `isResumable` · `orientation` · 상태), `order` 복원을
          끄면 G-2가 2건 실패한다
        - **`.corrupted` + 클립 0개는 확인 불가로 남겼다.** 만들 수단이 없다 —
          `orientationRaw`가 `private`이고 쓰는 곳 넷이 전부 유효값이나 `nil`만
          넣는다(`resetOrientation`이 넷째다). 하네스가 그 사실을 한 줄로
          찍는다. **유보가 확정이 아닌 이유가 이것이다**
    - **프로브 로그에 `방향=`을 더했다.** 초기화가 `delete` 안에서 일어나므로
      **호출부에는 흔적이 없고**, 그 줄이 없으면 실기기에서 2-10이 실제로
      돌았는지 관측할 방법이 없다. 2-8에서 `도출 방향=` 로그가 없었으면 항목 4
      판정이 성립하지 않았던 것과 같은 자리다
    - **✅ 실기기 게이트 통과 (2026-08-21, iPhone 12).** 절차는
      `Tools/2-10-device-check.md`. 다섯 항목 전부 통과했고 조건부가 없다

      |항목|판정|근거|
      |---|---|---|
      |1. 클립 0개 → 방향 미정|**✓**|콘솔|
      |2. 빈 세션이 이어가기 후보로 남음|**✓**|콘솔|
      |3. (2-8 항목 1) 세로 첫 컷 → portrait 도출|**✓**|콘솔|
      |4. (2-9 항목 4) 미정 세션 + 가로 → 차단 안 됨|**✓**|**화면**|
      |5. 가로 세션 첫 생성|**✓**|콘솔 + 화면|

        - **항목 1 — 0개가 되는 순간에만 초기화된다.** 2컷 세션에서 한 컷씩
          지웠다

          |남은|`방향=`|
          |---|---|
          |1컷|`portrait`|
          |**0컷 · 비었음**|**`미정`**|

            - **`없음(손상)`이 아니라 `미정`이다.** 이것이 "함정 (가)를
              피했다"는 직접 증거다 — 방향 초기화가 삭제보다 앞에 있었으면
              `.missing`으로 찍혔을 것이고, 그 구분은 이 한 줄로만 난다
            - `order=1`을 먼저 지웠다. `orderedClips.last`가 최대 `order`를
              잡으므로 PRD의 "직전에 촬영한 클립"과 일치한다
            - `재정렬 후 order 목록 = 0` → `(없음)`, 둘 다 `✓ 저장 결과와 일치`
            - **단계 5에서 두 번째 사례를 얻었다.** 방금 `portrait`가 붙은
              세션(1분 미만)을 지웠는데 똑같이 `미정`이 됐다 — 오래된 세션에만
              도는 것이 아니다
        - **항목 2 — 세션은 남고 방향만 돌아간다.** `id` 동일 ·
          `방향=미정 메타 0컷 / 파일 0개 이어가기=가능` · `디렉터리=있음`
            - 2-12(세션 삭제)의 영역을 침범하지 않았고 고아 파일도 없다
            - PRD 3장의 "잘못된 방향으로 첫 컷을 찍었을 때의 탈출구"가
              실기기에서 성립한다
        - **항목 3·4·5의 결과는 2-8·2-9 항목에 적었다.** 그쪽이 원래 주인이다
        - **부수 확인 — 한 사이클이 완주됐다.** `▶ 이어가기 … 방향=미정 0컷`
          로그가 **빈 세션을 실제 이어가기 경로로 잡는 것**을 보여준다. 재고
          조사가 아니라 `startOrResume`이 찍은 줄이다. 그 세션에 첫 컷이
          들어가 방향이 새로 정해졌으므로, **"미정으로 돌아간 세션은 다시
          방향을 정할 수 있다"는 2-10의 존재 이유가 끝까지 확인됐다**
        - **⚠ 저장 실패 경로는 이 게이트가 지나가지 않는다.** 실기기에서
          `save()` 실패를 재현할 수단이 없다. 그쪽 검출은 하네스 G군과
          코덱스 적대적 검토가 전부다 — 게이트 통과를 "실패 경로도 봤다"로
          읽으면 안 된다
- [x] **2-11** 마지막 컷 undo (연속 실행 가능)
    - **⚠ 구현 없이 닫힌 것이 아니라 선행 태스크로 흡수됐다.** 이 구분이
      중요하다 — "구현이 없다"로 읽으면 나중에 "왜 코드가 없지"가 되고,
      **어디로 갔는지**를 적어야 그 질문이 안 생긴다

      |F-04가 요구하는 것|어디로 갔나|
      |---|---|
      |삭제 · 재정렬 · 파일 처리|**2-5** (`ClipStore.delete`)|
      |방향 초기화|**2-10** (`delete` 안으로 들어갔다)|
      |대상 선정 (`orderedClips.last`)|**옮길 정책이 없었다** — 저장 단위와 무관한 읽기다|
      |0개일 때 비활성 (AC)|**3-5**|
      |필름스트립에서 사라짐 (AC)|**3-4**|

        - **2-10이 결정적이었다.** 2-11이 저장 계층에 가져갈 것이 남아
          있었다면 그건 방향 초기화인데, 훅이 `delete` 안으로 들어가면서
          **호출부가 잊으면 깨지는 것**이 사라졌다
        - **남은 AC 둘은 전부 UI이고 Phase 3 소관이다.** 2-11이 로직
          태스크로 요구하는 것은 이미 전부 동작한다
        - `ClipStore`에 `deleteLastClip(of:)` 같은 편의 API를 두지 않는다.
          호출부가 하나(3-5)이고 `session.orderedClips.last`는 이미 공개된
          읽기다 — **표면만 는다.** 2-5 API 점검에서 "없다"가 좋은 답이었던
          것과 같은 자리다
    - **"연속 실행 가능"에 코드가 필요 없다. 구조가 이미 보장한다**
        - `ClipStore`가 `@MainActor struct`이고 `delete(_:)`가 **동기
          `throws`**다. `async`가 아니라 **중단점이 없고** 메인 액터라 다른
          삭제가 끼어들 수 없다. 연타해도 각 호출이 완결된 뒤 다음이 시작된다
        - **하한도 구조로 지켜진다.** 호출부의
          `guard let clip = session.orderedClips.last else { return }`이 매번
          현재 상태를 다시 읽으므로 0개가 되면 조용히 반환한다
        - **2-10 적대적 검토가 `remaining` 스냅샷의 격리 근거로 든 것과 같은
          성질이다** — "계산부터 `save`까지 다른 삭제·삽입이 끼어들 수 없다"
    - **확인 근거**
        - **하네스 C군** — 연속 삭제로 0개까지 가는 경로를 그대로 돌린다
          (`for clip in bs.orderedClips { … delete … }`). 마지막 컷 삭제 후
          `order 0,1` · 클립 0개 · 방향 `.unset` 복귀까지 본다. **2-11용
          하네스를 따로 만들지 않는다**
        - **2-10 실기기 게이트** (2026-08-21, iPhone 12) — 단계 2에서 연속
          2회(2컷 → 1컷 → 0컷), 단계 5에서 1회. PRD F-04의 "누를 때마다 한
          개씩 뒤로"와 "0개가 하한"이 실기기 로그로 확인됐다
    - **⚠ undo가 만드는 상호작용이 2-D에 명세돼 있지 않다.** 정규화 큐에
      들어간 클립을 사용자가 undo하면 **큐의 대상이 사라진다.** 2-D
      전체(2-17~2-21)에 "삭제"도 "undo"도 한 번 나오지 않는다.
      **2-19에 걸었다** — 해법은 2-D 착수 시 정한다
    - **"연속 실행 가능"은 PRD F-04 "연속 undo 가능 (누를 때마다 한 개씩
      뒤로)"이다.** 한 번에 한 개씩 지우고, **0개까지 갈 수 있다** —
      PRD F-04 AC가 "클립이 0개일 때 undo 버튼은 비활성 상태다"이므로
      0개가 하한이고 거기서 버튼이 꺼진다(비활성 처리는 3-5)
    - **세션은 남는다.** 클립이 0개가 되어도 세션을 지우지 않는다. 방향만
      미정으로 돌아가고(2-10) 그 세션은 계속 이어갈 수 있다.
      PRD 3장: "첫 클립을 undo해서 클립이 0개가 되면 방향은 미정 상태로
      되돌아간다. **잘못된 방향으로 첫 컷을 찍었을 때의 탈출구다**"
    - **`ClipStore.delete`를 그대로 쓴다. 확인했다.**
      `delete(_:)`가 마지막 클립을 분기 없이 처리한다 — 함수 주석
      "마지막 클립도 같은 경로다. 재정렬할 뒤쪽이 없을 뿐이고 분기하지
      않는다"가 그 근거이고, 하네스 C군이 마지막 컷 삭제를 실제로 돌린다
      ("마지막 컷 삭제 후 2컷" / "order 가 0,1")
    - **order 재정렬은 코드가 이미 조건부로 건너뛴다.** 재번호 루프가
      `for (index, each) in remaining.enumerated() where each.order != index`
      라, 마지막 컷만 지우면 남은 클립의 `order`가 이미 `0..<n`이어서
      **쓰기가 한 건도 일어나지 않는다.** 별도 분기를 만들 이유가 없다
        - **"마지막이면 재정렬이 불필요하다"고 단정하지는 마라.** 2-16이
          파일 없는 메타데이터를 지워 틈이 생긴 세션에서는 마지막 컷을
          지워도 앞쪽이 다시 매겨진다. 그 경우까지 위 조건이 알아서 덮는다
    - **undo 대상은 `order` 최대값 클립이다.** `nextOrder`가 최대값+1이라
      가장 최근 클립이 항상 최대 `order`를 가지므로 PRD의 "직전에 촬영한
      클립"과 일치한다. 현재 프로브도 `session.orderedClips.last`를 쓴다
    - **⚠ 2-10과의 경계 — 정해졌다**
        - 번호 순서상 **2-10이 먼저 들어온다.** 따라서 2-11은 방향 초기화를
          직접 구현하지 않는다
        - **2-10이 훅을 `ClipStore.delete` 안으로 정했다.** 호출부가 셋 이상
          이고(undo · 3-11 개별 삭제 · 2-16 정리) 정책이 전부 같아서다.
          따라서 **2-11은 `delete`를 부르기만 하면 방향이 함께 초기화된다** —
          undo 경로에 따로 붙일 것이 없다. 근거는 2-10 참고
        - `Deletion.sessionBecameEmpty`는 그대로 남는다. 2-11은 이 값을 방향
          초기화가 아니라 **호출부 UI 갱신**(3-5 undo 버튼 비활성)에 쓴다
    - **파일 삭제 실패는 성공으로 보고된다.** `delete(_:)`가
      `removeClip` 실패를 로그만 남기고 삼키며 `fileRemoved: false`로
      돌려준다(2-5 결정 — 사용자가 본 결과는 이미 "지워짐"이다).
      **PRD F-04 AC "파일시스템에 파일이 남지 않는다"는 그 실패 경로에서
      즉시 참이 아니다** — 고아 파일로 남고 2-16이 치운다. AC를 이 계약에
      맞춰 읽을지 AC를 고칠지는 확인 불가이며 3-5/검증 시 판단한다
- [x] **2-12** 세션 삭제 (디렉터리 전체 제거)
    - **닫힌 세션도 삭제할 수 있다.** 2-12a의 "닫힌 세션은 읽기 전용"은
      **클립 추가·변경 불가**를 뜻하며 **세션 삭제는 예외다**
        - 근거: 읽기 전용의 의도는 **완성본과 클립이 어긋나는 것을 막는
          것**이지 삭제 금지가 아니다. 사진 앱 저장까지 끝난 세션을 앱에
          영구히 남길 이유가 없고, 안 지워지면 목록이 계속 길어진다
        - 2-12a AC 표의 "클립 0개 세션 닫기 — 불가. 저장할 것이 없다.
          **삭제(2-12)만 가능**"과도 맞는다
    - **사진 앱에 저장된 완성본은 건드리지 않는다.** 우리 소유가 아니고
      사용자가 이미 자기 라이브러리에 가진 것이다. **앱이 지우면 안 된다.**
      세션 삭제는 **앱 내부 데이터(세션 디렉터리 + 메타데이터)만** 지운다
    - 이미 있는 것: `SessionFileStore.removeSessionDirectory(_:)`가 디렉터리를
      통째로 지우고 "이미 없음"(`false`)과 실패(throw)를 구분한다.
      `Session.clips`의 `deleteRule: .cascade`가 클립 메타데이터를 함께
      지운다 — 클립을 하나씩 지울 필요가 없다
    - **순서는 기존 원칙이 이미 정한다** — 삭제는 **메타데이터 먼저, 파일
      나중**이다(CLAUDE.md "메타데이터와 파일의 정합성"). 어긋나면 고아
      디렉터리가 남고 2-16이 치운다. 반대로 하면 파일 없는 세션이 화면에
      남는다
    - **✅ `.cascade`가 실제로 도는 것을 확인했다.** 선언이지 관측이 아니었고,
      이 프로젝트는 SwiftData 선언 동작이 예상과 다른 것을 세 번 밟았다
      (`#Predicate` enum 미지원 · `transaction`이 롤백하지 않음 ·
      `rollback()`이 insert를 인메모리에 남김). **그래서 값으로 고정했다**
        - 새 컨테이너 재조회 `Clip` 0개 · 같은 컨텍스트 유령 0개
        - **관계를 한 번도 읽지 않고 지워도 돈다.** 목록에서 세션을 골라
          지우는 실사용이 그 모양이라 적대적 케이스로 따로 봤다
        - **파일은 cascade가 지우지 않는다** — 그래서 디렉터리 삭제가 따로 있다
        - 하네스 H-1·H-2. `deleteRule`을 `.nullify`로 바꿔 무는 것을 확인했다
          (클립 2·2·3개가 남는다)
    - **⚠ 순서를 뒤집으면 안 되는 이유를 반증 실험으로 굳혔다.** 디렉터리를
      먼저 지우고 `save()`를 실패시켰더니 **재실행 시점에 세션 1개 · 클립 2개 ·
      디렉터리 없음**이 남았다. 화면에는 컷이 있는데 재생도 병합도 안 되는
      세션이고 **스토어에 영구히 남는다.** 메타 먼저면 저장이 실패해도
      디렉터리를 아직 안 건드렸으므로 그 상태가 만들어지지 않는다
        - 하네스 H-4가 "디렉터리를 안 건드렸다"로 이 값어치를 지킨다.
          순서를 뒤집어 무는 것을 확인했다
    - **디렉터리 삭제 실패는 실패로 보지 않는다. 2-5와 같은 계약이다.**
      `Deletion.directoryRemoved: Bool`을 돌려주고 로그를 남긴다
        - 메타데이터가 이미 사라져 **사용자가 본 결과는 이미 "지워짐"**이고,
          되돌리려고 메타데이터를 되살리면 지운 세션이 목록에 다시 나타나
          더 놀란다. 고아 디렉터리는 2-16이 치운다
        - **⚠ 손실 규모는 2-5보다 크다.** 클립 하나가 아니라 **세션 전체
          (30컷이면 570MB)**가 고아로 남는다. 그래도 결론은 같다 — 사용자가
          명시적으로 "지운다"를 눌렀고, 이 실패로 눈에 보이는 결과를 뒤집는
          것이 더 나쁘다
        - "이미 없음"은 `false`이고 실패가 아니다(하네스 H-3)
    - **⚠ 알려진 동작: 삭제 실패 후 첫 조회가 거짓말한다.** 나중에 발견될
      버그가 아니라 **지금 아는 동작이다**

      ```
      rollback 후 fetch<Session> 1회차 → 0개
      rollback 후 fetch<Session> 2회차 → 1개     (3회 반복 실측)
      ```

        - 스토어는 온전하고 `session.isDeleted`도 `false`이며 클립은 그 사이에도
          살아 있다
        - **실사용에서는 이렇게 나타난다** — 삭제 실패 에러를 던진 직후
          `@Query`가 다시 도는데 그 조회가 1회차라 **세션이 목록에서 사라진다.**
          사용자는 **"삭제 실패 에러를 보면서 동시에 목록에서 사라진 화면"**을
          보고, 앱을 다시 켜면 부활한다. 3-8·3-15가 이 사실을 알아야 한다
        - **되돌리지 않는다. 되돌릴 대상이 없다.** 2-10의 방향·`order`는
          `fetch`를 아무리 돌려도 스스로 고쳐지지 않는 stale이라 명시적 복원이
          필요했지만, 이것은 **2회차 조회부터 저절로 맞는다.** 성질이 다르다
        - 탈락한 두 안:
            - **`context.insert(session)`으로 되살리기** — 이미 스토어에 있는
              객체를 `insert`하는 것이 계약상 맞는지 불확실하다
            - **`catch`에서 더미 `fetch`를 한 번 더 돌려 2회차를 앞당기기** —
              `fetch`는 대상을 지정하는 API가 아니라 **전체를 다시 읽는** API다.
              되돌릴 대상이 세션 하나여도 흔드는 범위는 같고, **2-10에서 같은
              이유로 탈락시켰다**(`purgePhantom`이 "방금 만든 id 하나만"
              걷어내는 것과 대비된다)
        - **하네스 H-4가 이 근거 자체를 회귀로 박는다.** 1회차·2회차 차이가
          바뀌면 이 항목과 코드 주석의 서술이 통째로 틀려진다
        - CLAUDE.md `rollback()` 절에도 단서를 달았다 — **"다시 읽으면 맞다"가
          "한 번 다시 읽으면 맞다"는 아니다**
    - **프로브 버튼은 `세션 삭제`다. `전부 삭제`가 아니다.** 저쪽은
      `SessionStore`를 우회해 `modelContext.delete(session)`을 직접 부르는
      **초기화 수단**이라 2-12가 검증할 대상이 아니다. 실기기 절차는
      `Tools/2-12-device-check.md`
    - **✅ 실기기 게이트 통과 (2026-08-21, iPhone 12).** 네 항목 전부 통과했고
      조건부가 없다. 대상은 2-10 게이트가 남긴 `8월 19일 세션 · 1컷 ·
      landscape`였다

      |항목|판정|근거|
      |---|---|---|
      |1. 세션이 사라진다|**✓**|콘솔 `세션 0개`|
      |2. 디렉터리가 파일시스템에서 사라진다|**✓**|콘솔 `실제 확인=없음 ✓`|
      |3. 클립 메타데이터도 사라진다 (`.cascade`)|**✓**|콘솔 `order=` 줄 없음|
      |4. 삭제 후 `세션 시작`이 새로 만든다|**✓**|콘솔 `+ 새 세션`|

        - **항목 2가 본전이다.** 두 값이 갈리지 않았다

          ```
          [probe] ⌦ 세션 삭제  F6426A5C-BB46-4E12-86EE-04158EC6CE30
          [probe]   디렉터리 지움  실제 확인=없음 ✓
          [probe]   cascade 로 함께 지워질 클립 1개
          ```

          앞은 `SessionFileStore`가 돌려준 값이고 뒤는 **파일시스템에 다시
          물어본 값**이다. 한 줄만 보면 계약이 깨져도 통과하므로 둘을 나란히
          찍는다 — 2-8의 `도출 방향=`과 같은 자리다
        - **항목 3 — `.cascade`가 실기기에서도 돈다.** 하네스 H-1·H-2가 Mac에서
          본 것이 재현됐다. `⚠ 고아 세션 디렉터리` 줄이 없는 것이 항목 2의
          **독립적인 두 번째 근거**이기도 하다
        - **항목 4는 2-7 이후 처음 밟은 생성 경로다.** 그동안 늘 이어갈 세션이
          있어 `startOrResume`의 생성 분기에 도달하지 못했다 — **2-12가 세션을
          0개로 만들 수 있게 되면서 열렸다.** `+ 새 세션 8월 21일 세션
          id=14B264CE…` · `방향=미정 0컷` · `clips/ 있음=true`(2-6 회귀)
        - **부수 확인 — 화면이 독립적으로 같은 말을 했다.** 삭제 직후 녹화
          버튼이 다시 또렷해지고 "가로로 들고 촬영하세요"가 사라졌다.
          `landscape` 세션이 실제로 없어져 2-9가 판정할 대상을 잃은 것이며,
          콘솔과 무관한 경로로 삭제를 확인해준다. 상단 재고 배지도
          `세션 1개` → `0개`로 갱신됐다(`@Query` 교차 확인)
        - **재설치가 `tmp`를 비우지 않는 것이 다시 확인됐다.** 컨테이너 UUID가
          `D8564EB6…` → `613E13E9…`로 바뀌었는데 세션·클립 메타데이터가 그대로
          따라왔고 `tmp` 3개도 크기까지 동일했다(8.2 / 18.7 / 7.5MB). 2-A의
          기록과 일치하며, 2-8의 "129개가 1개가 됐다"가 재설치가 아니라 기기
          초기화였다는 정정을 받친다
        - **⚠ 저장 실패 경로는 이 게이트가 지나가지 않는다.** 실기기에서
          `save()` 실패를 재현할 수단이 없다. "첫 조회가 거짓말한다"를 포함해
          실패 경로 검출은 하네스 H-4가 전부다
- [ ] **2-12a** 세션 닫기 — 저장 성공 시 `isClosed = true`
    - **번호를 미루지 않고 하위 번호로 끼운다.** Tasks.md는 실행 순서
      문서이므로 번호 순서가 곧 실행 순서다. `2-22`로 만들면 실행은
      2-13 앞인데 번호는 뒤가 되어 그 규칙이 깨진다. 2-13 자리에 밀어
      넣으면 2-13~2-21 참조 56곳이 전부 어긋난다(2-16만 20곳)
    - **계약: 닫기 = 사진 앱 저장을 시작하되, 성공한 뒤에만
      `isClosed = true`.** 닫힌 세션은 **읽기 전용**이며 클립 추가·삭제가
      금지된다
        - **PRD F-07 AC가 이미 그렇게 적고 있다** — "닫힌 세션에서는 클립
          추가·삭제가 불가능하다 (4.1). **미리보기와 세션 삭제는 그대로
          된다**". 아래 결정은 새로 정한 것이 아니라 이 항목의 요약이
          떨어뜨린 단서를 되붙인 것이다
        - **"읽기 전용"에 세션 삭제는 포함되지 않는다 (2-12).** 금지 대상은
          **클립 추가·변경**이며, 그 의도는 완성본과 클립이 어긋나는 것을
          막는 것이다. **닫힌 세션도 통째로 삭제할 수 있다** — 저장까지
          끝난 세션을 앱에 영구히 남길 이유가 없다. 사진 앱의 완성본은
          건드리지 않는다
    - 탈락한 안:
        - **단순 상태 플래그** — "세션을 닫으면 완성본이 나온다"(CLAUDE.md
          제품 원칙)가 거짓이 된다. 그 문장은 제품 정의에 가까워 고칠 대상이
          아니다
        - **완성본 생성·저장까지 수행** — 실질 동작은 같으나 **실패 시 상태가
          정해지지 않는다.** 채택안은 "성공한 뒤에만"이 실패 처리를 계약에
          포함한다
        - **저장과 닫기는 독립** — 사용자 결정이 하나 늘어 결정 최소화 원칙에
          어긋난다. "저장했는데 안 닫힘" / "닫혔는데 저장 안 됨" 두 상태가
          새로 생겨 3-8이 세션 카드를 세 종류로 그려야 한다
    - **저장 경로를 새로 만들지 않는다.** 1-18~1-19에서 검증된 병합 ·
      passthrough 익스포트 · 사진 앱 저장 경로를 그대로 쓰고, **성공 콜백에
      `isClosed = true`를 붙이는 것이 이 태스크의 실체다**
    - **이 계약이 2-19·2-21 충돌을 없앤다.** 2-20이 이미 "저장은 정규화 완료를
      기다린 뒤 시작한다"고 정했고, 닫기가 곧 저장이므로 그 대기가 닫기의
      대기다. **정규화가 안 끝난 세션은 애초에 닫히지 않으므로**, "닫힌 세션이
      2-19의 큐 재구성에서 빠져 미정규화 클립이 영영 재시도되지 않는다"는
      경로가 성립하지 않는다
    - **따라서 "닫는 중" 상태를 스키마에 추가하지 않는다.** 저장 진행 표시가
      곧 그 구간이고, 거기서 앱이 죽으면 세션은 **열린 채** 남아 2-19가
      정상적으로 큐를 재구성한다. `isClosed` 불리언 하나로 충분하다
    - UI는 3-13(저장 버튼)에 붙고 3-10(세션 상세)에서 진입한다.
      **버튼을 두 개 만들지 않는다**

    **수용 기준**

    |상황|동작|근거|
    |---|---|---|
    |클립 0개 세션 닫기|**불가**|저장할 것이 없다. 삭제(2-12)만 가능|
    |`.missing` / `.corrupted` 세션 닫기|**불가**|방향이 없어 병합이 성립하지 않는다. 2-16 복구가 선행|
    |정규화 실패 클립(2-21)이 있을 때|**닫힌다**|저장이 그 클립을 병합에서 제외한 채 성립한다 — 2-21이 이미 정한 동작|
    |닫은 뒤 클립 추가·삭제|**금지**|그래서 "닫혔고·비었고·방향 미정"인 세션이 생길 수 없다|
    |저장 실패|**열린 채로 남는다**|2-14·3-15의 사용자 주도 재시도 계약과 맞물린다|
    |재개방|**없다**|닫기 = 저장 완료라 되돌릴 이유가 약하다. Phase 4에서 다시 본다|
    |반복 호출|무해|두 번째 저장은 이미 닫힌 세션에서 일어나고, 첫 저장 때 정규화를 이미 기다렸다|

### 2-C. 견고성

- [ ] **2-13** 권한 거부 상태 처리 + 설정 앱 이동 버튼
- [ ] **2-14** 저장 공간 확인, 부족 시 촬영 진입 전 경고
    - 저장 실패 시 **즉시 재시도**가 의미 있는지 여기서 판단한다. 2-5에서
      실패한 파일을 원래 자리로 되돌려 재시도할 수 있는 상태로 남기지만,
      그것을 줍는 경로는 아직 없다
    - 판단 근거가 되는 것은 **실패 원인의 분포**다. 공간 부족이 유일한
      현실적 원인이라면 즉시 재시도는 무용지물이다(1초 사이에 공간이 생기지
      않는다). 일시적 원인이 실제로 존재하는지를 이 태스크에서 확인한다.
      `save()` 실패는 아직 실기기에서 관찰된 적이 없다 — 하네스에서 유도한
      실패는 인위적이다
    - **경고 전에 앱 자신의 잔여물을 먼저 봐야 한다.** 앱이 만든 tmp 잔여물이
      기기 공간의 5%를 차지한 상태가 실측으로 확인됐다(3.3GB / 64GB, 2-A).
      이 상태에서 "공간을 비우세요"라고 말하면 사용자는 사진이나 다른 앱을
      지우러 가고, **정작 공간을 먹은 것이 이 앱이라는 사실을 알 방법이 없다.**
      최소한 그 사실을 알고 있어야 하며, 경고 전에 자기 정리를 시도할지
      판단한다. 정리 자체는 3-17이 맡는다
- [ ] **2-15** 세션 인터럽션 처리 (전화 수신, 백그라운드 진입)
    - 부분 녹화분을 살릴지 버릴지는 **실기기 동작 확인 후 결정** (PRD 8장)
    - `stopRecording()`은 비동기이며, 현재 코드는 같은 큐에 `stopRunning()`을 바로 예약한다. 파일 마무리 전에 세션이 멈출 수 있는 경로. 재현 확인 후 처리 방침 결정 (1-7 리뷰에서 발견, Phase 1에서는 손대지 않음)
    - `stopPending` 경로는 탭 방식에서 사람 손으로 도달 불가능하다. 콜백 도착 20~58ms vs 최고 탭 간격 128~220ms. 기기가 바쁠 때(저장 공간 압박, 첫 실행 직후 등) 콜백이 지연되면 필요해지는 경로이므로 미실행이라고 제거하지 않는다. 2-15에서 인터럽션을 다룰 때 이 경로가 실제로 필요해지는지 함께 확인한다
- [ ] **2-16** 앱 시작 시 정합성 복구
    - 메타데이터에 없는 고아 파일 정리
    - 파일이 없는 메타데이터 제거
    - 1초 미만으로 폐기된 클립의 파일 삭제가 실패하면 현재는 로그만 남기고 넘어간다(Phase 1 허용). 그렇게 남은 고아 파일을 여기서 함께 정리한다
    - 진행 중 세션의 클립 파일 transform을 세션 방향과 대조해 어긋난 것을 정규화 큐에 넣는다(2-19). **정규화 상태 필드가 없으므로 파일이 유일한 근거다.** 정규화가 어느 지점에서 끊겼든 파일이 원본이면 어긋나 있고 교정본이면 일치하므로, 중간 상태를 해석할 필요가 없다
    - **`.missing`/`.corrupted` 복구는 닫힌 세션도 대상이다.** "진행 중 세션"
      제한이 걸린 것은 바로 위의 transform 대조 항목 하나뿐이다. 방향 복구와
      고아 정리는 닫힘 여부와 무관하게 전체가 대상이며, 파일이 남아 있는 한
      닫힌 세션도 복구할 수 있어야 한다
    - **2-12a의 선행 조건을 보장하는 자리가 여기다.** 닫기는 `.missing`/
      `.corrupted` 세션에서 막히므로(방향이 없으면 병합이 성립하지 않는다),
      그 세션을 다시 닫을 수 있게 만드는 것은 이 복구뿐이다
    - `Session.orientationState`가 `.missing` 또는 `.corrupted`인 세션을 복구한다. **클립이 있으면 첫 클립 파일의 `preferredTransform`이 세션 방향을 말해준다** — 2-17의 방향 판정과 같은 수단이다. `.corrupted`는 연관값에 원래 문자열이 실려 있어 무엇이 들어 있었는지도 알 수 있다
    - 복구 후에도 `.missing`이 남는 경우(클립 파일이 전부 없는 경우)는 고아 메타데이터 정리 경로로 처리한다
    - **스토어는 3파일이다** (`Mellow.store` / `-wal` / `-shm`). 데이터는
      본체가 아니라 `-wal`에 있다 — 실기기에서 본체 77KB는 첫 실행 이후
      변하지 않았고 세션·클립 전부가 350KB의 `-wal`에 있었다 (2-A)
    - **스토어를 파일 단위로 다룰 때 세 파일을 함께 다뤄야 한다.** `-wal`만
      유실되면 메타데이터가 통째로 사라진 것처럼 보이고, 그 상태에서 고아
      판정을 돌리면 **세션 디렉터리의 영상을 전부 지운다**
    - 따라서 정합성 복구는 "메타데이터가 비었다"를 곧바로 "고아다"로 읽으면
      안 된다. **스토어 자체의 이상과 개별 항목의 불일치를 구분해야 한다.**
      판별 수단과 그때의 동작(중단할지, 사용자에게 알릴지)을 여기서 정한다
    - `Application Support`는 백업 대상이고 `sessions/`는 제외이므로
      (2-2·2-3), 복원 시나리오에서 양쪽 불일치가 실제로 발생한다
    - **⚠ 손상 세션 픽스처가 없다.** 2-A 검증 기기에 `.missing` 세션이 하나
      있었는데(2-8 이전에 만들어진 것, 클립 2개, 방향 `NULL`), 2-8 실기기
      검증 뒤 "전부 삭제"로 소실됐다. **2-8 항목의 "기기에 그런 세션이 하나
      남아 있다"는 서술은 그래서 정정됐다** — 지금은 없다
        - **하네스로는 만들 수 없다.** `orientationRaw`가 `private`이라
          바깥에서 `.missing`/`.corrupted` 상태를 직접 쓸 방법이 없다. 그
          제약은 2-8 이후에도 그대로다 — `decideOrientation`은 `.unset`일
          때만 쓰고 `undoOrientationDecision`은 `nil`로만 되돌린다
        - **`.missing`은 클립을 저장 계층 밖에서 직접 삽입해야 만들어진다.**
          하네스 4군이 그렇게 한다(`context.insert(Clip(...))`). 앱에는 그
          경로가 없다
    - **⚠ 2-8이 검증 대상의 성격을 바꿨다. 착수 시 이것부터 반영해라.**
      2-8이 저장 거부를 넣어(비디오 트랙이 없거나 방향을 못 정하면 저장하지
      않는다) **`.missing`이 정상 경로로는 더 이상 만들어지지 않는다.**
      2-1이 `.missing`을 "값이 있었는데 사라진 손상"으로 정의한 것이 그때부터
      참이 됐다
        - **⚠ 정정: `.missing`은 2-8 이후 도달 불가다.** 이 항목에 한때
          "남은 발생 경로는 셋 — 파일 유실 · 강제 종료 · 스토어 손상"이라고
          적었는데 **틀렸다. 셋 다 `.missing`을 만들지 않는다.** #2 로그 문구
          판단 중에 경로를 하나씩 짚어보다 드러났다
        - **근거**: `.missing`은 `orientationRaw == nil && !clips.isEmpty`인데,
          2-8이 `alsoApply`와 `insert`를 **한 `save()` 트랜잭션**에 넣었다.
          방향과 클립이 함께 들어가거나 함께 안 들어가므로 그 조합이
          만들어지지 않는다
        - 후보 경로별 **실제** 결과:

          |경로|실제로 만들어지는 상태|2-16이 다뤄야 할 것|
          |---|---|---|
          |파일 유실 (`sessions/`가 백업 제외)|`.decided` + 파일 없는 메타데이터|**고아 메타데이터 정리**|
          |강제 종료 (adopt 후 save 전)|방향·클립 둘 다 없음 + 옮겨진 파일|**고아 파일 정리**|
          |`-wal` 유실|트랜잭션째 사라져 `.unset` + 0컷|(정상 상태다. 클립이 함께 사라진다)|
          |2-8 이전 코드|`.missing`|**레거시. 아래 참고**|

        - **기기에 있던 것은 2-8 이전 코드가 만든 레거시였다.** 그것마저
          "전부 삭제"로 사라졌고, **v0.1은 출시 전이라 외부에 존재하지 않는다.**
          지금 `.missing` 세션은 이 세상에 없다
        - `.corrupted`도 **앱 코드로 만들 수 없다.** `orientationRaw`가
          `private`이고 쓰는 곳 셋이 전부 유효값만 넣는다 — `init`은
          `Orientation?`, `decideOrientation`은 `Orientation`,
          `undoOrientationDecision`은 `nil`
        - **⚠ 2-16 명세를 이 관점에서 다시 봐야 한다 (다음 착수 시 첫 할 일).**
          이 항목의 여러 줄이 "`.missing`/`.corrupted` 복구"를 중심으로 쓰여
          있는데, **실제로 다뤄야 할 것은 위 표의 오른쪽 열이다** — 파일 없는
          메타데이터, 고아 파일, 그리고 `.unset` + 0컷의 취급. 방향 복구는
          도달 불가 상태를 위한 방어 코드에 가깝다
            - `2-12a`의 선행 조건 항목("닫기가 `.missing`에서 막히므로 그것을
              복구하는 것은 2-16뿐이다")도 함께 다시 봐야 한다. 막힐 세션이
              생기지 않는다면 그 의존이 성립하지 않는다
            - **⚠ `.missing` 도달 불가라는 전제에는 단서가 붙는다 — '2-10을
              올바로 만들었을 때'다.** 2-10이 클립 확인 없이 방향을 지우면
              `raw nil + 클립 있음`을 정상 경로가 직접 만든다. 2-10 항목의
              "구현 시 함정 (가)" 참고
            - **⚠ 클립 0개인 `.corrupted` 세션의 최종 처리를 여기서 정한다
              (2-10에서 넘어온 것).** 2-10은 `.decided`만 초기화하고
              `.corrupted`는 건드리지 않기로 **유보**했다 — 검증 수단이 없어
              어느 쪽도 증명되지 않기 때문이다. 이 케이스는 복구 근거(첫 클립
              파일)마저 없으므로 위 `.corrupted` 복구 설계와 **별도로** 봐야
              한다
            - **명세 본문은 아직 고치지 않았다.** 정정 기록만 남긴다
        - **지금 픽스처를 만들지 않는다.** 무엇을 재현할지가 위 재검토로
          바뀔 수 있다. 방법부터 고르면 재현 대상이 방법에 맞춰 휘어진다

### 2-D. 방향 정규화

세션 방향과 어긋나게 찍힌 클립(계열 내 180도)을 녹화 직후 백그라운드에서
교정한다. 근거는 결정 대기 목록 "세션 방향 모델 — 확정"과 B 라운드 측정.

**선행 조건**: 2-8(세션 방향 확정)이 있어야 "어긋남"을 판정할 수 있다.
계열 간 혼재는 2-9에서 녹화 자체가 차단되므로 여기 오는 것은 계열 내
180도 차이뿐이다.

- [ ] **2-17** 방향 대조와 정규화 판정
    - 클립 파일의 `preferredTransform`을 읽어 세션 방향과 대조한다
    - **파일이 유일한 근거다.** 정규화 상태를 SwiftData에 저장하지 않는다 —
      세션 방향과 파일 transform으로 언제든 다시 계산되는 파생값이기 때문이다
    - 실측 transform 표는 CLAUDE.md "실측 preferredTransform" 참고.
      각도→방향 매핑을 상수 테이블로 하드코딩하지 않는다는 제약은 유지한다
- [ ] **2-18** 정규화 실행
    - `prepareForOrientationFix(_:)` + `ClipExporter` 재사용 (1-17 B-3a, 1-21)
    - temp에 쓰고 `FileManager.replaceItemAt`으로 원자적 교체.
      **중간 상태가 존재하지 않아야 한다** — 교체 중 죽어도 파일은 원본이거나
      교정본이거나 둘 중 하나여야 큐 재구성이 성립한다
    - **완료 후 `Clip.duration`을 갱신한다.** 재인코딩이 CFR 정규화를 하면서
      duration이 5980/600(9.966667s)으로 수렴한다. 갱신하지 않으면 세션 총
      길이 표시와 병합 커서 전진이 실제와 어긋난다
- [ ] **2-19** 직렬 큐
    - **런타임 메모리에만 존재한다.** 취소 정책·우선순위·스로틀은 만들지 않는다 —
      클립당 1.77초로 촬영 사이클(10초) 대비 5.6배 여유가 있어 큐가 쌓이지 않는다
    - 카메라 running 중에도 그대로 돌린다. B 라운드에서 비용 차이와 발열 영향이
      없음을 확인했다
    - 앱 시작 시 진행 중 세션의 클립을 순회해 큐를 재구성한다 (2-16과 연동)
    - **⚠ 큐에 들어간 클립이 undo로 사라지면 무엇이 되는가 — 정해야 한다**
      (2-11 닫을 때 발견. **2-D 전체에 "삭제"도 "undo"도 한 번 나오지 않는다**)
        - 정규화는 백그라운드로 돌고 undo는 그 사이에 눌릴 수 있다. 노출 창은
          2-20이 잰 것과 같다 — 마지막 클립 촬영 후 약 1.8초
        - 걱정되는 것 둘: **큐가 사라진 클립을 잡고 있는 것**과, 정규화가
          끝난 뒤 **없는 메타데이터를 갱신하려 드는 것**이다. 후자는 2-18이
          `Clip.duration` 갱신 지점으로 예고돼 있어 실제로 닿는다
        - **2-21이 아니라 여기다.** 2-21은 **정규화 실패** 처리이고 undo는
          실패가 아니라 **대상 소멸**이라 성격이 다르다. 큐를 만드는 자리가
          "큐에 들어간 항목이 사라지면 어떻게 되는가"의 계약을 진다
        - **지금 해법을 정하지 않는다.** 2-D 착수 시 정한다. 여기서는 논점이
          존재한다는 사실만 남긴다
    - **진행 중 세션만 보는 것이 안전한 이유: 닫힌 세션은 정규화가 이미 끝나
      있다.** 2-12a의 닫기는 저장이고, 2-20이 저장에 정규화 완료 대기를
      걸어뒀다. 정규화가 남은 세션은 닫히지 않으므로 **닫힌 세션에 미정규화
      클립이 존재할 수 없다.** 이 전제가 깨지면 그 클립은 큐에서 영영 빠지고
      2-21의 "앱 재실행 시 다시 시도된다"가 거짓이 된다 — **2-12a의 계약을
      바꾸려면 여기를 먼저 봐라**
- [ ] **2-20** 미리보기·저장과의 동기화
    - **미리보기**: 진입을 막지 않는다. 1-13의 `isReadyForDisplay` 첫 프레임
      대기에 "정규화 완료" 조건을 얹는다. **별도 로딩 UI를 만들지 않는다** —
      "합치는 중 로딩을 보여주지 않는다"는 확정 결정을 지킨다
    - 노출 창은 좁다. 마지막 클립 촬영 후 약 1.8초이며, 그 이전 클립들은 촬영
      사이클 동안 이미 처리된다. 화면 전환과 탭에 걸리는 사람 손 시간을 감안하면
      대부분 이미 끝나 있다
    - **저장**: 이미 사용자가 기다리는 구간이므로(30개 2.45초) 정규화 완료를
      기다린 뒤 시작하고 기존 진행 표시에 포함한다
    - **이 대기가 곧 세션 닫기의 선행 조건이다** (2-12a). 닫기 = 저장이므로
      정규화가 남은 세션은 닫히지 않는다. 그 덕분에 "닫는 중" 상태를 스키마에
      두지 않아도 되고, 2-19가 닫힌 세션을 큐 재구성에서 빼는 것이 안전해진다
- [ ] **2-21** 실패 처리
    - **여기는 "정규화가 실패한 것"만 다룬다.** undo로 **대상 클립 자체가
      사라지는 것**은 실패가 아니라 대상 소멸이라 성격이 다르며 **2-19가
      진다.** 둘을 같은 경로로 다루면 사라진 클립에 "정규화 실패" 표시를
      붙이려 들게 된다 — 세션 상세에 그릴 클립이 없다
    - **1회 재시도 후 세션 내 포기.** 2회 시도해도 3.5초다. 에러 코드로 일시적/
      영구적을 구분하려 들지 않는다 — AVFoundation 에러는 원인이 뭉뚱그려져
      나온다(1-5의 `maximumDurationReached`가 정상인데 에러로 오는 사례)
    - **파일은 유지하고 병합에서만 제외한다.** 클립을 버리지 않는다 —
      PRD 3장 "이미 찍은 클립을 나중에 버리는 일은 없다"와 충돌한다.
      뒤집힌 채로 완성본에 넣지도 않는다(1-21에서 못 쓸 수준으로 판정)
    - 세션 상세에서 해당 클립에 표시한다. 조용히 빠지면 사용자가 버그로 인식한다
    - **앱 재실행 시 다시 시도된다.** 상태를 영구 저장하지 않으므로 파일이 실제로
      깨졌다면 매 실행마다 반복된다. 이를 막으려면 영구 실패 플래그가 필요한데,
      그것은 "파생 가능한 값을 저장하지 않는다"는 원칙을 깨므로 **반복을 감수한다.**
      세션당 3.5초이고 백그라운드다

### 게이트 2 — 통과 조건

- [x] 앱을 강제 종료했다 켜도 세션과 클립이 그대로 남아 있다
    - iPhone 12 실기기 확인 (2-A 검증). 클립 4개 저장 → 강제 종료 →
      재실행에서 세션·클립·`order`·파일이 전부 그대로였고, 가운데 컷
      삭제 후의 재정렬(0,1,2)도 재실행 뒤 유지됐다. 회수한 스토어를
      sqlite3 로 직접 읽어 `ZCLIP` 3행과 파일 3개가 일치함을 확인했다
- [x] 세션 삭제 후 디렉터리가 파일시스템에서 사라진다
    - iPhone 12 실기기 확인 (2-12 게이트, 2026-08-21). `세션 삭제` 프로브로
      `8월 19일 세션`(1컷)을 지우고 `[probe] 디렉터리 지움  실제 확인=없음 ✓`
      을 받았다. **뒤쪽이 파일시스템에 다시 물어본 값이다.** 재고 조사에
      `⚠ 고아 세션 디렉터리` 줄이 없는 것으로 한 번 더 확인했고,
      `.cascade`로 클립 메타데이터가 함께 사라지는 것도 같은 실행에서 봤다
- [ ] 권한을 모두 거부한 상태에서 앱이 크래시하지 않는다
- [ ] 녹화 중 전화를 받아도 앱이 죽지 않는다

---

## Phase 3 — UI

여기서 처음으로 화면을 제대로 만든다.

### 3-A. 촬영 화면

- [ ] **3-1** 풀스크린 프리뷰
    - 백그라운드 복귀 시 `startRunning()`에 약 205ms가 소요된다(iPhone 12 실측, 5회 202~211ms, 편차 3ms). 프레임워크 내부 비용이라 단축 불가. 복귀 중 검은 화면 대신 직전 프레임이나 가림막을 보여주는 처리를 여기서 검토한다
- [ ] **3-2** 10초 게이지 (실제 녹화 시간과 동기화)
- [ ] **3-3** 녹화 버튼 (탭 시작 / 10초 자동 정지 / 다시 탭하면 조기 종료, 시각적 피드백)
- [ ] **3-4** 하단 필름스트립 (촬영된 클립 썸네일)
- [ ] **3-5** undo 버튼 (클립 0개일 때 비활성)
    - **⚠ `.disabled`만으로는 비활성이 보이지 않는다** (2-10 게이트 준비 중
      확인). Phase 1 프로브의 `pill(_:)`이 `Text`에 `.foregroundStyle(.white)`를
      명시하고 배경을 직접 칠해서, **`.disabled`가 히트 테스트만 막고 외형은
      그대로다**(`ContentView.swift:780`). 프로브 버튼 넷이 전부 그렇다
        - **녹화 버튼은 이미 다르게 하고 있다.** 2-9가
          `.opacity(isRecordingBlocked ? 0.35 : 1)`을 직접 걸었다
          (`ContentView.swift:564`). undo 버튼도 같은 처리가 필요하다
        - 프로브 pill을 고칠 일은 아니다 — 3-13에서 사라진다. **여기 적는
          이유는 "0개일 때 비활성"이 `.disabled` 한 줄로 끝나지 않는다는
          것**이고, 실제로 그 착각으로 게이트 절차를 한 번 잘못 짰다
- [ ] **3-6** 방향 안내 표시 ("가로로 돌려주세요")
    - **신규 구현이 아니라 개선이다.** 2-9가 최소 안내 문구까지 넣으므로
      (2-9 (D)) 이 항목이 착수할 때는 이미 읽을 수 있는 한 줄이 화면에
      있다. 여기서 하는 것은 **카피·배치·등장 방식을 다듬는 것**이다
        - 항목 성격이 바뀐 것을 적어두는 이유는, "3-6이 안내를 만든다"로
          읽으면 2-9가 차단만 넣고 끝내게 되기 때문이다. 그러면 두 태스크
          사이에 **버튼이 죽었는데 이유가 없는 구간**이 생긴다
    - **어긋났을 때만 뜨는 안내만 담당한다.**
      **세션 방향 상시 표시는 이 항목이 아니다** — 방향 불일치와 무관하게
      **홈 화면에** 늘 보여야 하고 그것은 세션 정보 UI(3-8·3-9) 몫이다.
      여기서 따로 그리면 **같은 정보가 두 군데 생긴다**
    - **촬영 화면에는 세션 방향 상시 표시를 두지 않는다.** 폰을 들고
      있으므로 **사용자가 몸으로 안다.** 어긋날 때만 알려주면 된다
        - 촬영 화면은 **프리뷰가 주인공**이다. 상시 배지는 프리뷰를 가리면서
          정보를 더하지 않는다
        - PRD 4.4 결정 최소화 원칙과도 맞는다 — **늘 떠 있으면 사용자가
          신경 쓰게 된다.** 어긋났을 때만 뜨는 것이 곧 "신경 쓸 일이 없으면
          아무것도 보이지 않는다"이다
        - **3-A에 상시 표시 항목을 신설하지 않는다.** 이 결정이 그 자리를
          비워 두는 것이지 빠뜨린 것이 아니다
    - **세 항목이 한 화면씩 나눠 만든다. 경계를 흐리지 마라.** 사용자가 보는
      최종 형태는 이렇다:
        - **홈 화면**에 세션 방향이 늘 있고 (3-8 목록 · 3-9 배너)
        - **촬영 화면**에서는 평소 아무것도 없다가,
          **어긋나면 안내가 뜬다** (2-9가 넣고 3-6이 다듬는다)
- [ ] **3-7** 가로 모드일 때의 컨트롤 배치
    - **세션 방향 기준이다. 기기 방향이 아니다.** 세션이 가로면 프리뷰
      비율이 1920×1080이라 화면에 여백이 생기고, 그 여백을 전제로 한 구성을
      하나 만든다. 세션 방향은 첫 클립이 정하고 중간에 바뀌지 않으므로
      (F-03) **세션이 정해진 순간 확정되는 정적 구성**이다
    - **0-6과 충돌하지 않는다.** 0-6이 "세션 방향에 맞는 고정 구성 하나만"
      이라고 쓴 것 자체가 **세션 방향별로 구성이 다르다는 전제**다. 0-6이
      금지한 것은 **기기를 돌릴 때** 재배치되는 쪽이다
    - **3-7a와 축이 다르다.** 이쪽은 세션 방향 기준 정적 구성이고, 3-7a는
      기기 방향 기준 회전이다. 한 화면에 둘 다 걸릴 수 있다 — 가로 세션의
      고정 구성 위에서 기기를 세로로 들면 3-7a가 라벨을 돌린다
- [ ] **3-7a** 가로 방향에서 아이콘·텍스트 회전
    - 기기를 가로로 들었을 때 **아이콘과 텍스트 라벨을 제자리에서 90도
      회전**시킨다. **버튼 위치는 옮기지 않는다**
    - iOS 기본 카메라·인스타그램 스토리 등이 쓰는 방식이다. **카메라 UI는
      엄지가 닿는 위치가 중요해서** 레이아웃을 돌리면 셔터가 반대편으로
      가버린다. 손은 그대로 두고 글자만 읽히게 하는 것이 목적이다
    - **⚠ 확정 사항 "반응형 재배치는 만들지 않는다"와 충돌하지 않는다.**
      그 결정(0-6, CLAUDE.md "기기 회전에 따라 화면 구성이 바뀌는 대응은
      하지 않는다")이 금지한 것은 **재배치**이고, **회전은 명시된 바가
      없었다.** 둘은 다른 것이다 — 재배치는 요소가 어디 있는지를 바꾸고,
      회전은 같은 자리에서 무엇을 향하는지만 바꾼다. 이 구분을 흐리면
      3-7과 이 항목이 같은 것으로 읽혀 한쪽이 조용히 사라진다
    - **3-7과 축이 다르다.** 3-7은 **세션 방향** 기준의 정적 구성이고,
      이 항목은 **기기 방향** 기준의 회전이다. 3-7의 구성은 세션이 정해진
      순간 확정되고 기기를 돌려도 그대로이며, 그 위에서 라벨만 이 항목이
      돌린다. 둘은 겹치지 않고 함께 걸린다
    - **판정은 `UIDevice.current.orientation`을 쓴다.** 앱이 이미
      `CaptureSessionController.deviceOrientation`으로 들고 있고,
      `UIDeviceOrientation.isPortrait` / `.isLandscape`가 UIKit 제공이라
      각도→방향 테이블을 새로 만들지 않는다(2-9 조사에서 확인).
      `videoRotationAngleForHorizonLevelCapture`는 쓰지 않는다 — 그쪽은
      촬영 파이프라인용이고 UI 회전과 목적이 다르다
        - **`.faceUp` / `.faceDown` / `.unknown`에서 무엇을 보여줄지 정해야
          한다.** 이 셋은 세로도 가로도 아니라 판정이 빈다. 폰을 책상에
          놓으면 바로 이 상태가 된다
    - **녹화 버튼은 대상이 아니다.** 원형이라 회전이 무의미하다. 대상은
      **텍스트 라벨과 방향성 있는 아이콘**뿐이며, 회전할 것이 실제로
      몇 개인지 3-3~3-6이 끝난 뒤에 세는 것이 맞다
    - **지금 디버그 버튼들에 적용하지 않는다.** 그것들은 3-13에서 걷어낼
      대상이라 적용하면 버리는 코드에 일을 하는 셈이다
    - 번호를 미루지 않고 하위 번호로 끼운다. Tasks.md는 실행 순서 문서이고
      **번호 순서가 곧 실행 순서**다 — 2-12a와 같은 이유다

### 3-B. 나머지 화면

- [ ] **3-8** 홈: 세션 목록 (최신순), 커버 썸네일, 클립 수, 총 길이
    - **⚠ 세션 삭제가 실패하면 목록에서 잠깐 사라진다** (2-12 알려진 동작).
      `@Query`의 첫 조회가 삭제된 상태를 그대로 보이고 두 번째부터 복원된다.
      **사용자는 "삭제 실패 에러를 보면서 동시에 목록에서 사라진 화면"을
      본다.** 버그로 보고 고치려 들기 전에 2-12 항목을 먼저 읽어라 —
      되돌리지 않기로 한 결정이고 근거가 거기 있다
    - **닫힌 세션과 진행 중 세션을 구분해 보여야 한다** (2-12a). 닫힌 세션은
      일반 카드로 남고 완료된 것으로 읽혀야 한다. 카드 종류는 둘뿐이다 —
      닫기 = 저장이라 "저장했는데 안 닫힘" 같은 중간 상태가 없다
    - **세션 방향 상시 표시가 여기 몫이다** (3-6과의 경계). 방향 불일치와
      무관하게 늘 보이는 정보다. 어긋났을 때만 뜨는 안내는 2-9가 넣고 3-6이
      다듬는다 — 양쪽이 그리면 같은 정보가 두 군데 생긴다
- [ ] **3-9** 홈: 새 세션 버튼, 진행 중 세션 배너
    - **배너는 열린 세션에서만.** 닫힌 세션은 2-7의 이어가기 대상이 아니다
    - **배너에 세션 방향이 보인다** (3-6과의 경계). 이어갈 세션이 세로인지
      가로인지는 촬영에 들어가기 전에 알아야 하는 정보다. 3-6은 어긋났을
      때만 뜨는 안내이지 상시 표시가 아니다
- [ ] **3-10** 세션 상세: 클립 그리드
    - 가로 세션 = 1열, 세로 세션 = 3열 (PRD 4.2)
    - **닫힌 세션의 상세는 읽기 전용이다** (2-12a). 촬영 진입과 클립 삭제가
      막히고 미리보기·저장·세션 삭제만 남는다
- [ ] **3-11** 세션 상세: 클립 개별 삭제
    - **닫힌 세션에서는 비활성.** 닫은 뒤 클립이 빠지면 이미 저장된 완성본과
      세션 내용이 어긋난다
- [ ] **3-12** 미리보기: 재생 화면, 로딩 없이 즉시 시작
- [ ] **3-13** 저장 버튼 + 완료 피드백
    - **이 버튼이 곧 세션 닫기다** (2-12a). 저장 버튼과 닫기 버튼을 따로
      만들지 않는다. 저장이 성공하면 세션이 닫히고, 실패하면 열린 채로 남아
      3-15의 재시도로 이어진다
    - Phase 1 실험 버튼(재인코딩 1개 / 방향 교정 / 이동·복사 토글 / 전체 비교 / 미리보기)을 여기서 제거한다. Phase 2까지는 정규화 검증 수단으로 계속 쓴다
    - **⚠ 저장 계층 프로브 여섯도 여기서 제거한다** (2-11 닫을 때 발견 —
      위 목록에 없어 처분이 미명시였다). `세션 시작` · `클립 저장` ·
      `컷 삭제` · `전부 삭제` · `가운데 삭제` · `재고`, 그리고 그것들이
      부르는 `ContentView`의 `private` 함수들
      (`deleteLastClipOfActiveSession` · `deleteMiddleClipOfActiveSession` ·
      `saveLastClipToActiveSession`)과 `activeSessionID` · `probeNote` 상태
        - **`전부 삭제`는 특히 남기면 안 된다.** `ClipStore`를 거치지 않고
          `modelContext.delete(session)`으로 모든 세션을 지운다. 실사용 화면에
          남으면 사용자가 전체 데이터를 한 번에 날릴 수 있다
        - **3-5가 `deleteLastClipOfActiveSession()`을 그대로 부를 수 없다.**
          `probeNote` 문자열을 쓰고, `activeSessionID`에 묶여 있고,
          `StoreProbeLog` 호출이 섞여 있다. **본체는 세 줄이다** —
          `orderedClips.last` → `ClipStore.delete` → 결과 반영.
          **옮길 로직이 아니라 다시 쓸 세 줄이다.** 3-5가 자기 자리에서 쓴다
        - `StoreProbeLog`는 파일 전체가 `#if DEBUG`라 Release 이진에 없다
          (2-A 확인). 호출부가 사라지면 파일도 함께 지운다
    - **`CaptureTrace`를 파일 전체 `#if DEBUG`로 감쌀지 여기서 판단한다.**
      지금은 `isEnabled` 한 줄만 감싸고 있어 동작은 꺼지지만 타입·메서드·로그
      문자열이 Release 이진에 남는다(심볼 38건, 2-A에서 이진 확인).
      `StoreProbeLog`는 파일 전체를 감싸 0건이다. 남길지 감쌀지 지울지를
      프로브·측정 코드 정리와 함께 정한다. 근거는 CLAUDE.md
      "계측 코드와 릴리스 빌드" 참고
    - **`SessionStore`의 `[session]` 로그는 선반영됐다 (2-8 후속).**
      "후보가 여럿" 블록이 세션 id와 사용자 입력 제목(`displayTitle`)을
      찍고 있었고 `#if DEBUG`가 없어 Release에서 돌았다. `StoreProbeLog`를
      감싼 근거와 같아서 **여기까지 미루지 않고 그때 감쌌다** — 한 줄짜리라
      미루면 잊힌다
        - **"제외 N개" 줄은 감싸지 않았다.** 개수만 찍고 세션을 특정하지
          않아 성격이 다르다. Release에 남아 있다
        - 이진으로 확인했다: Release에서 `이어갈 수 있는 세션이` 0건,
          `남겨둠` 0건, `[probe]` 0건. `이어가기 후보에서 제외`만 1건 남는다
        - **Debug 빌드는 코드가 `Mellow.app/Mellow`가 아니라
          `Mellow.debug.dylib`에 들어간다.** 2-A 방식으로 이진을 셀 때 본체만
          보면 Debug가 0건으로 나와 잘못 읽는다. 두 파일을 갈라서 봐야 한다
        - 하네스 빌드 명령에 `-D DEBUG`가 추가됐다. 없으면 6군이 확인하려는
          로그가 안 찍힌다

### 3-C. 상태 처리

- [ ] **3-14** 빈 상태 (세션 없음 / 클립 없음)
- [ ] **3-15** 에러 상태 (권한 거부, 저장 실패)
    - **⚠ 세션 삭제 실패는 에러 문구와 화면이 모순되는 순간을 만든다**
      (2-12 알려진 동작). "지우지 못했습니다"를 띄우는 그 순간 목록에서는
      세션이 사라져 있다 — `@Query`의 첫 조회가 거짓말하기 때문이다.
      **문구를 쓸 때 이 모순을 알고 써라.** 3-8에도 같은 참조가 있다
    - **저장 실패 시 사용자 주도 재시도.** 사용자가 공간을 확보한 뒤 다시
      시도할 수 있어야 한다. 되돌려둔 파일이 `temporaryDirectory`에 있어
      시스템이 비웠을 수 있으므로, **재시도 불가 상태도 함께 다뤄야 한다**
    - 2-2의 스토어 열기 실패 화면(`StoreUnavailableView`)도 여기서 제대로 만든다
    - **에러 타입에 `LocalizedError`를 붙일지 여기서 정한다** (2-7 리뷰).
      `SessionStartError` / `SessionFileError` / `ClipSaveError` 가
      `CustomStringConvertible` 만 채택해서, `.alert(_:error:)` 나
      `localizedDescription` 을 타면 한국어 문구 대신
      `The operation couldn't be completed` 가 나온다. 지금 붙이지 않는 이유는
      **화면 없이 문구부터 짓게 되기 때문이다** — 소비자가 2-14·3-15 다
- [ ] **3-16** 앱 아이콘, 런치 스크린
- [ ] **3-17** tmp 잔여 녹화본 정리
    - **3-15가 먼저다.** 재시도를 어떻게 노출하느냐가 이 태스크의 기준을
      정한다. 아래 "정리 기준은 나이뿐" 참고 — 3-15보다 먼저 정할 수 없다
    - `tmp/mellow-<UUID>.mov`가 누적된다. 실기기에서 **129개 3.3GB** 확인
      (iPhone 12, 64GB의 5.2%. 2-A)
    - **2-16에 넣지 않는다.** 2-16의 판정 기준은 "메타데이터에 없으면 고아"인데,
      tmp 파일은 스토어에도 세션 디렉터리에도 등록돼 있지 않아 애초에 대조
      대상이 아니다. 그 기준을 tmp에 적용하면 **저장 실패로 되돌린 파일이 전부
      고아로 판정되어** 2-5의 재시도 계약이 깨진다 — 그 파일은 정의상
      메타데이터에 없는 것이 정상이다
    - **정리 기준은 파일 나이(mtime)뿐이다.** 잔여물과 저장 실패분을 구분할
      수단이 없다. `SessionFileStore.release`가 파일을 원래 `sourceURL`로
      되돌리는데 그 경로·이름·크기·duration이 잔여물과 완전히 같고,
      "저장을 시도했다가 실패했다"는 사실이 파일에도 스토어에도 기록되지 않는다.
      `recordedURLs`는 앱 재실행 시 사라진다
    - **따라서 나이 기준을 정하는 것이 곧 재시도 창의 상한을 정하는 것이다.**
      3-15가 사용자 주도 재시도를 어떻게 노출하는지에 따라 창이 며칠이 될 수도
      있다
    - 관찰된 사실:
        - **재설치는 tmp를 비우지 않는다.** 컨테이너 UUID가 6번 바뀌는 동안
          8월 10일·12일 파일이 mtime 그대로 살아남았다 — 경로만 바뀌고 내용은
          그대로 옮겨진다
        - **iOS도 4~6일간 한 번도 비우지 않았다.** "시스템이 알아서 치운다"는
          통념은 이 데이터와 맞지 않는다. 저장 공간 압박 시 비우는 동작이
          있는지는 확인하지 못했다(64GB에 3.3GB는 압박 상황이 아니다)
        - 익스포트·정규화 임시 디렉터리(`mellow-export-*`,
          `mellow-normalize-*`)는 0개다. 그 경로는 새지 않는다
    - **우선순위는 낮다.** 129개는 Phase 1 실험 특유의 축적이다. 3-3 이후
      촬영 즉시 저장이 되면 잔여물은 거의 생기지 않고, 남는 것은 저장 실패분과
      크래시 잔여물뿐이라 양이 다르다
    - 2-14가 "공간을 비우세요"를 말하기 전에 이 정리를 시도할지 판단한다

### 게이트 3 — 통과 조건

- [ ] 세로 세션과 가로 세션을 각각 처음부터 끝까지 완료할 수 있다
- [ ] 편집 화면을 한 번도 열지 않고 완성본이 나온다
- [ ] PRD 11장의 완료 판정 항목이 전부 체크된다

---

## Phase 4 — 실사용 검증

- [ ] **4-1** 실제 나들이 또는 여행에서 한 세션을 끝까지 사용
- [ ] **4-2** PRD 12장의 확인 항목 6개에 답을 적는다
- [ ] **4-3** 결과를 바탕으로 v0.2 우선순위 결정

**이 단계를 건너뛰고 v0.2 기능(자막·BGM)을 시작하지 않는다.**

---

## 결정 대기 목록

구현 중 답이 나오면 PRD에 반영한다.

|항목|언제 결정되나|
|---|---|
|전화 수신 시 부분 녹화분 처리|2-15 구현 시|
|세션 방향 모델 및 방향 혼재 허용 여부|**확정 (1-21).** (c) 촬영 직후 정규화, 데이터 모델은 2값|
|전면 카메라 지원 여부|Phase 1 스펙 통일 검증 후|
|세션 닫기의 의미|**확정 (2-6 착수 전).** 닫기 = 저장, 성공 시에만 `isClosed = true`. 2-12a|

**세션 닫기 계약 — 확정 (2-6 착수 전)**

**결론: 닫기 = 사진 앱 저장을 시작하되, 성공한 뒤에만 `isClosed = true`.
닫힌 세션은 읽기 전용이다.**

발단은 **`isClosed`를 `true`로 만드는 코드도 태스크도 없다**는 것이었다.
`Session.swift`에 필드와 `inProgress()` predicate만 있고 쓰는 쪽이 없었으며,
2-6~2-12에도 3-8~3-13에도 닫기 태스크가 없었다. PRD에서는 4.1 화면표의
세션 상세 요소로 한 번 언급될 뿐 수용 기준이 없었다.

배치보다 **계약이 미정이라는 것**이 먼저였다. CLAUDE.md 제품 원칙은
"세션을 닫으면 완성본이 나온다"인데 PRD 메인 플로우는 촬영 → 상세 →
미리보기 → 저장이고 닫기가 그 흐름에 없어, 닫기가 상태 플래그인지 완성본
트리거인지 문서로는 정해지지 않았다.

**탈락한 안과 이유는 2-12a 본문 참고.** 채택안이 실패 처리("성공한 뒤에만")를
계약 안에 넣는다는 점이 결정적이었다.

**부수 효과: 2-19·2-21 충돌이 사라졌다.** 검토 중 발견된 것은 이랬다 —
2-19는 앱 시작 시 **진행 중 세션만** 순회해 정규화 큐를 재구성하는데,
정규화가 안 끝난 세션을 닫아버리면 그 클립이 다음 실행에서 큐에 들어가지
못해 2-21의 "앱 재실행 시 다시 시도된다"가 거짓이 된다. 완성본에서 영영
빠진 클립이 남는다.

채택한 계약이 이 경로를 막는다. 2-20이 이미 저장에 정규화 완료 대기를
걸어뒀고 닫기가 곧 저장이므로, **정규화가 남은 세션은 애초에 닫히지 않는다.**
그래서 `isClosed` 외에 "닫는 중" 상태를 스키마에 추가할 필요가 없다.

**논의 경위** (같은 실수를 반복하지 않기 위해 남긴다)

1. **"닫을 수단이 없으면 2-7의 '새로 만든다' 분기가 두 번째부터 검증
   불가"** — **틀렸다.** 세션을 삭제하면(2-12, 또는 프로브의 "전부 삭제")
   진행 중 세션이 비어 그 분기를 몇 번이든 탈 수 있다. 닫기로만 도달하는
   상태는 **"완료된 세션과 진행 중 세션이 목록에 동시에 있는 것"**이고,
   그건 3-8에서 처음 필요하다. 검증 가능성은 닫기를 Phase 2로 당길 근거가
   되지 못한다
2. **"close가 없으면 2-16의 스코프가 무한히 자란다"** — **과장이었다.**
   2-16에서 "진행 중 세션"으로 제한된 것은 **transform 대조 항목 하나**다.
   고아 파일 정리와 `.missing`/`.corrupted` 방향 복구는 닫힘 여부와 무관하게
   전체가 대상이므로 닫아도 줄지 않는다. 정확한 서술은 **"2-16의 transform
   대조 대상과 2-19의 시작 시 큐 재구성 후보 집합이 누적 세션 수에 비례해
   커진다"**이다. 결론(닫기가 Phase 2 안에 있어야 한다)은 유지되지만 근거는
   좁혀서 말해야 한다
3. **2-D 큐가 자란다는 표현** — 큐 자체는 메모리에 있고 직렬 처리되므로
   누적되지 않는다. 커지는 것은 큐에 넣을지 판단하려고 훑는 후보 집합이다
4. **확정** — 위 "결론" 참고. 실행 태스크는 2-12a

**세션 방향 모델 — 확정 (1-21 이후)**

**결론: (c) 촬영 직후 단일 클립 정규화. 데이터 모델은 2값 유지.**

**선택지 (b) 세션 전체 재인코딩 — 탈락.** 방향 교정 익스포트가 실시간의 0.229배이므로 클립 20개(200초) 세션이면 약 46초가 걸린다. PRD F-06의 "클립 20개 3초 이내"를 15배 초과한다.
(1-17에서 Mac 값으로 한 번 탈락시켰다가 1-19의 96ms를 보고 철회했으나, 그 96ms는 videoComposition이 없는 단순 재인코딩이었다. 1-21의 실기기 교정 실측으로 다시 탈락이며 이번 근거는 확정적이다.)

**선택지 (c) 촬영 직후 정규화 — 채택.** 세션 방향과 어긋난 클립을 녹화 직후 백그라운드에서 방향 교정 재인코딩해 세션 방향에 맞춘다. 익스포트 경로는 passthrough를 유지한다.

- 비용: 10초 클립 1개당 약 1.77초 (B 라운드 단독 실측). 확정 당시 근거는 2.3초였으나 그것은 배치값이었고, 단독 정규화는 그보다 빠르다. 사용자가 기다리는 구간이 아니다
- 화질: 비트레이트 97.0%로 사실상 유지
- "한 세션 안 클립은 전부 동일 스펙" 불변식을 깨지 않는다. 저장 시점에 이미 동일해지므로 Phase 2 파일 모델이 그대로 간다

**데이터 모델은 PRD 7장 원안인 2값을 유지한다.** `Orientation: portrait | landscape`
2값이 무너졌던 이유는 가로L/가로R 혼재 시 한쪽이 뒤집힌다는 것이었는데, 정규화가 이를 흡수하므로 2값이 다시 성립한다. 사용자에게는 "가로로 돌려주세요"만 표시되고 좌우는 앱이 맞춘다. 세로/거꾸로도 같은 방식이다.

**계열 간(세로↔가로) 혼재는 계속 차단한다.** 표시 규격이 달라 정규화 시 필러박스가 불가피하다. 계열 내 180도 차이는 같은 캔버스를 정확히 채우므로 무손실이지만 계열 간은 그렇지 않다.

**Phase 2로 넘기는 미결: 전부 해소됨.** 정규화는 2-D(2-17~2-21)로
실행 태스크가 됐다.

**해소됨 (B 라운드)**

- ~~정규화 진행 중 다음 촬영을 시작할 때 카메라와 인코더가 동시에 도는 상황의 발열·성능 영향~~
  → 영향 없음. 카메라 running 조건에서 비용이 동일하고(1763ms vs stopped 1791ms) `thermalState`는 전 구간 `nominal`을 유지한다
- ~~여러 클립이 연달아 어긋날 때의 큐 처리~~
  → 클립당 1.77초로 촬영 사이클(10초)보다 훨씬 짧아 큐가 쌓이지 않는다. 단순 직렬 처리로 충분하며 취소 정책·우선순위·스로틀이 불필요하다
- ~~배치 처리로 비용을 줄일 수 있는가~~
  → 고정비가 거의 없어 이득이 없다. 단독이 배치보다 오히려 빠르다

**해소됨 (설계 확정, 2-D로 태스크화)**

- ~~정규화가 끝나기 전에 미리보기·저장을 시도할 때의 동작~~
  → 2-20에서 정의. 미리보기는 첫 프레임 대기에 조건을 얹고, 저장은 기존
    진행 표시에 포함한다. 별도 로딩 UI를 만들지 않는다
- ~~정규화 실패 시 처리~~
  → 2-21에서 정의. 1회 재시도 후 포기, 파일 유지·병합 제외, 사용자에게 표시

**논의 경위** (같은 실수를 반복하지 않기 위해 남긴다)

1. **2값(portrait \| landscape)** — PRD 7장의 원안. transform 실측에서 가로L(0)과 가로R(180)이 서로 다른 행렬임이 드러나 무너졌다. 2값으로는 둘을 같은 "landscape"로 묶는데, 그러면 한쪽이 거꾸로 재생된다
2. **3값(portrait \| landscapeLeft \| landscapeRight)** — 위 문제를 막으려 제안했다가 **철회**했다. 이유: 가로 촬영 시 좌우를 의식해 고르는 사용자와 잡히는 대로 눕히는 사용자가 둘 다 존재한다. 3값 모델은 후자에게 "이미 가로로 들고 있는데 녹화가 안 되는" 설명 불가능한 상태를 만든다
3. **거꾸로(270) 재평가** — "거꾸로 촬영을 의도하는 시나리오가 없다"는 전제가 틀렸다. 아이폰은 카메라가 본체 한쪽 끝에 있어 낮은 앵글을 잡을 때 뒤집으면 촬영이 쉽다. 실제로 쓰는 방법이므로 차단 대상으로 볼 수 없다
4. **"계열 간이 계열 내보다 어렵다"는 층위 구분** — 세로↔가로는 표시 규격이 달라 레터박스가 생기고, 계열 안의 180도 차이는 규격이 같으니 더 쉬울 것으로 봤다. **틀렸다.** 실측에서 세 조합 전부 픽셀 손실이 없었다. 세로와 가로의 표시 규격이 정확히 90도 회전 관계라 캔버스를 빈틈없이 채운다
5. **혼재 허용 배제** — 수치상 무손상이라는 것과 결과물로 쓸 만하다는 것은 다른 문제였다. 실기기 관찰에서 혼재 결과물이 못 쓸 수준으로 판정되어, 혼재를 그대로 허용하는 선택지는 배제됐다
6. **Mac 값으로 실기기를 추정한 것** — 1-17에서 Mac M2 재인코딩 비용(실시간의 0.150배)을 실기기로 환산해 (b)를 탈락시켰다. **틀렸다.** 1-19 실측에서 실기기가 M2보다 16배 빨랐다. 플랫폼 간 비율 환산이 성립하지 않는 경로가 있다는 것을 여기서 배웠다
7. **측정 대상이 실제 대상과 달랐던 것** — 6번을 근거로 (b) 탈락을 철회했으나, 96ms는 `videoComposition`이 없는 **단순 재인코딩**이었고 실제로 필요한 것은 **방향 교정**이었다. 1-21에서 재보니 클립당 2281ms로 24배 느렸고 (b)는 다시 탈락했다. 대체값으로 판단할 때는 그 값이 진짜 같은 작업을 잰 것인지 먼저 따져야 한다
    - **같은 실수가 한 번 더 있었다.** 그 2281ms 자체가 4클립 배치를 나눈 값이라 단독 정규화 비용이 아니었다. B 라운드에서 단독으로 재니 1.77초였다. (b) 탈락 결론은 그대로지만, **"클립당"이라는 표현이 배치를 나눈 값인지 단독으로 잰 값인지 구분해야 한다**
8. **확정** — (c) 촬영 직후 정규화. 위 "확정" 절 참고

- PRD 7장의 2값 모델(portrait \| landscape)로는 가로L/가로R 혼재 시 한쪽이 거꾸로 재생된다. 세로/거꾸로도 동일한 문제
- **실측(iPhone 12, AVMutableComposition)**
    - 컴포지션 비디오 트랙은 `preferredTransform`을 하나만 갖는다. 세그먼트별로 갖지 않는다
    - transform 미지정 시 identity로 남는다. 첫 클립 값을 물려받지 않는다
    - `naturalSize`는 transform과 무관하게 원본 해상도(1920×1080)를 따른다
    - 방향이 섞인 세 조합 전부에서 픽셀 손실 없음. 레터박스·크롭·늘어남 없이 회전만 발생
        - 계열 간(세로90 + 가로L0): 90° 회전
        - 계열 내(세로90 + 거꾸로270): 180° 회전
        - 계열 내(가로L0 + 가로R180): 180° 회전
    - 즉 잘못된 클립은 손상되는 것이 아니라 돌아가 있을 뿐이다
- **실기기 재생 관찰(가로L/가로R 혼재 4클립)**: 180도 뒤집힌 클립이 픽셀 손실 없이 회전만 발생하나, 사용자 관점에서 못 쓸 수준이다. 방향 혼재를 그대로 허용하는 선택지는 배제된다. 남은 선택지는 (a) 방향 고정으로 막기 (b) 섞인 세션만 재인코딩 (c) 기타
- **재인코딩 경로별 실기기 실측** (iPhone 12). 세 값의 차이가 결정을 갈랐다

  |경로|클립당|실시간 대비|비고|
  |---|---|---|---|
  |passthrough|—|0.0029배 상당|재인코딩 없음. 완성본 저장에 쓴다|
  |단순 재인코딩 (`Preset1920x1080`)|96ms|0.010배|하드웨어 인코더 경로|
  |**방향 교정** (`+ AVVideoComposition`)|**2281ms**|**0.229배**|픽셀을 다시 그린다. 위의 24배. **4클립 배치값** — 단독은 1770ms(18배), B 라운드 참고|

- 기술적으로 방향 교정 자체는 성립한다(1-17 B-3, 1-21). 계열 내 180도는 두 계열 모두 여백 없이 정방향으로 선다
- 비트레이트는 실기기에서 **97.0%** 로 사실상 유지된다(1-21). Mac의 68.2%는 M2의 preset 동작이었다. `AVAssetWriter`가 필요 없어졌다
- 실측 transform 행렬은 CLAUDE.md "실측 preferredTransform" 참고

**사용자 맥락 (확인된 사실)**

- 거꾸로 촬영에는 실용적 이유가 있다. 아이폰은 카메라가 본체 한쪽 끝에 있어 낮은 앵글을 잡을 때 뒤집으면 촬영이 쉽다
- 가로 촬영 시 좌우를 의식해서 고르는 사용자와 잡히는 대로 눕히는 사용자가 둘 다 존재한다
- 따라서 방향 혼재를 단순히 차단하는 것은 사용자를 배제하는 결정이다

**해소됨**

|항목|결론|
|---|---|
|iOS 17 방향 API의 정확한 형태|`AVCaptureDevice.RotationCoordinator` + `AVCaptureConnection.videoRotationAngle`. 실측 각도는 1-9 참고 (1-2에서 확인)|
|`portraitUpsideDown` 회전각|capture 270 / preview 90. 네 방향 실측 완료 (1-2에서 확인)|
|녹화 버튼 방식 (누르고 있기 vs 탭)|**탭 방식으로 확정.** 10초는 손가락으로 버티기에 긴 시간이다. 여행 중 한 손 촬영이면 더욱 그렇고 흔들림도 늘어난다. 누르고 있는 방식은 "언제 뗄지"를 계속 신경 쓰게 만들어 결정 최소화 원칙에 어긋난다. 탭 방식은 누르고 잊으면 되고 10초 자동 정지가 종료를 책임진다. Phase 4 실사용 전이지만 Phase 1 검증 중 반복 촬영에서 명확히 드러나 앞당겨 확정한다|