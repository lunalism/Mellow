멜로우는 10초 클립을 이어붙여 브이로그를 만드는 iOS 앱이다. 여행이나 나들이 중에 10초짜리 컷을 툭툭 찍어두면, 돌아와서 편집 앱을 거치지 않고도 한 편의 영상이 완성되어 있다.

이 문서는 v0.1에서 무엇을 만들고 무엇을 만들지 않을지를 정의한다. 완료 판정 기준은 10장에, 의도적으로 제외한 범위는 9장에 있다.

---

## 1. 배경

### 해결하려는 문제

여행에서 영상은 찍는데, 편집을 안 해서 사진첩에만 쌓여 있다. 편집 앱은 진입 장벽이 높고(타임라인, 트리밍, 트랜지션), 한 번 열면 30분이 사라진다. 그래서 대부분 "나중에 해야지" 하고 끝난다.

### 타깃 사용자

- 여행·나들이를 기록하고 싶지만 영상 편집은 못 하거나 귀찮은 사람
- 편집 앱을 깔아봤지만 몇 번 쓰고 안 쓰게 된 사람
- 기준: **편집 화면을 한 번도 열지 않아도 결과물이 나와야 만족한다**

### 핵심 약속

> 편집 화면을 열지 않아도 결과물이 완성된다.

이 문장이 모든 의사결정의 기준이다. 기능을 추가할지 고민될 때 "이게 사용자가 내려야 할 결정을 늘리는가?"를 먼저 묻는다. 늘린다면 기본적으로 보류한다.

---

## 2. 용어

|용어|정의|
|---|---|
|**세션**|최상위 단위. 여행·나들이 같은 이벤트 하나. 클립들의 컨테이너|
|**클립**|10초짜리 영상 파일 하나. 세션 안에서 순서를 가짐|
|**세션 방향**|세로 또는 가로. 세션당 하나로 고정되며 중간에 바뀌지 않음|
|**완성본**|세션의 클립들을 순서대로 이어붙인 결과 영상. v0.2의 분할 화면 배치(9.2)가 이 정의를 넓힌다|
|**진행 중 세션**|아직 닫히지 않아 클립을 추가할 수 있는 세션|

---

## 3. 세션 방향

멜로우의 촬영·병합 구조를 지탱하는 전제는 **한 세션 안의 모든 클립이 완전히 동일한 스펙**이라는 것이다. 스펙이 같아야 재인코딩 없이 이어붙일 수 있고, 그래야 "즉시 저장"이 성립한다.

그래서 방향은 세션 단위로 고정한다.

||세로 세션|가로 세션|
|---|---|---|
|해상도|1080 × 1920|1920 × 1080|
|공통|30fps / H.264 / AAC|30fps / H.264 / AAC|

### 방향은 첫 클립이 정한다

세션을 만들 때 방향을 묻지 않는다. 사용자가 평소처럼 기기를 들고 첫 컷을 찍으면, **그때의 방향이 세션 방향으로 확정**된다.

```
세션 생성 → 방향 미정
   ↓
첫 촬영 → 그 방향으로 확정
   ↓
이후 다른 방향으로 들면 → 녹화 불가 + 안내 표시
```

이후 클립은 세션 방향과 같을 때만 촬영할 수 있다. 어긋난 상태에서는 녹화 버튼이 비활성화되고 "가로로 돌려주세요" 같은 안내가 뜬다. **이미 찍은 클립을 나중에 버리는 일은 없다.** 애초에 찍히지 않게 막는다.

첫 클립을 undo해서 클립이 0개가 되면 방향은 미정 상태로 되돌아간다. 잘못된 방향으로 첫 컷을 찍었을 때의 탈출구다.

---

## 4. 화면과 플로우

### 4.1 화면 구성

```
홈 ──▶ 촬영 ──▶ (뒤로) ──▶ 세션 상세 ──▶ 미리보기 & 저장
 │                              ▲
 └──────────────────────────────┘
```

|화면|목적|주요 요소|
|---|---|---|
|**홈**|세션 목록|세션 카드(커버·제목·클립 수·총 길이), 새 세션 버튼, 진행 중 세션 배너|
|**촬영**|클립 쌓기|풀스크린 프리뷰, 10초 게이지, 녹화 버튼, 하단 필름스트립, undo, 방향 안내|
|**세션 상세**|세션 관리|클립 그리드, 클립 개별 삭제, 미리보기 & 저장 진입|
|**미리보기 & 저장**|결과 확인|이어붙인 영상 재생, 사진 앱 저장 버튼(= 세션 닫기)|

**세션 닫기는 별개 동작이 아니다.** 사진 앱 저장이 곧 닫기이며, **저장에
성공한 뒤에만** 세션이 닫힌다. 저장 버튼과 닫기 버튼을 따로 두지 않는다 —
사용자가 내리는 결정이 하나 늘어나고(4.4), "저장했는데 안 닫힘" 같은 중간
상태가 생겨 홈의 세션 카드 종류가 늘어난다. 저장이 실패하면 세션은 열린 채로
남아 다시 시도할 수 있다.

**닫힌 세션은 읽기 전용이다.** 클립 추가·삭제가 막힌다. 이미 저장된 완성본과
세션 내용이 어긋나지 않게 하기 위해서다. 세션 삭제와 미리보기는 그대로 된다.
근거와 수용 기준은 Tasks.md 2-12a 참고.

### 4.2 클립 그리드 레이아웃

세션 상세의 클립 목록은 **클립의 원래 비율을 유지**한 채 화면 폭을 채운다. 세션 방향이 하나로 정해져 있으므로 레이아웃도 자동으로 결정된다.

- **가로 세션** — 한 줄에 클립 1개 (16:9 썸네일이 폭을 가득 채움)
- **세로 세션** — 한 줄에 클립 3개 (9:16 썸네일이 나란히)

세로 클립을 가로 썸네일에 잘라 넣거나 그 반대로 하지 않는다.

### 4.3 메인 플로우

1. 홈에서 **새 세션** 탭 → 제목 입력(선택, 비우면 날짜로 자동 생성) → 촬영 화면 진입
2. 녹화 버튼을 **탭하면 녹화 시작**. 10초에 도달하면 자동으로 끝나고, 그전에 끊고 싶으면 다시 탭
3. 첫 클립 촬영 시 기기 방향이 세션 방향으로 확정
4. 10초에 도달하면 **자동 정지** 후 클립 저장
5. 클립 저장 직후 다음 촬영 준비 상태로 복귀 (별도 확인 단계 없음)
6. 필요하면 **마지막 컷 undo**로 방금 찍은 클립 삭제
7. 촬영 종료 → 세션 상세 → **미리보기**에서 이어진 영상 확인
8. **저장** → 사진 앱에 한 편으로 저장. **이것이 세션 닫기다** — 저장에
   성공하면 세션이 닫히고 이후 읽기 전용이 된다(4.1). 실패하면 열린 채로
   남는다

### 4.4 결정 최소화 원칙

촬영 중 사용자가 내리는 결정은 **찍을지 말지**와 **방금 컷을 지울지** 두 가지뿐이다. 화질·길이·필터·순서·방향 등은 일절 묻지 않는다. 방향조차 묻지 않고 기기를 든 자세에서 자동으로 읽는다.

---

## 5. 기능 요구사항

수용 기준(AC)이 모두 통과해야 해당 기능이 완료된 것으로 본다.

### F-01. 세션 생성

- 홈에서 새 세션을 만들 수 있다
- 제목은 선택 입력. 비우면 `8월 9일 세션` 형태로 자동 생성
- 생성 시 `Documents/sessions/{uuid}/clips/` 디렉터리가 만들어진다
- 생성 직후 세션 방향은 **미정** 상태다

**AC**

- [ ] 제목 없이 생성해도 목록에 이름이 표시된다
- [ ] 생성 직후 촬영 화면으로 자동 진입한다
- [ ] 진행 중 세션이 이미 있으면 새로 만들지 않고 그 세션을 이어간다

### F-02. 10초 촬영

- 녹화 버튼을 탭하면 녹화 시작
- 10초 도달 시 자동 종료
- 녹화 중 다시 탭하면 조기 종료. 없으면 모든 클립이 10초로 강제되어 짧은 컷을 의도적으로 찍을 길이 사라진다
- 최소 길이 미만(1초)은 클립으로 저장하지 않고 폐기. 조기 종료로 1초 미만이 나오면 폐기된다
- 촬영 스펙: 30fps / H.264 / AAC, 해상도는 세션 방향에 따름

**AC**

- [ ] 10초에 정확히 자동 정지한다 (±0.2초)
- [ ] 한 세션 안의 모든 클립이 동일한 해상도·fps·코덱을 갖는다
- [ ] 탭 직후 0.5초 안에 다시 탭하면 클립이 생성되지 않는다
- [ ] 게이지가 실제 녹화 시간과 동기화되어 차오른다
- [ ] 클립 저장 후 추가 탭 없이 바로 다음 촬영이 가능하다

### F-03. 세션 방향 고정

세션 방향은 **세로 / 가로 2값**이다. 첫 클립 촬영 시점의 기기 방향으로 확정한다.

**같은 계열 안에서 180도 다르게 들어도 촬영을 막지 않는다.** 가로 세션에서 폰을 반대로 눕히거나, 세로 세션에서 거꾸로 드는 경우다. 이때는 녹화 직후 백그라운드에서 방향 교정 재인코딩(정규화)을 걸어 세션 방향에 맞춘다. 사용자는 좌우를 신경 쓰지 않는다.

- 첫 클립 촬영 시점의 기기 방향으로 세션 방향을 확정 (세로 / 가로)
- **계열 내 180도 차이**: 녹화 허용 → 저장 직후 정규화로 방향을 맞춘다
- **계열 간 불일치**(세로 세션에서 가로로 들기, 그 반대): 녹화 버튼 비활성 + 안내 표시
- 클립이 0개가 되면 방향 미정으로 초기화

계열 간을 막는 이유는 표시 규격이 다르기 때문이다. 세로 계열은 1080×1920, 가로 계열은 1920×1080이라 정규화하면 필러박스가 불가피하다. 계열 내 180도 차이는 같은 캔버스를 정확히 채우므로 무손실이다.

정규화 비용은 10초 클립 1개당 약 1.77초이며 화질은 유지된다(비트레이트 97.0%, iPhone 12 실측). 촬영 사이클 10초보다 훨씬 짧아 연달아 어긋나도 밀리지 않는다. 사용자가 기다리는 구간이 아니다. 근거와 탈락한 선택지는 Tasks.md "세션 방향 모델 — 확정", 단독 실측은 Tasks.md "B. 단일 클립 정규화 비용 실측" 참고.

**AC**

- [ ] 첫 컷을 가로로 찍으면 세션이 가로로 확정된다
- [ ] **가로 세션에서 폰을 반대로 눕혀도 녹화가 된다**
- [ ] **반대로 눕혀 찍은 클립이 완성본에서 정방향으로 나온다**
- [ ] **세로 세션에서 거꾸로 들어도 녹화되고, 완성본에서 정방향으로 나온다**
- [ ] 가로 세션에서 세로로 들면 녹화 버튼이 눌리지 않는다 (계열 간)
- [ ] 방향이 어긋난 상태에서 무엇을 해야 하는지 화면에 표시된다
- [ ] 첫 클립을 undo하면 다른 방향으로 다시 시작할 수 있다
- [ ] 기기를 천천히 기울일 때 안내가 깜빡이며 튀지 않는다
- [ ] **정규화 중에도 다음 촬영을 시작할 수 있다** _(B 라운드에서 비용·발열 영향 없음 확인, 구현 후 최종 확인)_
- [ ] **정규화에 실패한 클립은 삭제되지 않고, 세션 상세에 표시되며 완성본에서만 빠진다**

### F-04. 마지막 컷 undo

- 직전에 촬영한 클립 1개를 삭제
- 파일과 메타데이터를 함께 삭제
- 연속 undo 가능 (누를 때마다 한 개씩 뒤로)

**AC**

- [ ] undo 후 필름스트립에서 해당 클립이 사라진다
- [ ] 파일시스템에 파일이 남지 않는다
- [ ] 클립이 0개일 때 undo 버튼은 비활성 상태다

### F-05. 미리보기 재생

- 클립들을 순서대로 이어붙인 결과를 재생
- **익스포트 없이 즉시 재생.** 로딩 화면을 보여주지 않는다
- `AVMutableComposition`을 `AVPlayer`에 직접 연결

**AC**

- [ ] 진입 후 1초 이내에 재생이 시작된다
- [ ] 클립 경계에서 영상이 끊기거나 검은 프레임이 끼지 않는다
- [ ] 클립 경계에서 오디오가 튀거나 어긋나지 않는다
- [ ] 클립 30개(총 5분)에서도 위 기준을 만족한다
- [ ] 가로 세션은 가로로, 세로 세션은 세로로 꽉 차게 재생된다

### F-06. 사진 앱 저장

- 완성본을 하나의 파일로 사진 앱에 저장
- `AVAssetExportSession` + passthrough 프리셋, 재인코딩 없음
- **저장이 곧 세션 닫기다** (4.1). 성공한 뒤에만 세션이 닫힌다

**AC**

- [ ] 클립 20개 기준 3초 이내에 저장이 끝난다
- [ ] 저장된 영상의 화질이 원본 클립과 동일하다
- [ ] 사진 앱에서 재생 시 이음새가 자연스럽다
- [ ] 저장 완료가 사용자에게 명확히 표시된다
- [ ] **저장에 성공하면 세션이 닫히고 이후 읽기 전용이 된다**
- [ ] **저장에 실패하면 세션이 열린 채로 남아 다시 시도할 수 있다**
- [ ] 클립이 0개이거나 세션 방향이 정해지지 않았으면 저장할 수 없다

### F-07. 세션 목록과 상세

- 홈에서 세션 목록 조회 (최신순)
- 세션 상세에서 클립 그리드 확인, 개별 삭제
- 세션 삭제 시 디렉터리 전체 제거

**AC**

- [ ] 앱을 껐다 켜도 세션과 클립이 그대로 남아 있다
- [ ] 세션 삭제 후 해당 디렉터리가 파일시스템에서 사라진다
- [ ] 클립 개별 삭제 후 나머지 클립의 순서가 빈틈 없이 유지된다
- [ ] 세션 방향에 맞는 그리드 레이아웃으로 표시된다
- [ ] **닫힌 세션에서는 클립 추가·삭제가 불가능하다** (4.1). 미리보기와 세션
      삭제는 그대로 된다
- [ ] 홈에서 닫힌 세션과 진행 중 세션이 구분된다

---

## 6. 비기능 요구사항

|항목|기준|
|---|---|
|촬영 진입 속도|세션 생성 후 프리뷰 표시까지 1초 이내|
|미리보기 지연|익스포트 없음. 진입 후 1초 이내 재생 시작|
|저장 속도|passthrough. 클립 20개 기준 3초 이내|
|저장 위치|영상은 파일시스템, 메타데이터는 SwiftData|
|네트워크|사용하지 않음. 전 기능 오프라인 동작|
|최소 지원|iOS 17+ (SwiftData 요구사항)|

---

## 7. 데이터 모델 (개요)

```
Session
  ├ id: UUID
  ├ title: String
  ├ createdAt: Date
  ├ orientation: Orientation?   // nil = 미정, 첫 클립이 확정
  ├ isClosed: Bool             // 사진 앱 저장 성공 시에만 true (F-06).
                               // 닫히면 읽기 전용. 재개방은 없다
  └ clips: [Clip]               // order 기준 정렬

Clip
  ├ id: UUID
  ├ order: Int
  ├ fileName: String            // 세션 디렉터리 기준 상대 경로
  ├ duration: Double
  ├ recordedAt: Date
  └ session: Session

Orientation: portrait | landscape   // 2값 확정 (1-21). 계열 내 180도 차이는
                                    // 녹화 직후 정규화가 흡수한다
```

**방향은 2값으로 충분하다.** 가로L/가로R을 구분하지 않는 이유는, 어긋난 클립을 저장 시점에 정규화해 세션 방향으로 맞추기 때문이다. 데이터에는 이미 정방향인 클립만 남으므로 좌우를 기록할 필요가 없다. 세로/거꾸로도 같다.

**원칙: 절대 경로를 저장하지 않는다.** iOS는 앱 재설치·복원 시 컨테이너 경로가 바뀌므로, 파일명만 저장하고 경로는 런타임에 조합한다.

---

## 8. 예외 상황

구현 시 반드시 다뤄야 하는 케이스. 대부분 실기기에서만 재현된다.

|상황|처리|
|---|---|
|카메라·마이크 권한 거부|안내 화면 + 설정 앱으로 이동하는 버튼|
|사진 앱 접근 권한 거부|저장 시점에 요청. 거부 시 안내|
|세션 방향과 기기 방향 불일치|녹화 버튼 비활성 + 안내. 기울기 경계에서 안내가 떨리지 않도록 임계값 처리|
|녹화 중 기기 회전|진행 중인 녹화는 중단하지 않는다. 이미 시작된 클립은 시작 시점 방향 유지. **실측 확인(iPhone 12)**: 네 방향 시작 6건 전부에서 파일 transform이 시작 시점 각도 값으로 기록됨. 녹화 중 각도가 최대 3단계(270→0→90→180) 변한 경우에도 유지. duration 9.960~9.962초로 회전이 녹화를 중단시킨 사례 없음|
|녹화 중 전화 수신|세션 인터럽션 감지 → 현재 클립 중단. 부분 녹화분 처리는 미정|
|녹화 중 백그라운드 진입|위와 동일 처리|
|저장 공간 부족|촬영 진입 전 여유 공간 확인, 부족 시 사전 경고|
|앱 강제 종료 후 재실행|메타데이터에 없는 고아 파일 정리, 진행 중 세션 복구|
|클립 0개 상태로 미리보기 진입|버튼 비활성화|
|파일은 있는데 메타데이터가 없거나 반대|불일치 감지 시 정합성 복구 로직|

**전화 수신 시 부분 녹화분 처리는 사양이 미정이다.** 구현 단계에서 실제 동작을 확인한 뒤 결정한다.

---

## 9. 설계는 확정, 구현은 v0.2

지금 결정해두되 v0.1에서는 만들지 않는다. 나중에 붙여도 앞선 작업을 버리지 않는 기능들이다.

### 9.1 자막 (날짜 · 도시)

**완성본 전체에 상시 표시되는 오버레이.** 특정 클립에만 나오거나 중간에 사라지지 않고, 재생 내내 같은 자리에 떠 있는다.

```
┌─────────────────────────┐
│                         │
│                         │
│       (영상)             │
│                         │
│  8월 9일          오사카  │
└─────────────────────────┘
```

- 좌하단: 날짜 / 우하단: 도시
- 값은 자동으로 채운다. 사용자 입력 화면을 만들지 않는다
    - 날짜 → 클립의 촬영 시각에서 추출
    - 도시 → 촬영 위치의 역지오코딩 결과 (위치 권한 필요)
- 자동으로 채운 값이 틀렸을 때만 수정할 수 있게 한다 (선택적)

**미결 사항**

- 세션이 여러 날에 걸칠 때 날짜 표기 (`8.9` vs `8.9–8.11`)
- 세션이 여러 도시에 걸칠 때 도시 표기
- 세로 세션에도 같은 레이아웃을 적용할지
- 위치 권한을 거부했을 때 도시 자리를 비울지, 자막 자체를 끌지

**저장 경로에 미치는 영향 — 중요**

자막을 얹으면 픽셀을 새로 그려야 하므로 `AVVideoComposition`이 붙고 **재인코딩이 발생한다.** passthrough의 즉시 저장이 성립하지 않는다. 따라서 v0.2부터 저장 경로가 둘로 나뉜다.

|경로|내용|비용|
|---|---|---|
|그냥 저장|자막 없음, passthrough|즉시|
|꾸며서 저장|자막 포함, 재인코딩|영상 길이에 비례|

이 분기를 사용자에게 질문으로 던지지 않을 방법을 v0.2 설계 시 찾는다. 질문을 던지는 순간 "결정 최소화" 원칙이 깨진다.

### 9.2 완성본 분할 화면 배치

완성본 한 프레임에 여러 영역을 두고, 각 영역이 서로 다른 영상을 보여준다.
지금까지의 완성본은 클립을 **순서대로 이어붙인** 것이었고(2장 용어), 이
기능은 그 정의를 넓힌다.

**두 개의 독립된 축이다.** 배치는 공유하지만 소비하는 클립 수가 달라
설계도 비용도 따로 봐야 한다.

|축|무엇이 들어가나|10초당 소비|크롭 손실|
|---|---|---|---|
|**(A) 다중 클립**|서로 다른 클립 3~5개|3~5개|배치·방향에 따라 5% ~ 68%|
|**(B) 단일 클립 분할**|한 클립을 영역별로 다르게 샘플링|1개|**없음**|

(B)는 (A)의 대안이 아니라 보완재다. 아래 "빈 슬롯" 항목 참고.

---

#### 배치 네 가지

네 배치는 전부 **긴 변 1920을 640씩 3등분**한 것이다. 세로 캔버스
(1080×1920)와 가로 캔버스(1920×1080) 모두 긴 변이 1920이라 같은
분할이 성립한다.

```
배치 1 — 세로 3개 나란히          배치 2 — 가로 3개 스택
가로 캔버스 1920×1080             세로 캔버스 1080×1920

┌─────┬─────┬─────┐              ┌───────────────┐
│     │     │     │              │   1080×640    │
│ 640 │ 640 │ 640 │              ├───────────────┤
│  ×  │  ×  │  ×  │              │   1080×640    │
│1080 │1080 │1080 │              ├───────────────┤
│     │     │     │              │   1080×640    │
└─────┴─────┴─────┘              └───────────────┘
슬롯 비율 0.593 (≈9:16)           슬롯 비율 1.688 (≈16:9)


배치 3 — 세로 2 + 가운데 가로 3   배치 4 — 가로 2 + 사이 세로 3
가로 캔버스 1920×1080             세로 캔버스 1080×1920

┌─────┬─────┬─────┐              ┌───────────────┐
│     │640×3│     │              │   1080×640    │
│ 640 ├──60─┤ 640 │              ├────┬────┬─────┤
│  ×  │640×3│  ×  │              │360 │360 │ 360 │
│1080 ├──60─┤1080 │              │×640│×640│×640 │
│     │640×3│     │              ├────┴────┴─────┤
└─────┴─────┴─────┘              │   1080×640    │
                                 └───────────────┘
가운데 640×360 = 정확히 16:9      가운데 360×640 = 정확히 9:16
```

**배치 3과 4는 서로 전치 관계다.** 3은 1의 가운데 열을 3행으로,
4는 2의 가운데 줄을 3열로 나눈 것이다.

---

#### (A) 다중 클립 — 캔버스가 세션 방향과 **반대**다

슬롯 모양에 맞는 클립을 넣으려면 캔버스를 뒤집어야 한다.

|배치|슬롯 모양|맞는 세션|캔버스|슬롯 수|손실|
|---|---|---|---|---|---|
|1|9:16|**세로**|가로|3|약 5%|
|2|16:9|**가로**|세로|3|약 5%|
|3|섞임|—|가로|5|5% / 68% 혼재|
|4|섞임|—|세로|5|5% / 68% 혼재|

**배치 3·4는 (A)에 잘 맞지 않는다.** 슬롯 모양이 두 종류인데 한 세션의
클립은 한 계열뿐이라, 어느 쪽이든 한 종류는 프레임의 3분의 2를 버린다.
9:16 클립을 16:9 슬롯에 넣으면 높이 1920이 607.5로 줄어 **68%가 잘린다.**
어디를 남길지 알 방법이 없어 센터 크롭 외에 선택지가 없고, 피사체를
가장자리에 두고 찍었으면 잘려나간다.

---

#### (B) 단일 클립 분할 — 손실이 0이고 캔버스가 세션과 **같다**

한 클립을 슬롯마다 다른 영역으로 샘플링한다. 슬롯들의 샘플링 영역이
원본 프레임을 빈틈없이 덮으므로 버려지는 픽셀이 없다.

|배치|캔버스|원본|자르는 방식|
|---|---|---|---|
|1|가로|1920×1080|640 폭 세 조각|
|2|세로|1080×1920|640 높이 세 조각|
|3|가로|1920×1080|640 폭 세 조각, 가운데를 다시 360 높이 셋으로|
|4|세로|1080×1920|640 높이 세 조각, 가운데를 다시 360 폭 셋으로|

**네 배치 모두 나눗셈이 정확히 떨어진다.** 반올림도 남는 픽셀도 없다.
그리고 캔버스 방향이 세션 방향과 일치하므로 (A)에서 생기는 캔버스 반전
문제가 없다.

**거터가 곧 효과다.** 슬롯 사이 간격이 0이면 결과가 원본과 픽셀 단위로
같아져서 아무것도 안 보인다. 위 그림의 60px 간격이 그 자리이며, 간격
없이는 이 기능이 존재하지 않는다.

---

#### 미결 사항

- **배치를 누가 고르나.** 세션 방향이 2값인데 배치가 4개다. 사용자가
  고르면 4.4 결정 최소화 원칙에 걸리고, 앱이 고르면 기준이 필요하다.
  (A)에서는 세션 방향이 배치 1·2를 자동으로 가르므로 남는 선택은
  "단순(3슬롯) / 중첩(5슬롯)" 하나뿐이다
- **시간축.** 한 화면이 10초인지, 슬롯마다 다른 길이인지. (A)는 30클립
  세션이 100초가 아니라 10화면이 되므로 완성본 길이가 3분의 1로 줄어든다
- **빈 슬롯.** (A) 3분할에 클립이 7개면 마지막 화면에 두 칸이 빈다.
  **(B)로 채우는 것이 유력한 답이다** — 남는 자리를 앞 클립의 다른 영역으로
  메우면 빈 칸도 억지 반복도 없다
- **정적인 컷에서 (B)가 격자를 얹은 것으로만 읽힐 수 있다.** 슬라이스마다
  재생 시점을 조금씩 어긋내는 처리가 필요한지
- **(B)에서 클립을 하나만 쓰는데 왜 나누나** — 시각 효과로서의 값어치를
  실제 영상으로 확인해야 한다. 계산상 성립하는 것과 볼 만한 것은 다르다

#### 측정하지 않은 것 — 중요

**동시 디코딩 비용을 잰 적이 없다.** 같은 소스를 여러 트랙에 넣으면
디코더가 슬롯 수만큼 붙을 가능성이 있고, 한 번 디코딩해서 여러 번 그리는
경로(커스텀 컴포지터)가 있으나 실측이 없다. 배치 3·4는 슬롯이 5개다.

**기존 측정치를 여기에 전용하지 마라.** 1-21의 방향 교정 0.229배는
입력 1개 → 출력 1개, 동일 규격 변환을 잰 값이다. 다중 디코딩 합성은
다른 워크로드이며 **자릿수도 말할 수 없는 상태다.** 실측 전에는 추정치를
만들지 않는다.

F-05가 "익스포트 없이 즉시 재생, 로딩 화면 없음"이므로 미리보기가 이
비용에 직접 걸린다. v0.2 착수 시 첫 라운드에서 측정한다.

#### 저장 경로에 미치는 영향

자막(9.1)과 같다. `AVVideoComposition`이 붙어 재인코딩이 발생하므로
passthrough 즉시 저장이 성립하지 않는다. 9.1이 만든 "꾸며서 저장" 경로에
합류하며 새 경로를 만들지 않는다.

|경로|내용|비용|
|---|---|---|
|그냥 저장|이어붙이기만, passthrough|즉시|
|꾸며서 저장|자막 · 분할 배치, 재인코딩|영상 길이에 비례|

**F-06의 "클립 20개 3초 이내"는 이 경로에 적용되지 않는다.** 그 기준은
passthrough 전제로 세운 값이다. 꾸며서 저장의 목표치는 v0.2에서 실측 후
정한다.

#### v0.1에 영향을 주지 않는 이유

내보내기 단계에만 붙는다. 촬영 파이프라인 · 세션 방향 모델 · 정규화
그룹(2-D) 어느 것도 건드리지 않는다. 특히 **한 세션에 두 계열이 섞일
필요가 없다** — 슬롯 모양과 클립 방향의 결합이 크롭으로 끊어지고,
(B)에서는 애초에 클립이 하나다. 계열 간 혼재 차단(10장)은 그대로 유지된다.

---

## 10. 비범위 (v0.1에서 만들지 않는 것)

의도적으로 뺀 것들이다. 개발 중 "이거 있으면 좋겠는데"가 떠오르면 이 목록을 먼저 확인한다.

- 자막 _(설계 확정, 구현은 v0.2 — 9.1 참조)_
- 완성본 분할 화면 배치 _(설계 확정, 구현은 v0.2 — 9.2 참조)_
- BGM, 비트 기반 컷 전환
- 클립 순서 변경 (드래그 재정렬)
- 전환 효과, 필터, 색보정
- 클립 트리밍
- 주간 리캡 알림
- 공유, 소셜, 계정
- 클라우드 백업
- 한 세션 안에서 **계열을 넘는** 방향 섞기 (세로 세션에 가로 클립, 그 반대) — 표시 규격이 달라 필러박스가 불가피하다. **계열 안의 180도 차이(가로L/가로R, 세로/거꾸로)는 v0.1 범위에 들어온다** — 녹화를 막지 않고 정규화로 흡수한다 (F-03)
- 화질 옵션, 클립 길이 옵션
- 전면 카메라 전환 _(스펙 통일 검증 후 v0.2에서 판단)_

---

## 11. v0.1 완료 판정

아래를 모두 만족하면 v0.1 완료로 본다.

- [ ] 실기기에서 세션을 만들고 클립 10개 이상을 촬영할 수 있다
- [ ] 세로 세션과 가로 세션을 각각 만들어 끝까지 완료할 수 있다
- [ ] 앱을 껐다 켜도 데이터가 유지된다
- [ ] 미리보기가 로딩 없이 즉시 재생되고 이음새가 자연스럽다
- [ ] 사진 앱에 저장된 영상을 다른 앱에서 정상 재생할 수 있다
- [ ] 권한 거부 상태에서 앱이 크래시하지 않는다
- [ ] 위 과정 전체를 **편집 화면 없이** 완료할 수 있다

---

## 12. 검증 계획

v0.1 완성 후 **실제 나들이나 여행을 한 번 다녀오면서 직접 사용한다.** 기능을 더 붙이기 전에 이 단계를 반드시 거친다.

확인할 것:

1. 촬영 중 앱을 여는 게 번거롭지 않았는가
2. 10초가 짧다고 느꼈는가, 길다고 느꼈는가
3. 결과물을 남에게 보여줄 만하다고 느꼈는가
4. 10초 컷이 딱딱 끊겨서 지루한가 — 전환 효과나 가변 길이가 필요한지 판단
5. 실제로 몇 개의 클립을 찍었는가 — 예상보다 적다면 촬영 UX 문제
6. 세션 방향 고정이 답답하게 느껴졌는가 — 방향을 바꾸고 싶은 순간이 있었는가

이 결과에 따라 v0.2 우선순위를 정한다.

---

_문서 버전 v0.3 · 대상 릴리스 v0.1(내부 검증용) · 상태: 작성 완료, 구현 전_